# Audit 2026-05-17 — CertQuests (itcertifweb)

Weekly scheduled sweep. Working tree was clean on `main` (36 commits pulled since last week).

## Auto-fixed (this run)

### SEO 🟡 → ✅
- **sitemap.xml**: added 11 missing pages discovered on disk but absent from the index. Site now publishes 178 URLs (was 167) with zero duplicates. Coverage for `careers/`, `news/`, `failbase/`, `certifications/` directories is now 100%.
  - `/careers/sc-200-interview-questions-2026/`
  - `/careers/sysadmin-to-sre-roadmap-2026/`
  - `/news/aws-security-specialty-scs-c02/`
  - `/news/azure-az104-administrator-associate/`
  - `/news/azure-az500-security-engineer/`
  - `/news/cisco-devnet-associate-2026/`
  - `/news/cka-certified-kubernetes-administrator-2026/`
  - `/news/comptia-cloud-plus-cv0-004/`
  - `/news/comptia-cysa-plus-cs0-003/`
  - `/news/comptia-network-plus-n10-009/`
  - `/news/gcp-professional-cloud-security-engineer/`

## Open — needs manual attention

### Security 🟢
- **Secret scan: clean.** Only grep hit is the legitimate `AKIA...` prose in `news/aws-72k-bill-overnight/index.html:310` (educational article about leaked-key scanners). Same false positive as last week.
- `.gitignore` covers `.env*`, `.dev.vars*`, `__pycache__/`, `.wrangler`. No tracked credential files.
- `src/auth.js:30-33` Supabase config verified — decoded JWT payload shows `"role":"anon"` (public key, safe in client). Service-role key is not present anywhere in the tree.
- 28 `.innerHTML =` sites in `src/*.js`; spot-checked the post-2026-05-10 additions (`mascot-cheer.js:41`, `pack-picker.js`, `hud.js`) — all use template literals composed from trusted internal values or pre-escaped strings. No user-content sinks introduced this week.
- No `eval(` / `new Function(` anywhere in `src/`.

### SEO 🟡
- **Title-length overruns** (>60 chars hurt CTR by truncation in SERPs). Sampled 10 pages, 7 fail:
  - `train.html` (68), `news/aws-72k-bill-overnight/` (78), `careers/sc-200-interview-questions-2026/` (83), `careers/aws-sap-c02-worth-it-2026/` (90), `failbase/theranos-fraude-elizabeth-holmes/` (103), `failbase/google-stadia-cloud-gaming/` (103), `path.html` (63).
  - **Skipped — reason:** title rewrites are copywriting, outside the auto-fix mandate.
- **Meta-description overruns** (>160 chars get truncated). Sampled 10 pages, 8 fail. Worst offenders are the two new FailBase pages (494 and 439 chars) and several careers articles in the 200–260 range.
  - **Skipped — reason:** copy decision; needs editorial trim.
- `/learning/index.html` hub page — **shipped 2026-05-18** (domain-grouped library, self-canonical, ItemList JSON-LD, sitemap entry). `/certifications/index.html` already exists in the tree — the earlier flag was stale.
- JSON-LD coverage on `news/*/index.html`, `careers/*/index.html`, `failbase/*/index.html` — **100% (32/32)**.
- `robots.txt` correct (allow-all except `/stats.html`, sitemap reference present).
- `<html lang>` present on every page in the deployed tree. (Two `docs/email-templates/*.html` files use the xhtml form `<html xmlns="..." lang="en">` further down the file — fine; templates aren't shipped.)

### Question-bank integrity 🟢
- Validated all **56 packs / 2,762 questions**. **Zero structural issues**: every question has 4 options, valid `correct` index (or array for multi-correct), non-empty `explanation`, no duplicate prompts.
- **Staleness flag SKIPPED — reason:** packs don't carry a `last_updated` / `version` field by convention. Last-touched data lives in `git log` instead. Would require a schema decision to enable; flagged for future ticket.

### Bugs / Code quality 🟢
- Parse-checked every `src/*.js` modified in the last 7 days (5 files: `daily.js`, `hearts.js`, `mascot-cheer.js`, `pack-picker.js`, `screens/home.js`, `screens/results.js`, `profile.js`, `cq-core.js`). All clean.
- Unit tests: **120/120 passing** via `npm test`. **Fixed 2026-05-19** — `scripts.test` was changed from `node --test test/` to `node --test test/*.js`; Node 22 no longer auto-recurses the directory form, which made the script resolve to a non-existent `/test` module.
- `git grep` for `TODO BUG | FIXME | XXX` in `src/*.js` and `*.html`: **zero hits**.
- Sampled 30 internal links from across `*.html` — all resolve to existing files/directories.

### Performance 🟡
- Every `<img>` tag without `loading="lazy"` is a header-logo favicon (above-the-fold). 100% of below-fold images already lazy. **Nothing to auto-fix.**
- Every `<img>` has an `alt` attribute. **Nothing to auto-fix.**
- Every `target="_blank"` link has `rel="..."`. **Nothing to auto-fix.**
- `src/styles/main.css` is **114 KB**, `src/styles/desktop.css` is **101 KB** — both over the 80 KB threshold and growing every week. Combined ~215 KB is heavy for Cloudflare Pages first-paint. **Open — needs manual triage:** a PurgeCSS / unused-rule pass would likely strip 30–40%, but the bundle is intermixed with mobile-shell and game-HUD rules used only on `/path.html`. Splitting into `desktop.css` / `path.css` / `mobile.css` route bundles is the correct fix; not safe to auto-strip rules.
- `index.html` carries **112 KB of inline `<style>`** (above the 50 KB threshold). Per CLAUDE.md this is intentional — the inline block contains the `mobile-v10-override` cascade gate that must load before external CSS to win the cascade. **Skipped — reason:** documented architectural choice; auto-extraction would break the override layer.

### Repo hygiene
- Orphan branches on `origin` are still present (carry-over from last two weekly audits). Not auto-deleted because some may still be live. Recommend a one-shot manual sweep.

## Scores (1–10)
- Security: **10**
- SEO: **9** (sitemap drift fixed; title/description overruns + missing hubs cap it at 9)
- Content: **10** (zero question-bank defects across all 56 packs)
- Stability: **10** (`npm test` script repaired 2026-05-19; 120/120 passing)
- Performance: **8** (CSS bundles still trending up; index.html inline style at 112 KB)
- Accessibility: **9** (every image has alt, every link has discernible text)
- **Overall: 9.2**

## Trend vs last week (AUDIT-2026-05-11.md)
- **+0 net.** Last week was 9.2; this week is 9.2.
- **Wins:** sitemap fully back in sync after a week of new content (+11 URLs); question-bank integrity perfect across 2.8k questions.
- **Carries over:** `/learning/` + `/certifications/` hubs absent; orphan branches; CSS bundles bloating.
- **New regression:** title/description length discipline slipped on the 4 new pages (FailBase + careers) added since last audit — they all exceed SERP-truncation thresholds.

{
  "packId": "cks",
  "title": "Certified Kubernetes Security Specialist (CKS)",
  "brandName": "CNCF",
  "brandColor": "#60a5fa",
  "chapters": [
    {
      "id": "ch1",
      "title": "Secrets",
      "tag": "secrets",
      "nodes": [
        {
          "id": "c1-concept",
          "type": "concept",
          "title": "Incident response: Key Ideas",
          "content": "IR follows a documented lifecycle so a panicked 3am pager doesn't turn into 'what do we do now?'. The classic NIST flow is preparation, detection, containment, eradication, recovery, lessons learned. Exam questions test which phase owns which action.",
          "flashcards": [
            {
              "front": "NIST IR lifecycle — name the phases.",
              "back": "(1) Preparation: playbooks, contact lists, hardened images. (2) Detection & Analysis: SIEM alerts, IDS, user reports. (3) Containment: short-term (isolate host) + long-term (block C2 domains). (4) Eradication: remove malware, close vuln. (5) Recovery: restore from clean backup, monitor for re-infection. (6) Lessons learned: post-mortem within 2 weeks, action items tracked."
            },
            {
              "front": "Containment — short-term vs long-term?",
              "back": "Short-term: stop the bleed fast — pull the network cable, kill the user session, block the IP. May be reversible. Long-term: durable fix — patch the vuln, rotate creds, deploy IPS rule, sometimes rebuild from scratch. Don't skip short-term: every minute of attacker dwell time multiplies damage."
            },
            {
              "front": "Chain of custody — why does it matter?",
              "back": "If the incident leads to prosecution, evidence must be admissible — every transfer, image, and access logged from acquisition to court. Drives required to be hashed (SHA-256) immediately, copies (not originals) used for analysis, originals stored in a tamper-evident bag with signatures. Skip this and the lawyer dismisses your case."
            },
            {
              "front": "What's a post-mortem (lessons learned) supposed to produce?",
              "back": "Blameless write-up within 2 weeks: timeline, what worked, what didn't, action items with owners + due dates. The goal is process improvement, not punishment — if engineers fear blame they hide details and the next incident is worse. Track action items in your normal sprint board, not in a doc nobody reopens."
            }
          ],
          "source": "concept-library",
          "sourceTag": "incident-response"
        },
        {
          "id": "c1-quiz1",
          "type": "quiz",
          "title": "Secrets — Drill 1",
          "questionIds": [
            "cks-036",
            "cks-027",
            "cks-033",
            "cks-034",
            "cks-057"
          ]
        },
        {
          "id": "c1-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "A Secret must never appear in `kubectl describe pod` output or in `/proc/<pid>/environ`. Which consumption pattern satisfies both?",
                "choices": [
                  "Load it via `envFrom.secretRef` for convenience",
                  "Reference it via `env[].valueFrom.secretKeyRef`, one value at a time",
                  "Mount the Secret as a projected volume file and have the app read it from disk",
                  "Convert it to a ConfigMap with `immutable: true` — ConfigMaps encrypt at rest by default"
                ],
                "correctIndex": 2,
                "explanation": "Volume-mounted Secret files never appear in the pod's environment (`/proc/<pid>/environ`) and are not printed by `kubectl describe pod`; only the Secret name is shown. Both `env.valueFrom.secretKeyRef` and `envFrom.secretRef` place the resolved values into the process environment, where they are visible to anyone with `describe` rights or read access to `/proc`. ConfigMaps are not encrypted at rest by default (they inherit whatever EncryptionConfiguration is set for `configmaps`) and are not designed for secret data."
              },
              {
                "q": "A leaked Secret's external value has been rotated at the third-party provider. What completes the remediation inside Kubernetes?",
                "choices": [
                  "Delete the old Secret and recreate it with the new value; then `kubectl rollout restart` any Deployment that consumed it as environment variables",
                  "Set `immutable: true` on the existing Secret to prevent further tampering — the flag also invalidates the old token cached in running pods so they re-authenticate against the new value",
                  "`kubectl patch secret <name> --patch '{\"data\": null}'` to wipe the stored payload without deleting the object, so consumers keep their existing Secret reference and Kubernetes re-populates the payload from a backing store",
                  "Bump the Secret's `metadata.resourceVersion` field to force the kubelet's Secret watcher to fire an update event and re-sync all consumer pod mounts and env values"
                ],
                "correctIndex": 0,
                "explanation": "Recreating the object with the new value is the only way to change its data. Deployments that read the Secret via `env`/`envFrom` bake values in at pod start, so a rolling restart is required; volume-mounted Secrets get refreshed automatically by the kubelet, no restart needed. `immutable: true` only prevents future edits — it doesn't rotate the value. Patching `data: null` corrupts the object; readers get empty strings. `resourceVersion` is a server-managed optimistic-lock field; clients cannot set it manually."
              },
              {
                "q": "How is at-rest encryption for Kubernetes Secrets in etcd enabled using the built-in mechanism?",
                "choices": [
                  "Add the annotation `encryption: true` to each Secret manifest — kube-apiserver's built-in EncryptionAnnotationController honours the annotation and transparently AES-encrypts the entry before it reaches etcd",
                  "Write an `EncryptionConfiguration` listing the `secrets` resource with an `aescbc` or KMS provider, then start kube-apiserver with `--encryption-provider-config=<path>`",
                  "Set `etcd.dataEncryption: true` in the kubeadm `ClusterConfiguration` under `etcd.local` so etcd generates a data-key on init and encrypts its own on-disk pages before the API server sees them",
                  "Set `--allow-privileged=false` on kube-apiserver so the process runs in a hardened profile that implicitly enables the EncryptionAtRest feature gate for `secrets` and `configmaps`"
                ],
                "correctIndex": 1,
                "explanation": "The API server (not etcd itself) performs the encryption; you point it at an `EncryptionConfiguration` file with the desired provider (`aescbc`, `aesgcm`, `secretbox`, or `kms`). Newly written Secrets are encrypted; to encrypt pre-existing rows, rewrite them (`kubectl get secrets -A -o json | kubectl replace -f -`). The other three describe fields/flags that don't exist. Notably `--allow-privileged` controls whether pods can request privileged containers; it has no relationship to storage encryption."
              },
              {
                "q": "Why are Kubernetes Secrets not truly secure by default?",
                "choices": [
                  "Because RBAC rules on the built-in `secrets` resource are advisory in default kubeadm installs and any pod in the namespace can read them until a NetworkPolicy on the metadata endpoint is applied",
                  "Because Secret bodies travel from kube-apiserver to etcd over plain HTTP by default and can be sniffed by anyone with control-plane node access until mutual TLS is manually enabled",
                  "Because the AES-256 key that protects each Secret lives inside the same etcd database it protects, so anyone with etcd read access can unwrap the value with a single call",
                  "Because they are stored in etcd as base64-encoded blobs — base64 is an encoding, not encryption, and trivially reversible"
                ],
                "correctIndex": 3,
                "explanation": "Without an EncryptionConfiguration, Secrets are stored in etcd as base64 payloads that anyone with etcd disk or backup access can decode with a one-liner. RBAC absolutely applies to Secrets — that is one of the tighter default protections. Communication between kube-apiserver and etcd is TLS by default in kubeadm-installed clusters. There is no default AES-encrypted-with-key-in-etcd flow; the danger is precisely that there is no encryption at all until you opt in."
              },
              {
                "q": "Attackers are reading `/proc/1/environ` inside containers to harvest credentials that were passed to the pod as environment variables. Which change most directly stops that class of leak?",
                "choices": [
                  "Stop putting credentials in the pod's `env:` block; deliver them as file mounts from Secrets (or via a secrets-manager sidecar / CSI) so they never appear in the process environment",
                  "Set `hostPID: true` cluster-wide so the pod's `/proc` view merges with the host's — a unified PID namespace lets an admission control filter see and reject `/proc/1/environ` reads centrally",
                  "Add a NetworkPolicy that denies all egress from the pod so any credentials that do end up in the environment cannot be exfiltrated to an attacker-controlled destination",
                  "Switch the container runtime from containerd to CRI-O — CRI-O's default profile masks `/proc/<pid>/environ` for non-init processes, while containerd exposes it to the container root user"
                ],
                "correctIndex": 0,
                "explanation": "Once a value is in the process environment, `/proc/<pid>/environ` reveals it to anyone with read access on the pod — including an attacker with RCE. Mounting the Secret as a file with restrictive perms (or fetching at runtime via Vault Agent/CSI) keeps the value out of the environment block entirely. `hostPID: true` broadens process visibility across the host rather than restricting it. An egress deny stops exfiltration but doesn't stop the local read of `environ`. Both containerd and CRI-O expose `environ` identically — swapping runtimes doesn't affect this leak."
              }
            ]
          },
          "title": "Secrets — Lightning Round"
        },
        {
          "id": "c1-boss",
          "type": "subboss",
          "title": "Boss: Secrets",
          "questionIds": [
            "cks-036",
            "cks-027",
            "cks-033",
            "cks-034",
            "cks-057"
          ]
        },
        {
          "id": "ch1-bonus1",
          "type": "minigame",
          "mode": "acronym-decoder",
          "title": "Decode: PSA",
          "data": {
            "acronym": "PSA",
            "correct": {
              "fullName": "Pod Security Admission",
              "useCase": "The built-in admission controller that enforces the Pod Security Standards (privileged / baseline / restricted) per namespace; it replaced PodSecurityPolicy."
            },
            "distractors": [
              {
                "fullName": "Pod Scheduling Agent",
                "useCase": "Decides which node each pod runs on."
              },
              {
                "fullName": "Private Subnet Access",
                "useCase": "Restricts pods to a private network range."
              },
              {
                "fullName": "Policy Sync Adapter",
                "useCase": "Replicates RBAC roles across clusters."
              }
            ]
          }
        },
        {
          "id": "c1-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-1"
        }
      ]
    },
    {
      "id": "ch2",
      "title": "Pod Security Admission",
      "tag": "pod-security-admission",
      "nodes": [
        {
          "id": "c2-concept",
          "type": "concept",
          "title": "Kubernetes Pod Security Admission: Key Ideas",
          "content": "Pod Security Admission (PSA) replaced PodSecurityPolicy in Kubernetes 1.25+. It enforces three built-in security profiles at the namespace level using labels. CKS exams test the profiles and enforcement modes.",
          "flashcards": [
            {
              "front": "Three Pod Security Standards?",
              "back": "Privileged (no restrictions), Baseline (prevents known privilege escalations), Restricted (strongly hardened — requires non-root, drops all capabilities, enforces seccompProfile)."
            },
            {
              "front": "Three PSA enforcement modes?",
              "back": "enforce (reject violating Pods), audit (log violations but allow), warn (display warning to the user but allow). A namespace can have different levels per mode."
            },
            {
              "front": "How to label a namespace for PSA?",
              "back": "kubectl label namespace <ns> pod-security.kubernetes.io/enforce=restricted. Add --version=latest to pin the policy version evaluated."
            },
            {
              "front": "What does seccompProfile: RuntimeDefault do?",
              "back": "Applies the container runtime's built-in seccomp filter (e.g., Docker's default), which blocks ~300+ dangerous syscalls — required by the Restricted profile."
            }
          ],
          "source": "concept-library",
          "sourceTag": "pod-security-admission"
        },
        {
          "id": "c2-quiz1",
          "type": "quiz",
          "title": "Pod Security Admission — Drill 1",
          "questionIds": [
            "cks-013",
            "cks-025",
            "cks-029",
            "cks-032"
          ]
        },
        {
          "id": "c2-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "Which built-in Kubernetes admission controller — GA since v1.25 — enforces that Pods in a namespace cannot run with `privileged: true`?",
                "choices": [
                  "PodSecurity, with the namespace labelled `pod-security.kubernetes.io/enforce=baseline` (or stricter)",
                  "SecurityContextDeny, enabled in the API server's admission plugin list",
                  "NodeRestriction, applied at the kubelet layer",
                  "ValidatingAdmissionWebhook, pointing to an in-tree Kubernetes webhook that ships with the API server"
                ],
                "correctIndex": 0,
                "explanation": "`PodSecurity` implements the Pod Security Standards. The `baseline` profile prohibits privileged containers (as does the stricter `restricted`), and is activated by labelling the namespace with `pod-security.kubernetes.io/enforce=baseline`. `SecurityContextDeny` was deprecated and removed — you cannot rely on it. `NodeRestriction` limits what a kubelet can write via the API; it has no visibility into pod security context values. `ValidatingAdmissionWebhook` is the generic hook mechanism — there is no in-tree webhook that ships with Kubernetes for this; you'd have to deploy an external policy engine."
              },
              {
                "q": "The `payments` namespace is labelled `pod-security.kubernetes.io/enforce=restricted`. A deployment is rejected because its image runs as root. Which fix keeps the enforcement in place and lets the pod through?",
                "choices": [
                  "Add `runAsNonRoot: true` and `runAsUser: 1000` on the container `securityContext`, or rebuild the image with a non-root `USER` line",
                  "Delete the namespace's LimitRange object — LimitRanges default new pods' security context to `runAsNonRoot: true` and dropping the LimitRange removes that default",
                  "Add `privileged: true` on the container `securityContext` so the container's security profile is granted admin exemptions and bypasses the `restricted` non-root check",
                  "Change the namespace's enforce label from `restricted` to `baseline` so root pods are admitted while other privileged-container prohibitions remain in force"
                ],
                "correctIndex": 0,
                "explanation": "The `restricted` standard requires a non-root UID; declaring one in `securityContext` (or setting `USER` in the Dockerfile) satisfies it without loosening the namespace policy. Downgrading to `baseline` weakens security — not a fix, a workaround. LimitRange caps CPU/memory, it has nothing to do with user identity. `privileged: true` is explicitly forbidden by both `baseline` and `restricted`, so it would make the rejection worse."
              },
              {
                "q": "You want `production` to REJECT non-compliant pods, `staging` to WARN, and `development` to log VIOLATIONS to the audit trail. Which set of namespace labels achieves that?",
                "choices": [
                  "`production: pod-security.kubernetes.io/enforce=restricted`, `staging: pod-security.kubernetes.io/warn=baseline`, `development: pod-security.kubernetes.io/audit=baseline`",
                  "Enable the `PodSecurity` feature gate and set the mode on the pods themselves",
                  "A single ClusterPolicy specifying `enforce: restricted, warn: baseline, audit: baseline`",
                  "`production: pod-security.kubernetes.io/mode=enforce`, `staging: pod-security.kubernetes.io/mode=warn`, `development: pod-security.kubernetes.io/mode=audit`"
                ],
                "correctIndex": 0,
                "explanation": "The label key encodes the *mode* (`enforce`, `warn`, `audit`) and the value is the *level* (`privileged`, `baseline`, `restricted`). Labels like `pod-security.kubernetes.io/mode=...` invent a non-existent `mode` key — the mode is part of the key, not the value. `PodSecurity` has been stable for many releases; nothing configures modes at the pod level. Vanilla Kubernetes has no `ClusterPolicy` type — that belongs to Kyverno."
              },
              {
                "q": "You just labelled `prod` with `pod-security.kubernetes.io/enforce=restricted` and several existing deployments break. What is the least-disruptive way to identify every violation before enforcing?",
                "choices": [
                  "Change enforcement to `pod-security.kubernetes.io/enforce=privileged`",
                  "Set `pod-security.kubernetes.io/enforce-version=latest` to test the newest built-in version",
                  "Move to `pod-security.kubernetes.io/enforce=baseline` for now",
                  "Switch to `pod-security.kubernetes.io/audit=restricted` — violations are logged but no pod is rejected"
                ],
                "correctIndex": 3,
                "explanation": "`audit` is the non-blocking observability mode: violations are annotated on API objects and written to the audit log, no admission is rejected. Setting `enforce=privileged` disables the policy entirely — nothing is checked. `enforce=baseline` is a valid stepping stone but still rejects `baseline` violations, so it's not the least disruptive choice. `enforce-version` selects which Kubernetes version's rules to apply, not the enforcement mode."
              }
            ]
          },
          "title": "Pod Security Admission — Lightning Round"
        },
        {
          "id": "c2-boss",
          "type": "subboss",
          "title": "Boss: Pod Security Admission",
          "questionIds": [
            "cks-013",
            "cks-025",
            "cks-029",
            "cks-032"
          ]
        },
        {
          "id": "ch2-bonus2",
          "type": "minigame",
          "mode": "scenario-builder",
          "title": "How the API server vets a request",
          "data": {
            "scenario": "A user runs kubectl apply. Order the security checks the API server applies before the object is stored.",
            "pool": [
              "Authentication",
              "Authorization (RBAC)",
              "Admission control",
              "Persist to etcd",
              "Network policy",
              "Audit logging"
            ],
            "correctOrder": [
              "Authentication",
              "Authorization (RBAC)",
              "Admission control",
              "Persist to etcd"
            ]
          },
          "explanation": "Every request to the kube-apiserver is checked in order: authenticate the identity, authorize it via RBAC, run admission controllers (mutating then validating), and finally persist the object to etcd."
        },
        {
          "id": "c2-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-2"
        }
      ]
    },
    {
      "id": "ch3",
      "title": "Mixed Topics",
      "tag": "mixed-topics",
      "nodes": [
        {
          "id": "c3-concept",
          "type": "concept",
          "title": "Mixed exam topics: Key Ideas",
          "content": "Some exam objectives don't fit neatly into a single domain — they span protocols, tools, or concepts covered briefly in multiple sections. These cards treat each item as a stand-alone fact, the way an exam question presents it: one concept, one answer.",
          "flashcards": [
            {
              "front": "What is the difference between a hub, a switch, and a router?",
              "back": "Hub: Layer 1, repeats every frame to all ports — all devices share bandwidth, collisions guaranteed. Switch: Layer 2, forwards frames using a MAC address table to only the destination port — full duplex, no collisions, each port gets full bandwidth. Router: Layer 3, makes forwarding decisions based on IP address, connects different networks. Hub is legacy; switches dominate LAN; routers are at network edges."
            },
            {
              "front": "What is RAID 5 and how many drives can it tolerate losing?",
              "back": "RAID 5 stripes data and parity across at least 3 drives. Any single drive failure can be recovered by reconstructing missing data from the remaining drives and parity. It cannot tolerate 2 simultaneous drive failures. Effective capacity = (N-1) × drive size. Read performance is excellent; write requires parity recalculation. RAID 6 tolerates 2 failures using double parity."
            },
            {
              "front": "What does a proxy server do, and how does a forward proxy differ from a reverse proxy?",
              "back": "Forward proxy: sits between clients and the internet on behalf of clients — used for content filtering, caching, and anonymizing outbound requests (the server sees the proxy's IP). Reverse proxy: sits in front of servers on behalf of the server — used for load balancing, SSL termination, caching, and hiding backend topology (the client sees the proxy's IP). Both intermediate HTTP, but they protect opposite sides."
            },
            {
              "front": "What is the purpose of a DMZ in network architecture?",
              "back": "A demilitarized zone is a network segment between the untrusted internet and the trusted internal network, typically protected by two firewalls (or two interfaces on one firewall). Public-facing services (web, email, DNS) sit in the DMZ: internet can reach them but not the internal network; if the DMZ host is compromised, the inner firewall still blocks access to the internal LAN."
            }
          ],
          "source": "concept-library",
          "sourceTag": "mixed-topics"
        },
        {
          "id": "c3-quiz1",
          "type": "quiz",
          "title": "Mixed Topics — Drill 1",
          "questionIds": [
            "cks-030",
            "cks-006",
            "cks-015",
            "cks-007",
            "cks-023"
          ]
        },
        {
          "id": "c3-quiz2",
          "type": "quiz",
          "title": "Mixed Topics — Drill 2",
          "questionIds": [
            "cks-051",
            "cks-052",
            "cks-002",
            "cks-005",
            "cks-017"
          ]
        },
        {
          "id": "c3-quiz3",
          "type": "quiz",
          "title": "Mixed Topics — Drill 3",
          "questionIds": [
            "cks-018",
            "cks-024",
            "cks-037",
            "cks-039",
            "cks-041"
          ]
        },
        {
          "id": "c3-quiz4",
          "type": "quiz",
          "title": "Mixed Topics — Drill 4",
          "questionIds": [
            "cks-046",
            "cks-053",
            "cks-055",
            "cks-026",
            "cks-040"
          ]
        },
        {
          "id": "c3-checkpoint",
          "type": "subboss",
          "title": "Checkpoint: Mixed Topics",
          "questionIds": [
            "cks-014",
            "cks-multi-1",
            "cks-050",
            "cks-054"
          ]
        },
        {
          "id": "c3-quiz5",
          "type": "quiz",
          "title": "Mixed Topics — Drill 5",
          "questionIds": [
            "cks-028",
            "cks-031",
            "cks-044",
            "cks-045",
            "cks-001"
          ]
        },
        {
          "id": "c3-quiz6",
          "type": "quiz",
          "title": "Mixed Topics — Drill 6",
          "questionIds": [
            "cks-003",
            "cks-004",
            "cks-010",
            "cks-011",
            "cks-008"
          ]
        },
        {
          "id": "c3-quiz7",
          "type": "quiz",
          "title": "Mixed Topics — Drill 7",
          "questionIds": [
            "cks-059",
            "cks-056",
            "cks-009",
            "cks-016",
            "cks-019"
          ]
        },
        {
          "id": "c3-quiz8",
          "type": "quiz",
          "title": "Mixed Topics — Drill 8",
          "questionIds": [
            "cks-021",
            "cks-022",
            "cks-038",
            "cks-043",
            "cks-047"
          ]
        },
        {
          "id": "c3-quiz9",
          "type": "quiz",
          "title": "Mixed Topics — Drill 9",
          "questionIds": [
            "cks-048",
            "cks-012",
            "cks-042",
            "cks-060"
          ]
        },
        {
          "id": "c3-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "Compliance requires that every read of a Secret is logged with the full request AND response payloads. Which combination of audit `stage` and `level` provides that?",
                "choices": [
                  "Stage `RequestReceived`, level `Metadata`",
                  "Stage `RequestReceived`, level `Request`",
                  "Stage `ResponseStarted`, level `Request`",
                  "Stage `ResponseComplete`, level `RequestResponse`"
                ],
                "correctIndex": 3,
                "explanation": "`RequestResponse` is the highest audit level and captures both bodies; `ResponseComplete` is the stage after the response has been fully sent, so the response body is available to log. `Metadata` records who/what/when but drops the bodies — exactly what compliance is asking to see. `Request` records the request body only; the read's *response* (which carries the Secret data) is not logged. `ResponseStarted` fires when the response head is written and is only useful for long-lived watches/exec streams, not point-in-time reads."
              },
              {
                "q": "You want to stream Kubernetes audit events to an external SIEM in near real time. Which built-in audit backend fits?",
                "choices": [
                  "The webhook backend — kube-apiserver POSTs audit events to an HTTP endpoint you configure with `--audit-webhook-config-file`",
                  "The dynamic backend, enabled with `--audit-dynamic-configuration=true` — an AuditSink CR then routes events to any HTTP endpoint that can serve as a SIEM receiver",
                  "The Kafka backend, enabled with `--audit-kafka-config=/etc/kubernetes/audit-kafka.yaml` — kube-apiserver publishes each event straight to the configured Kafka topic",
                  "The log backend (`--audit-log-path=/var/log/audit.log`) — kube-apiserver writes JSON events to the file and a node-local shipper such as Fluent Bit tails it to the SIEM"
                ],
                "correctIndex": 0,
                "explanation": "The webhook backend is the near-real-time option: batched POSTs of audit events to your SIEM's collector or a forwarder. The dynamic audit config was removed in Kubernetes 1.27. There is no in-tree Kafka backend for audit — Kafka integration is external (e.g., through the webhook backend feeding a Kafka producer). The log backend works but is file-based and pulls latency through whatever tails it; the webhook is the more direct streaming path."
              },
              {
                "q": "After a Secret in the `prod` namespace was deleted maliciously, which audit-log fields let you attribute the action to a specific user and object?",
                "choices": [
                  "`event.source.component`, `event.requestURI`, and `event.verb`",
                  "`annotations.authorization.k8s.io/reason`, `userAgent`, and `responseStatus.code`",
                  "`user.username`, `verb`, `objectRef.namespace`, `objectRef.resource`, `objectRef.name`",
                  "`auditID`, `sourceIPs`, and `requestReceivedTimestamp`"
                ],
                "correctIndex": 2,
                "explanation": "Together these fields identify *who* did *what* to *which* object *where*. The authorization-reason annotation only explains why the decision was allowed/denied. `userAgent` and `responseStatus.code` describe the client and the HTTP status, not the actor identity. `auditID`, `sourceIPs`, `requestReceivedTimestamp` are useful correlation metadata but don't name the user or the deleted Secret."
              },
              {
                "q": "You want to block pods that use the `:latest` image tag cluster-wide using OPA Gatekeeper. Which two resources must you author?",
                "choices": [
                  "A `ValidatingWebhookConfiguration` pointing at a Rego policy file mounted on the API server, plus a `PolicySource` object listing which paths are enforced cluster-wide",
                  "A `ConstraintTemplate` (defining the Rego logic) plus a `Constraint` (an instance targeting kinds and namespaces)",
                  "A single `Policy` object referencing Gatekeeper's built-in `no-latest-tag` template, with `parameters.excludedNamespaces` covering system namespaces that legitimately float on latest",
                  "A `ClusterRole` denying the `create` verb on pods with `image` containing `latest`"
                ],
                "correctIndex": 1,
                "explanation": "Gatekeeper's model is: a `ConstraintTemplate` CRD defines the Rego and its input schema, and a `Constraint` (a CRD instance of that template) turns it on for specified kinds/namespaces. The `ValidatingWebhookConfiguration` used by Gatekeeper is installed automatically at deploy time — you don't author it. There is no `Policy` object in vanilla Gatekeeper (that is Kyverno's type name). RBAC evaluates verb/resource, not the *contents* of a field like the image string."
              },
              {
                "q": "You want to block pods that pull images from any registry other than `registry.example.com`. Which cluster-wide control fits?",
                "choices": [
                  "Add `imagePullSecrets` in every namespace pointing only to `registry.example.com`",
                  "Add a ClusterRole that denies `create` on pods with foreign registry prefixes",
                  "Set an environment variable `ALLOWED_REGISTRIES=registry.example.com` on kube-apiserver",
                  "Deploy OPA Gatekeeper (or Kyverno) with a ConstraintTemplate that examines each container image prefix"
                ],
                "correctIndex": 3,
                "explanation": "A policy engine looking at `input.review.object.spec.containers[_].image` at admission time can reject any prefix that doesn't start with the allowed registry. `imagePullSecrets` provide credentials — a pod can still reference `docker.io/x/y` without them and the kubelet will attempt to pull anonymously. There is no such kube-apiserver env var. RBAC evaluates verb/resource combinations only; it cannot inspect a field value like `image`."
              },
              {
                "q": "You author a Gatekeeper ConstraintTemplate that blocks images whose prefix is not `registry.internal/`. Which Rego shape matches Gatekeeper's contract?",
                "choices": [
                  "`deny[msg] { not startswith(input.review.object.spec.containers[_].image, \"registry.internal/\"); msg := \"image not allowed\" }`",
                  "`violation[{\"msg\": msg}] { c := input.review.object.spec.containers[_]; not startswith(c.image, \"registry.internal/\"); msg := sprintf(\"image %v not allowed\", [c.image]) }`",
                  "`deny { every c in input.review.object.spec.containers { not startswith(c.image, \"registry.internal/\") }; msg := \"all containers must come from registry.internal\" }`",
                  "`block[msg] { image := input.spec.containers[_].image; not contains(image, \"registry.internal\"); msg := sprintf(\"blocked image %v\", [image]) }`"
                ],
                "correctIndex": 1,
                "explanation": "Gatekeeper reads `violation[{\"msg\": msg}]` rules — that fixed name and shape are part of its contract. Neither `deny` nor `block` is a Gatekeeper rule head. Rules that reference `input.spec.containers` also use the wrong input path (Gatekeeper wraps admission review data under `input.review.object`). A `deny` rule with an `every` block cannot emit a per-violation `msg`, so even if `deny` were valid it would not attach the required message field."
              },
              {
                "q": "A regulated workload needs stronger kernel isolation than the default runc container runtime. Which Kubernetes resource selects an alternative runtime such as gVisor or Kata Containers for a pod?",
                "choices": [
                  "A PodDisruptionBudget attached to the pod's controller",
                  "Setting `spec.hostPID: false` and `spec.hostNetwork: false` on the pod",
                  "A `securityContext.readOnlyRootFilesystem: true` combined with `privileged: false`",
                  "A `RuntimeClass` object, referenced by name from `spec.runtimeClassName` on the pod"
                ],
                "correctIndex": 3,
                "explanation": "`RuntimeClass` names a runtime handler (`runsc` for gVisor, `kata` for Kata) that the container runtime — configured by the node — will select instead of the default. `PodDisruptionBudget` governs voluntary evictions; unrelated to runtime choice. Read-only root FS + non-privileged is good hygiene, but the same underlying kernel and runc process still run the workload. Turning off host namespaces is baseline pod isolation, again with the default runtime — no sandbox is added."
              },
              {
                "q": "Given a containerd handler named `runsc` for gVisor, which YAML creates the RuntimeClass a pod can then reference?",
                "choices": [
                  "`apiVersion: apps/v1` / `kind: RuntimeClass` / `spec.runtime: runsc`",
                  "`apiVersion: v1` / `kind: RuntimeClass` / top-level `handler: runsc`",
                  "`apiVersion: node.k8s.io/v1` / `kind: RuntimeClass` / top-level `handler: runsc`",
                  "`apiVersion: node.k8s.io/v1beta1` / `kind: RuntimeClass` / `spec.runtimeHandler: runsc`"
                ],
                "correctIndex": 2,
                "explanation": "The stable API is `node.k8s.io/v1`, with `handler` at the root of the object (no nested `spec`). `apps/v1` hosts Deployments/StatefulSets/DaemonSets — not RuntimeClass. The core `v1` group doesn't contain RuntimeClass either. The `v1beta1` shape with `spec.runtimeHandler` is the older schema and was replaced by the stable v1 layout."
              }
            ]
          },
          "title": "Mixed Topics — Lightning Round"
        },
        {
          "id": "c3-boss",
          "type": "subboss",
          "title": "Boss: Mixed Topics",
          "questionIds": [
            "cks-020",
            "cks-035",
            "cks-049",
            "cks-058"
          ]
        },
        {
          "id": "c3-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-3"
        }
      ]
    }
  ],
  "finalBoss": {
    "id": "final-boss",
    "type": "finalboss",
    "title": "Final Exam: Certified Kubernetes Security Specialist (CKS)",
    "questionCount": 18,
    "timeMinutes": 27
  },
  "meta": {
    "generatedAt": "2026-08-10T07:17:27.221Z",
    "totalNodes": 27,
    "totalQuestions": 61,
    "nodeTypes": [
      "concept",
      "quiz",
      "minigame",
      "subboss",
      "finalboss"
    ]
  }
}
{
  "packId": "prometheus-pca",
  "title": "Prometheus Certified Associate (PCA)",
  "brandName": "Linux Foundation",
  "brandColor": "#22c55e",
  "chapters": [
    {
      "id": "ch1",
      "title": "Promql",
      "tag": "promql",
      "nodes": [
        {
          "id": "c1-concept",
          "type": "concept",
          "title": "Promql: Key Ideas",
          "content": "Quick primer on Promql. Tap to flip cards covering the most-asked exam ideas in this domain.",
          "flashcards": [
            {
              "front": "A Grafana panel that graphs request throughput over a 6-hour window queries the counter `http_requests_total` with `$__rate_interval` set to 1 minute. Which PromQL function is conventionally chosen and why?",
              "back": "`rate()` is the canonical choice for graphing counters because it averages the per-second growth across the entire range vector and accounts for counter resets, producing a smooth long-range curve. `irate()` looks at only the last two samples and is meant for high-resolution alerting on short windows — on a 6-hour panel it produces a spiky graph. `increase()` returns the total number of events in the range (not a per-second value); to graph throughput you would still wrap it as `increase(...)[1m]`-equivalent which is exactly what `rate()` does. `delta()` is intended for gauges and does NOT compensate for counter resets, so applying it to a counter gives wrong results when a process restarts."
            },
            {
              "front": "Service `api` exposes a histogram named `http_request_duration_seconds_bucket` with `le` labels in seconds (alongside `_sum` and `_count`). Which PromQL expression returns an estimate of the 95th-percentile request latency over the last 5 minutes, grouped by `job`?",
              "back": "Computing a quantile from a Prometheus histogram requires `histogram_quantile()` applied to the per-second rate of the bucket counters, summed across whatever dimensions you want to collapse while preserving `le`. The canonical form is `histogram_quantile(0.95, sum by (job, le) (rate(bucket[5m])))`. `quantile()` is an aggregation operator that takes scalar values, not histogram buckets, and would return a meaningless number across bucket samples. The `_sum / _count` ratio gives the AVERAGE latency, not the 95th percentile. The last option is syntactically wrong: `histogram_quantile` does not take a range vector as its second argument, and dividing a quantile by `_count` has no semantic meaning."
            },
            {
              "front": "An operator wants to rewrite the `path` label so values ending in `/v1` (for example `/users/v1`) are collapsed onto the same series as their non-versioned counterpart `/users`. Which PromQL expression performs that rewrite at query time?",
              "back": "`label_replace(vector, dst_label, replacement, src_label, regex)` rewrites a label value using a capture group from `regex` applied to `src_label`. Here the regex `(.*)/v1$` captures the prefix and `$1` reassigns it back to `path`, dropping the `/v1` suffix and letting subsequent aggregation collapse the two series. `label_join` concatenates several source labels with a separator — it cannot remove a suffix. The filter option simply removes the versioned series instead of folding them. The fourth option mixes `sum by` with a `without` clause that is not valid PromQL syntax — `without` takes labels, not equality expressions."
            },
            {
              "front": "An operator wants the per-cluster sum of `kube_pod_status_ready{condition=\"true\"}` across all namespaces, preserving the `cluster` label and discarding `namespace`, `pod`, and `condition`. Which PromQL expression is idiomatic?",
              "back": "`sum by (cluster) (...)` keeps the listed labels and aggregates across everything else — exactly the requirement. The label filter `{condition=\"true\"}` is applied at selection time so only ready pods are summed. `sum without (cluster)` does the opposite, discarding `cluster` and keeping every other label. The `count(...) by (cluster, namespace)` option counts series rather than summing values and still retains `namespace`. The `avg by (pod, cluster) ... / count(...)` option preserves `pod`, computes an average, and divides by an unrelated count — wrong on every level."
            }
          ],
          "source": "auto-derived"
        },
        {
          "id": "c1-quiz1",
          "type": "quiz",
          "title": "Promql — Drill 1",
          "questionIds": [
            "prometheus-pca-012",
            "prometheus-pca-001",
            "prometheus-pca-013",
            "prometheus-pca-003",
            "prometheus-pca-009"
          ]
        },
        {
          "id": "c1-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "A Grafana panel that graphs request throughput over a 6-hour window queries the counter `http_requests_total` with `$__rate_interval` set to 1 minute. Which PromQL function is conventionally chosen and why?",
                "choices": [
                  "`rate(http_requests_total[1m])` — averages the per-second growth of the counter across the whole range, smoothing transient spikes while still accounting for counter resets",
                  "`irate(http_requests_total[1m])` — uses only the two newest samples in the window, which is documented as the recommended choice for long-range smoothed graphs",
                  "`increase(http_requests_total[1m])` — returns a per-second rate by design and is the canonical Prometheus dashboarding choice for long ranges",
                  "`delta(http_requests_total[1m])` — designed for counters, ignores resets, and returns the same per-second value as rate() for monotonically increasing series"
                ],
                "correctIndex": 0,
                "explanation": "`rate()` is the canonical choice for graphing counters because it averages the per-second growth across the entire range vector and accounts for counter resets, producing a smooth long-range curve. `irate()` looks at only the last two samples and is meant for high-resolution alerting on short windows — on a 6-hour panel it produces a spiky graph. `increase()` returns the total number of events in the range (not a per-second value); to graph throughput you would still wrap it as `increase(...)[1m]`-equivalent which is exactly what `rate()` does. `delta()` is intended for gauges and does NOT compensate for counter resets, so applying it to a counter gives wrong results when a process restarts."
              },
              {
                "q": "Service `api` exposes a histogram named `http_request_duration_seconds_bucket` with `le` labels in seconds (alongside `_sum` and `_count`). Which PromQL expression returns an estimate of the 95th-percentile request latency over the last 5 minutes, grouped by `job`?",
                "choices": [
                  "`quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) by (job)`",
                  "`topk(1, rate(http_request_duration_seconds_sum[5m]) / rate(http_request_duration_seconds_count[5m])) by (job)`",
                  "`histogram_quantile(0.95, sum by (job, le) (rate(http_request_duration_seconds_bucket[5m])))`",
                  "`histogram_quantile(0.95, http_request_duration_seconds_bucket[5m]) / on(job) http_request_duration_seconds_count`"
                ],
                "correctIndex": 2,
                "explanation": "Computing a quantile from a Prometheus histogram requires `histogram_quantile()` applied to the per-second rate of the bucket counters, summed across whatever dimensions you want to collapse while preserving `le`. The canonical form is `histogram_quantile(0.95, sum by (job, le) (rate(bucket[5m])))`. `quantile()` is an aggregation operator that takes scalar values, not histogram buckets, and would return a meaningless number across bucket samples. The `_sum / _count` ratio gives the AVERAGE latency, not the 95th percentile. The last option is syntactically wrong: `histogram_quantile` does not take a range vector as its second argument, and dividing a quantile by `_count` has no semantic meaning."
              },
              {
                "q": "An operator wants to rewrite the `path` label so values ending in `/v1` (for example `/users/v1`) are collapsed onto the same series as their non-versioned counterpart `/users`. Which PromQL expression performs that rewrite at query time?",
                "choices": [
                  "`label_replace(rate(http_requests_total[5m]), \"path\", \"$1\", \"path\", \"(.*)/v1$\")`",
                  "`label_join(rate(http_requests_total[5m]), \"path\", \"/\", \"path\", \"v1\")`",
                  "`rate(http_requests_total{path!~\".*/v1\"}[5m])` — filters the versioned variant out so only the bare path remains",
                  "`sum by (path) (rate(http_requests_total[5m])) without (path = \"v1\")`"
                ],
                "correctIndex": 0,
                "explanation": "`label_replace(vector, dst_label, replacement, src_label, regex)` rewrites a label value using a capture group from `regex` applied to `src_label`. Here the regex `(.*)/v1$` captures the prefix and `$1` reassigns it back to `path`, dropping the `/v1` suffix and letting subsequent aggregation collapse the two series. `label_join` concatenates several source labels with a separator — it cannot remove a suffix. The filter option simply removes the versioned series instead of folding them. The fourth option mixes `sum by` with a `without` clause that is not valid PromQL syntax — `without` takes labels, not equality expressions."
              },
              {
                "q": "An operator wants the per-cluster sum of `kube_pod_status_ready{condition=\"true\"}` across all namespaces, preserving the `cluster` label and discarding `namespace`, `pod`, and `condition`. Which PromQL expression is idiomatic?",
                "choices": [
                  "`sum without (cluster) (kube_pod_status_ready)`",
                  "`count(kube_pod_status_ready) by (cluster, namespace)`",
                  "`avg by (pod, cluster) (kube_pod_status_ready) / count(kube_pod_status_ready)`",
                  "`sum by (cluster) (kube_pod_status_ready{condition=\"true\"})`"
                ],
                "correctIndex": 3,
                "explanation": "`sum by (cluster) (...)` keeps the listed labels and aggregates across everything else — exactly the requirement. The label filter `{condition=\"true\"}` is applied at selection time so only ready pods are summed. `sum without (cluster)` does the opposite, discarding `cluster` and keeping every other label. The `count(...) by (cluster, namespace)` option counts series rather than summing values and still retains `namespace`. The `avg by (pod, cluster) ... / count(...)` option preserves `pod`, computes an average, and divides by an unrelated count — wrong on every level."
              },
              {
                "q": "An engineer writes `rate(http_requests_total[5m])` for a counter but sees the value briefly go negative after a service restart. What does Prometheus actually do, and what is the likely explanation?",
                "choices": [
                  "`rate()` automatically handles counter resets, so a restart is accounted for; a transient negative usually reflects out-of-order or misaligned samples rather than normal reset handling.",
                  "`rate()` cannot be negative; the metric must be a gauge.",
                  "You must manually subtract the previous value to handle resets.",
                  "Counters never reset, so the data is corrupt."
                ],
                "correctIndex": 0,
                "explanation": "`rate()` (and `increase()`) are designed to detect counter resets — when a counter drops to zero on restart, the function treats it as a reset rather than a huge negative delta, so normal restarts do not produce negative rates. A brief negative typically points to out-of-order/misaligned samples or scrape timing artifacts. You do not manually handle resets, counters do reset on restart, and the metric is genuinely a counter (not a gauge)."
              },
              {
                "q": "A service exposes latency as a classic histogram metric `http_request_duration_seconds`, and you must chart the 95th-percentile request latency over the last five minutes. Which PromQL expression is correct?",
                "choices": [
                  "`quantile(0.95, http_request_duration_seconds)`",
                  "`avg(http_request_duration_seconds) * 0.95`",
                  "`histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))`",
                  "`topk(0.95, http_request_duration_seconds_count)`"
                ],
                "correctIndex": 2,
                "explanation": "For a classic histogram you compute quantiles from the `_bucket` series: take `rate()` of the buckets, aggregate by the `le` label, and pass to `histogram_quantile(0.95, ...)`. The `quantile()` aggregation operates over instant-vector samples across series, not histogram buckets; multiplying an average by 0.95 is meaningless; and `topk` selects top series, not a percentile."
              },
              {
                "q": "A query `sum(rate(node_cpu_seconds_total{mode=\"idle\"}[5m])) by (instance)` returns idle CPU seconds, but the team wants percent CPU busy per instance. Which expression is correct?",
                "choices": [
                  "`100 * (1 - avg by (instance)(rate(node_cpu_seconds_total{mode=\"idle\"}[5m])))`",
                  "`100 - node_cpu_seconds_total{mode=\"idle\"}`",
                  "`sum(node_cpu_seconds_total) * 100`",
                  "`rate(node_cpu_seconds_total[5m]) / 100`"
                ],
                "correctIndex": 0,
                "explanation": "Averaging the idle-mode rate per instance across CPUs gives the fraction of time CPUs were idle; `1 - that` is the busy fraction, and multiplying by 100 yields percent busy — hence `100 * (1 - avg by (instance)(rate(...idle...[5m])))`. Subtracting a raw counter from 100 mixes units, summing the raw counter is not a rate or a percentage, and dividing a rate by 100 is meaningless here."
              }
            ]
          },
          "title": "Promql — Lightning Round"
        },
        {
          "id": "c1-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-1"
        }
      ]
    },
    {
      "id": "ch2",
      "title": "Mixed Topics",
      "tag": "mixed-topics",
      "nodes": [
        {
          "id": "c2-concept",
          "type": "concept",
          "title": "Mixed exam topics: Key Ideas",
          "content": "Some exam objectives don't fit neatly into a single domain — they span protocols, tools, or concepts covered briefly in multiple sections. These cards treat each item as a stand-alone fact, the way an exam question presents it: one concept, one answer.",
          "flashcards": [
            {
              "front": "What is the difference between a hub, a switch, and a router?",
              "back": "Hub: Layer 1, repeats every frame to all ports — all devices share bandwidth, collisions guaranteed. Switch: Layer 2, forwards frames using a MAC address table to only the destination port — full duplex, no collisions, each port gets full bandwidth. Router: Layer 3, makes forwarding decisions based on IP address, connects different networks. Hub is legacy; switches dominate LAN; routers are at network edges."
            },
            {
              "front": "What is RAID 5 and how many drives can it tolerate losing?",
              "back": "RAID 5 stripes data and parity across at least 3 drives. Any single drive failure can be recovered by reconstructing missing data from the remaining drives and parity. It cannot tolerate 2 simultaneous drive failures. Effective capacity = (N-1) × drive size. Read performance is excellent; write requires parity recalculation. RAID 6 tolerates 2 failures using double parity."
            },
            {
              "front": "What does a proxy server do, and how does a forward proxy differ from a reverse proxy?",
              "back": "Forward proxy: sits between clients and the internet on behalf of clients — used for content filtering, caching, and anonymizing outbound requests (the server sees the proxy's IP). Reverse proxy: sits in front of servers on behalf of the server — used for load balancing, SSL termination, caching, and hiding backend topology (the client sees the proxy's IP). Both intermediate HTTP, but they protect opposite sides."
            },
            {
              "front": "What is the purpose of a DMZ in network architecture?",
              "back": "A demilitarized zone is a network segment between the untrusted internet and the trusted internal network, typically protected by two firewalls (or two interfaces on one firewall). Public-facing services (web, email, DNS) sit in the DMZ: internet can reach them but not the internal network; if the DMZ host is compromised, the inner firewall still blocks access to the internal LAN."
            }
          ],
          "source": "concept-library",
          "sourceTag": "mixed-topics"
        },
        {
          "id": "c2-quiz1",
          "type": "quiz",
          "title": "Mixed Topics — Drill 1",
          "questionIds": [
            "prometheus-pca-002",
            "prometheus-pca-008",
            "prometheus-pca-010",
            "prometheus-pca-014",
            "prometheus-pca-005"
          ]
        },
        {
          "id": "c2-quiz2",
          "type": "quiz",
          "title": "Mixed Topics — Drill 2",
          "questionIds": [
            "prometheus-pca-020",
            "prometheus-pca-006"
          ]
        },
        {
          "id": "c2-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "A team's short-lived batch CronJobs finish before Prometheus has a chance to scrape them, so their metrics never appear in queries. What is the documented Prometheus pattern for capturing metrics from these batch jobs?",
                "choices": [
                  "Add a `prometheus.io/scrape: true` annotation to each CronJob pod so the scrape configuration automatically extends its interval until the pod has produced samples",
                  "Have each batch job push its final metric values to the Prometheus Pushgateway, then have Prometheus scrape the Pushgateway like a normal target",
                  "Have each batch job send metrics over remote_write to a dedicated Prometheus instance running in agent mode, which buffers samples until the central server pulls them",
                  "Modify the batch jobs to keep their `/metrics` HTTP endpoint alive with a long sleep after work completes so the regular scraper reaches them before they exit"
                ],
                "correctIndex": 1,
                "explanation": "The Pushgateway exists precisely for service-level batch jobs whose lifetime is shorter than the scrape interval — the job pushes its final values to the Pushgateway and Prometheus scrapes the Pushgateway as if it were any other target. Annotations do not change scrape interval behaviour. remote_write goes from Prometheus to a long-term storage receiver, not from a job to Prometheus, and 'agent mode' is the receiver-side concept. Holding `/metrics` open with a sleep is the anti-pattern the Pushgateway was designed to avoid; it also pollutes job exit codes and Kubernetes lifecycle expectations."
              },
              {
                "q": "A batch job runs to completion in seconds and Prometheus never scrapes it in time. Which component is intended for this case, and what is the key caution?",
                "choices": [
                  "Scrape the job directly with a very short scrape_interval.",
                  "Push metrics to the Pushgateway, which holds them for Prometheus to scrape — but it is only for service-level batch jobs, not a general push proxy, and stale pushed metrics persist until deleted.",
                  "Use the Alertmanager to receive the metrics.",
                  "Store the metrics in the job's local file for later import."
                ],
                "correctIndex": 1,
                "explanation": "The Pushgateway exists so ephemeral/batch jobs can push their metrics to a location Prometheus then scrapes. The documented cautions: it is meant for service-level batch jobs (not as a general-purpose push gateway or to make Prometheus push-based), and because it retains the last pushed value, stale metrics linger until explicitly deleted. Shortening scrape_interval cannot reliably catch a seconds-long job, Alertmanager handles alerts not metric ingestion, and a local file is not scrapeable by Prometheus."
              },
              {
                "q": "Using Kubernetes service discovery, an operator wants to scrape ONLY the pods that carry the annotation `prometheus.io/scrape=\"true\"` and rewrite the target `__address__` to combine the pod IP with a port advertised through another annotation. Which relabel-config combination matches this intent?",
                "choices": [
                  "A `drop` action on `__meta_kubernetes_pod_label_scrape` matching `true`, followed by a `labelmap` of `__meta_kubernetes_pod_(.+)` to `pod_$1`",
                  "A `keep` action on the source label `__address__` matching the pod IP, followed by `hashmod` over the scrape interval to load-balance pulls between Prometheus replicas",
                  "A `replace` that overwrites the `job` label with the value of the pod annotation, followed by a `labeldrop` of `__meta_kubernetes_.*` to keep storage compact",
                  "A `keep` action on `__meta_kubernetes_pod_annotation_prometheus_io_scrape` matching `true`, then a `replace` that builds `__address__` from `__meta_kubernetes_pod_ip` and `__meta_kubernetes_pod_annotation_prometheus_io_port`"
                ],
                "correctIndex": 3,
                "explanation": "The documented pattern for opt-in pod scraping is exactly this pair: a `keep` step matching the annotation source label so only annotated pods survive target discovery, and a `replace` step that constructs `__address__` from the pod IP and the port annotation. The `drop`/`labelmap` option does the opposite of `keep` (it removes matching targets) and never rewrites the address. The `__address__` `keep`/`hashmod` option misuses `hashmod` (which is for sharding across replicas, not for filtering by IP) and never reads the annotation. The `replace`+`labeldrop` option rewrites `job` but never enforces the opt-in filter, so every pod still gets scraped."
              },
              {
                "q": "In a Prometheus scrape config using Kubernetes service discovery, you want to keep only pods with the annotation `prometheus.io/scrape: \"true\"`. Which relabeling action accomplishes this?",
                "choices": [
                  "A `labelmap` action on the annotation.",
                  "A `replace` action that renames the target.",
                  "A `hashmod` action to shard the targets.",
                  "A `keep` action in relabel_configs that matches the annotation meta-label against `true`."
                ],
                "correctIndex": 3,
                "explanation": "During target relabeling, a `keep` action retains only targets whose source labels (here the `__meta_kubernetes_pod_annotation_prometheus_io_scrape` meta-label) match the regex `true`, dropping all others. `labelmap` copies label names by pattern, `hashmod` is for sharding targets across Prometheus instances, and `replace` rewrites a label value — none of which filter targets by annotation."
              },
              {
                "q": "An SRE team sizing disk for a Prometheus 2.x server needs to understand how the local time-series database stores samples and enforces retention before setting flags. Which statement is correct about Prometheus 2.x local TSDB storage and retention?",
                "choices": [
                  "The default retention is 15 days; older blocks are removed by the retention enforcer based on `--storage.tsdb.retention.time`, with `--storage.tsdb.retention.size` being opt-in",
                  "By default, samples are retained for 30 days; `--storage.tsdb.retention.time` can only shorten that window, never extend it",
                  "The TSDB writes new samples directly to immutable on-disk blocks; the WAL is replayed only after a clean shutdown to merge cached data into the latest block",
                  "Local storage is the intended primary long-term store for Prometheus; Thanos and Cortex exist solely to add multi-tenancy, not to extend retention"
                ],
                "correctIndex": 0,
                "explanation": "The Prometheus default retention is 15 days, configured via `--storage.tsdb.retention.time`; size-based retention via `--storage.tsdb.retention.size` is opt-in and the two combine with whichever limit triggers first. The 30-day claim is incorrect. The TSDB does NOT write samples directly to immutable blocks — samples land in the in-memory head block backed by the WAL on disk, and the WAL is replayed on EVERY startup (clean or unclean) to recover the head. Local storage is explicitly documented as short-to-medium term; long-term retention is the use case Thanos / Cortex / Mimir address via remote_write and object storage."
              },
              {
                "q": "An operator wants Prometheus to keep 30 days of local data but is concerned about disk usage. Which statement about the local TSDB is accurate?",
                "choices": [
                  "Retention can only be set by number of samples, not by time.",
                  "The TSDB writes each sample as an individual file on disk.",
                  "Prometheus stores data indefinitely and cannot delete it.",
                  "Retention is controlled by `--storage.tsdb.retention.time` (and/or a size limit), and old blocks are compacted and eventually deleted past the window."
                ],
                "correctIndex": 3,
                "explanation": "Local retention is governed by `--storage.tsdb.retention.time` (with an optional `retention.size` cap); the TSDB compacts data into blocks and deletes blocks older than the retention window to bound disk usage. Retention is time- (or size-) based, not only sample-count; Prometheus does delete old data; and it does not write one file per sample — samples are batched into compressed blocks."
              },
              {
                "q": "An HTTP server library wants the server itself to compute pre-aggregated request-latency percentiles per handler and expose them so Prometheus can scrape them directly, without anyone running `histogram_quantile()` at query time. Which Prometheus metric type matches that requirement?",
                "choices": [
                  "Counter — counters monotonically increase, and a counter-based client library derives percentiles from the cumulative count at scrape time",
                  "Summary — a summary exposes precomputed quantile time series such as `{quantile=\"0.5\"}` and `{quantile=\"0.99\"}` calculated inside the client",
                  "Gauge — gauges can increase and decrease, and the client library exposes precomputed quantile labels alongside each gauge sample",
                  "Histogram — buckets capture the full distribution, and the client library serialises pre-aggregated percentile values as `_quantile` time series"
                ],
                "correctIndex": 1,
                "explanation": "Summary is the metric type that exposes quantiles computed CLIENT-SIDE: each scrape returns time series like `http_request_latency{quantile=\"0.99\"}` already containing the percentile value. Histograms work the opposite way — they expose cumulative bucket counts under `_bucket{le=\"...\"}` and percentiles must be derived at query time via `histogram_quantile()`. Counters only express monotonic totals and cannot represent percentiles directly. Gauges can move up or down but the Prometheus client libraries do not expose a built-in quantile workflow on gauges; the `quantile` label is reserved for summaries."
              },
              {
                "q": "A developer instruments a service and must choose between a Histogram and a Summary for request latency, needing accurate percentiles aggregated across many instances. Which choice and reason is correct?",
                "choices": [
                  "Summary, because its client-side quantiles can be averaged across instances.",
                  "Either one; they are functionally identical.",
                  "Histogram, because its buckets can be aggregated server-side across instances and percentiles computed with histogram_quantile.",
                  "Gauge, because latency is a point-in-time value."
                ],
                "correctIndex": 2,
                "explanation": "Histograms expose bucket counts that are additive, so you can aggregate them across instances (sum by le) and compute percentiles server-side with `histogram_quantile`. Summary quantiles are computed client-side per instance and cannot be meaningfully averaged/aggregated across instances. Histograms and Summaries are not identical, and a gauge does not capture a latency distribution."
              }
            ]
          },
          "title": "Mixed Topics — Lightning Round"
        },
        {
          "id": "c2-boss",
          "type": "subboss",
          "title": "Boss: Mixed Topics",
          "questionIds": [
            "prometheus-pca-019",
            "prometheus-pca-007",
            "prometheus-pca-018",
            "prometheus-pca-017",
            "prometheus-pca-011",
            "prometheus-pca-022",
            "prometheus-pca-004",
            "prometheus-pca-016"
          ]
        },
        {
          "id": "c2-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-2"
        }
      ]
    }
  ],
  "finalBoss": {
    "id": "final-boss",
    "type": "finalboss",
    "title": "Final Exam: Prometheus Certified Associate (PCA)",
    "questionCount": 15,
    "timeMinutes": 23
  },
  "meta": {
    "generatedAt": "2026-08-14T17:40:03.148Z",
    "totalNodes": 11,
    "totalQuestions": 22,
    "nodeTypes": [
      "concept",
      "quiz",
      "minigame",
      "subboss",
      "finalboss"
    ]
  }
}
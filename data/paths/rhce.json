{
  "packId": "rhce",
  "title": "Red Hat Certified Engineer (RHCE EX294)",
  "brandName": "Red Hat",
  "brandColor": "#EE0000",
  "chapters": [
    {
      "id": "ch1",
      "title": "Part 1",
      "tag": "part-1",
      "nodes": [
        {
          "id": "c1-concept",
          "type": "concept",
          "title": "Part 1: Key Ideas",
          "content": "Quick primer on Part 1. Tap to flip cards covering the most-asked exam ideas in this domain.",
          "flashcards": [
            {
              "front": "An engineer creates the file `/etc/ansible/hosts` on the control node, but `ansible-inventory --list` for the project still returns an empty inventory when run from `~/ex294/`. The project's `ansible.cfg` in that directory contains a `[defaults]` block with `inventory = ./inventory`. Why does Ansible ignore `/etc/ansible/hosts`?",
              "back": "Ansible's configuration precedence is: ANSIBLE_CONFIG env var → ./ansible.cfg in CWD → ~/.ansible.cfg → /etc/ansible/ansible.cfg. The first match wins for the whole file (no merging). Because the project's `ansible.cfg` sets `inventory = ./inventory`, the system default `/etc/ansible/hosts` is never consulted. INI is still supported (option A's claim about removal is false), and root vs user has no bearing on which config is read."
            },
            {
              "front": "A playbook must run on hosts in the `webservers` group, connecting as user `devops` and escalating to root via sudo without a password prompt. Which combination of play-level keywords is correct?",
              "back": "`remote_user` controls who SSHs into the target; `become: true` switches identity after the connection; `become_method: sudo` (the default in most configs) chooses the escalation tool; the default `become_user` is `root`. Passwordless sudo for `devops` must be configured on the target (a sudoers drop-in). `connection:` selects a connection plugin like `ssh`/`local`/`winrm`, not an escalation method, and `privilege` is not a real keyword."
            },
            {
              "front": "A task installs the `httpd` package and is followed by a handler that restarts `httpd`. The engineer wants the handler to run only when the package was actually installed or upgraded — not on every play run. Which task definition achieves this idiomatically?",
              "back": "Handlers in Ansible are notified by tasks and run once at the end of the play, but only if the notifying task reported `changed: true`. The `dnf` module returns `changed` when it installs/upgrades a package and `ok` (unchanged) when the package is already at the requested state. So `state: present` + `notify:` gives \"restart only when the install actually changed something\" for free — no `when:` plumbing needed. Forcing `changed_when: true` defeats idempotence, and pre_tasks run before normal tasks regardless of changes."
            },
            {
              "front": "An engineer needs to encrypt the file `group_vars/db/vault.yml` so it can be committed to git, while leaving the other group_vars files in plaintext. Which command produces a single-file vault?",
              "back": "`ansible-vault encrypt <file>` encrypts an existing plaintext file in place using AES-256 with a password (or vault-id). `ansible-vault create` is for making a brand-new encrypted file — running it against an existing file would overwrite it. `decrypt`, `edit`, `view`, and `rekey` cover the rest of the lifecycle. Ansible Vault is a self-contained format and does not call GPG."
            }
          ],
          "source": "auto-derived"
        },
        {
          "id": "c1-quiz1",
          "type": "quiz",
          "title": "Part 1 — Drill 1",
          "questionIds": [
            "rhce-001",
            "rhce-002",
            "rhce-003",
            "rhce-004"
          ]
        },
        {
          "id": "c1-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "An engineer creates the file `/etc/ansible/hosts` on the control node, but `ansible-inventory --list` for the project still returns an empty inventory when run from `~/ex294/`. The project's `ansible.cfg` in that directory contains a `[defaults]` block with `inventory = ./inventory`. Why does Ansible ignore `/etc/ansible/hosts`?",
                "choices": [
                  "Ansible only reads `/etc/ansible/hosts` when invoked as root; the engineer must use `sudo ansible-inventory`",
                  "The project-level `ansible.cfg` overrides the system default — Ansible reads `./inventory`, not `/etc/ansible/hosts`",
                  "`/etc/ansible/hosts` must be in YAML for Ansible 2.9+; the INI format has been removed",
                  "Inventories under `/etc/` are only honored when `ANSIBLE_CONFIG` is unset, which is never the case after package install"
                ],
                "correctIndex": 1,
                "explanation": "Ansible's configuration precedence is: ANSIBLE_CONFIG env var → ./ansible.cfg in CWD → ~/.ansible.cfg → /etc/ansible/ansible.cfg. The first match wins for the whole file (no merging). Because the project's `ansible.cfg` sets `inventory = ./inventory`, the system default `/etc/ansible/hosts` is never consulted. INI is still supported (option A's claim about removal is false), and root vs user has no bearing on which config is read."
              },
              {
                "q": "A playbook must run on hosts in the `webservers` group, connecting as user `devops` and escalating to root via sudo without a password prompt. Which combination of play-level keywords is correct?",
                "choices": [
                  "`hosts: webservers`, `remote_user: devops`, `become: true`, `become_method: sudo`",
                  "`hosts: webservers`, `user: root`, `become: false` — sudo is implicit when targeting root",
                  "`hosts: webservers`, `connection: sudo`, `become_user: devops`",
                  "`hosts: webservers`, `remote_user: devops`, `privilege: escalate` — `privilege` is the modern replacement for `become`"
                ],
                "correctIndex": 0,
                "explanation": "`remote_user` controls who SSHs into the target; `become: true` switches identity after the connection; `become_method: sudo` (the default in most configs) chooses the escalation tool; the default `become_user` is `root`. Passwordless sudo for `devops` must be configured on the target (a sudoers drop-in). `connection:` selects a connection plugin like `ssh`/`local`/`winrm`, not an escalation method, and `privilege` is not a real keyword."
              },
              {
                "q": "A task installs the `httpd` package and is followed by a handler that restarts `httpd`. The engineer wants the handler to run only when the package was actually installed or upgraded — not on every play run. Which task definition achieves this idiomatically?",
                "choices": [
                  "Use `ansible.builtin.dnf` with `state: latest` and `register: pkg_result`, then call the handler from a `when: pkg_result.rc == 0` block",
                  "Add `changed_when: true` to force the handler to fire each run, then let the handler decide whether to restart",
                  "Use `ansible.builtin.dnf` with `state: present` plus `notify: Restart httpd` — handlers fire only when the task reports `changed`",
                  "Place the handler in `pre_tasks` so it always runs before `httpd` is touched"
                ],
                "correctIndex": 2,
                "explanation": "Handlers in Ansible are notified by tasks and run once at the end of the play, but only if the notifying task reported `changed: true`. The `dnf` module returns `changed` when it installs/upgrades a package and `ok` (unchanged) when the package is already at the requested state. So `state: present` + `notify:` gives \"restart only when the install actually changed something\" for free — no `when:` plumbing needed. Forcing `changed_when: true` defeats idempotence, and pre_tasks run before normal tasks regardless of changes."
              },
              {
                "q": "An engineer needs to encrypt the file `group_vars/db/vault.yml` so it can be committed to git, while leaving the other group_vars files in plaintext. Which command produces a single-file vault?",
                "choices": [
                  "`ansible-vault encrypt group_vars/db/vault.yml`",
                  "`ansible-playbook --encrypt group_vars/db/vault.yml`",
                  "`gpg --symmetric group_vars/db/vault.yml` — Ansible Vault delegates to GPG under the hood",
                  "`ansible-vault create group_vars/db/vault.yml` — `create` re-encrypts existing files in place"
                ],
                "correctIndex": 0,
                "explanation": "`ansible-vault encrypt <file>` encrypts an existing plaintext file in place using AES-256 with a password (or vault-id). `ansible-vault create` is for making a brand-new encrypted file — running it against an existing file would overwrite it. `decrypt`, `edit`, `view`, and `rekey` cover the rest of the lifecycle. Ansible Vault is a self-contained format and does not call GPG."
              }
            ]
          },
          "title": "Part 1 — Lightning Round"
        },
        {
          "id": "c1-boss",
          "type": "subboss",
          "title": "Boss: Part 1",
          "questionIds": [
            "rhce-001",
            "rhce-002",
            "rhce-003",
            "rhce-004"
          ]
        },
        {
          "id": "c1-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-1"
        }
      ]
    },
    {
      "id": "ch2",
      "title": "Part 2",
      "tag": "part-2",
      "nodes": [
        {
          "id": "c2-concept",
          "type": "concept",
          "title": "Terraform variables and locals: Key Ideas",
          "content": "Terraform input variables make configurations reusable; locals simplify repeated expressions. The TF-003 exam heavily tests type constraints, validation blocks, sensitive flags, and the precedence order for variable assignment.",
          "flashcards": [
            {
              "front": "Five ways to assign a Terraform variable (highest to lowest precedence)?",
              "back": "-var flag > -var-file flag > *.auto.tfvars (alphabetical) > terraform.tfvars > TF_VAR_name env var. Higher-precedence sources override lower ones."
            },
            {
              "front": "What does sensitive = true do on a variable?",
              "back": "Redacts the value from CLI output and plan display. It does NOT encrypt the value in state — state still stores it in plaintext, so the backend must be secured separately."
            },
            {
              "front": "When to use locals vs variable?",
              "back": "Use locals for computed or repeated expressions internal to a module that callers should not override (e.g. a concatenated name). Use variables for values callers should supply. Locals cannot be set from outside the module."
            },
            {
              "front": "Type constraint any vs object({ ... }) — trade-offs?",
              "back": "object({ key = type }) enforces a specific schema with required keys and types — prefer this for safety. any disables type checking entirely and should only be used for dynamic wrapper modules where the type is intentionally unknown."
            }
          ],
          "source": "concept-library",
          "sourceTag": "variables"
        },
        {
          "id": "c2-quiz1",
          "type": "quiz",
          "title": "Part 2 — Drill 1",
          "questionIds": [
            "rhce-006",
            "rhce-005",
            "rhce-007",
            "rhce-008"
          ]
        },
        {
          "id": "c2-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "A play needs to create the same user account on every host in a group, but the user's primary group differs per host. The engineer sets `group_vars/webservers.yml` with `primary_group: web` and `host_vars/db01.yml` with `primary_group: dba`. On `db01`, which value of `primary_group` does Ansible use?",
                "choices": [
                  "`web`, because group_vars are applied after host_vars",
                  "Ansible raises a duplicate-variable error and refuses to run",
                  "Whichever file was modified most recently on disk",
                  "`dba`, because host_vars override group_vars at the same precedence tier"
                ],
                "correctIndex": 3,
                "explanation": "Ansible's documented variable precedence (low → high) places `group_vars/all` below `group_vars/<group>` below `host_vars/<host>`. `db01.yml` is host-scoped, so its `primary_group: dba` wins on that host; on other webservers (which have no host_vars) the group value `web` applies. The order is deterministic — Ansible never errors on \"duplicate\" variables across precedence tiers, and file mtime is irrelevant."
              },
              {
                "q": "A task must create the directories `/srv/app/{cache,logs,uploads}` with mode `0755` on every host. Which task is the most idiomatic Ansible way to do this?",
                "choices": [
                  "Three separate `ansible.builtin.file` tasks, one per directory — loops are discouraged for filesystem operations",
                  "A single `ansible.builtin.command` task running `mkdir -p /srv/app/{cache,logs,uploads} && chmod 0755 ...`",
                  "A single `ansible.builtin.file` task with `state: directory`, `mode: '0755'`, and `loop: [cache, logs, uploads]` using `path: \"/srv/app/{{ item }}\"`",
                  "An `ansible.builtin.shell` task with brace expansion — only `shell` (not `command`) expands `{a,b,c}`"
                ],
                "correctIndex": 2,
                "explanation": "The `file` module with `state: directory` is the idempotent, declarative choice; iterating with `loop:` keeps the play DRY and lets Ansible report changed/ok per item. Falling back to `command`/`shell` for filesystem state defeats idempotence checks and the module's permission/owner handling. Three duplicated tasks technically work but waste lines and obscure intent — there's no \"loops discouraged\" rule."
              },
              {
                "q": "An engineer writes a Jinja2 template `httpd.conf.j2` that should render the server's primary IPv4 address. Which expression is correct?",
                "choices": [
                  "`{{ ansible_default_ipv4.address }}` — pulled from gathered facts",
                  "`{{ ansible_facts.ip }}` — top-level shortcut added in Ansible 2.10",
                  "`{% ip = lookup('hostname', '-I') %}` — `lookup` runs on the target and returns the first interface",
                  "`{{ inventory_hostname }}` — Ansible resolves the inventory name to an IP at template time"
                ],
                "correctIndex": 0,
                "explanation": "When fact gathering is enabled (the default), Ansible exposes networking facts under `ansible_facts['default_ipv4']` (and the legacy `ansible_default_ipv4` alias). The `.address` key holds the IPv4 address of the interface used to reach the default gateway — exactly what a server config usually wants. `ansible_facts.ip` doesn't exist, `lookup` plugins run on the *controller* (not the target), and `inventory_hostname` is the inventory string verbatim, never resolved to an IP."
              },
              {
                "q": "A role called `webapp` lives in `~/ex294/roles/webapp/`. The engineer wants to call it from `site.yml` so its `tasks/main.yml`, `handlers/main.yml`, and `defaults/main.yml` all load in the standard order. Which play body is correct?",
                "choices": [
                  "Add a top-level `tasks:` block with `- import_tasks: roles/webapp/tasks/main.yml` — roles are just a directory convention",
                  "Use the play-level `roles:` keyword (alongside `hosts:`) with `- webapp` as the only entry",
                  "Add `- role: webapp` inside `pre_tasks:` — `role:` is only valid in `pre_tasks`",
                  "List `roles/webapp` under `vars_files:` — Ansible auto-discovers tasks and handlers from `vars_files` entries"
                ],
                "correctIndex": 1,
                "explanation": "The canonical way to apply a role is the play-level `roles:` keyword, which Ansible resolves under `roles_path` (project-local `./roles/`, then `~/.ansible/roles`, then `/usr/share/ansible/roles`, etc.). Ansible automatically wires `tasks/`, `handlers/`, `defaults/`, `vars/`, `meta/`, and `templates/` from the role's standard skeleton. `import_role:`/`include_role:` are valid alternatives inside `tasks:`, but pointing `import_tasks:` at a single tasks file skips all the other directories. `vars_files:` only loads variable files."
              }
            ]
          },
          "title": "Part 2 — Lightning Round"
        },
        {
          "id": "c2-boss",
          "type": "subboss",
          "title": "Boss: Part 2",
          "questionIds": [
            "rhce-006",
            "rhce-005",
            "rhce-007",
            "rhce-008"
          ]
        },
        {
          "id": "c2-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-2"
        }
      ]
    },
    {
      "id": "ch3",
      "title": "Part 3",
      "tag": "part-3",
      "nodes": [
        {
          "id": "c3-concept",
          "type": "concept",
          "title": "Networking fundamentals: Key Ideas",
          "content": "Networking moves packets between machines using layered protocols (the OSI / TCP-IP stack). Most exam questions test routing decisions, subnetting math, the difference between L2 and L3, and how DNS / DHCP make the whole thing usable for humans.",
          "flashcards": [
            {
              "front": "What's the difference between Layer 2 and Layer 3?",
              "back": "L2 (data link) uses MAC addresses to deliver frames inside a single broadcast domain (a switch). L3 (network) uses IP addresses to route packets between networks (a router). Switches do L2; routers do L3."
            },
            {
              "front": "How does a subnet mask work?",
              "back": "The mask splits an IP into a network portion (the high bits, set to 1) and a host portion (the low bits, set to 0). `/24` means the first 24 bits are network = up to 256 hosts (254 usable). `/30` is 4 IPs = 2 hosts — typical for a point-to-point link."
            },
            {
              "front": "What does DNS do?",
              "back": "Domain Name System translates human-readable names (`example.com`) into IP addresses. A recursive resolver asks the root, the TLD server (`.com`), and finally the authoritative server for the domain. TTL controls how long answers are cached."
            },
            {
              "front": "TCP vs UDP — when do you pick each?",
              "back": "TCP is reliable, ordered, congestion-controlled — use it when every byte matters (HTTP, SSH, SMTP). UDP is lightweight, no handshake, no retransmit — use it for streaming, gaming, DNS queries, anywhere latency beats reliability."
            }
          ],
          "source": "concept-library",
          "sourceTag": "networking"
        },
        {
          "id": "c3-quiz1",
          "type": "quiz",
          "title": "Part 3 — Drill 1",
          "questionIds": [
            "rhce-009",
            "rhce-010",
            "rhce-011",
            "rhce-012"
          ]
        },
        {
          "id": "c3-game",
          "type": "minigame",
          "gameType": "lightning",
          "mode": "lightning-round",
          "data": {
            "secondsPerQuestion": 7,
            "livesAllowed": 3,
            "questions": [
              {
                "q": "A task uses the `ansible.posix.firewalld` module to open TCP port 8080. The engineer wants the rule to apply immediately *and* survive a reboot. Which parameter combination is required?",
                "choices": [
                  "`state: enabled`, `port: 8080/tcp`, `immediate: true`, `permanent: true`",
                  "`state: enabled`, `port: 8080/tcp` — `firewalld` is permanent by default",
                  "`state: present`, `port: 8080/tcp`, `runtime: true` — `runtime` is the modern replacement for `immediate`",
                  "`state: enabled`, `port: 8080/tcp`, `permanent: true` — the runtime config reloads automatically on the next task"
                ],
                "correctIndex": 0,
                "explanation": "`firewalld` distinguishes between the *runtime* configuration (active until the daemon restarts) and the *permanent* configuration (written to `/etc/firewalld/`, applied on reload). The Ansible module mirrors this with `immediate: true` (apply now) and `permanent: true` (write to disk). You need both to satisfy \"apply now and survive a reboot.\" `state:` for ports is `enabled`/`disabled`, not `present`/`absent`, and there is no `runtime:` parameter."
              },
              {
                "q": "An engineer needs to attempt a risky migration step, run a cleanup task whether it succeeds or fails, and substitute a fallback configuration only if the migration raised an error. Which Ansible construct expresses this cleanly?",
                "choices": [
                  "Three sequential tasks with `ignore_errors: true` on the first and `when:` guards on the others, inspecting the registered result",
                  "A single task with `failed_when: false` and a `notify:` to a handler that decides what to do",
                  "A `block:` / `rescue:` / `always:` structure — main steps in `block`, fallback in `rescue`, cleanup in `always`",
                  "`pre_tasks:` for the migration, `post_tasks:` for cleanup, and `rescue_tasks:` for the fallback"
                ],
                "correctIndex": 2,
                "explanation": "Ansible's `block:` / `rescue:` / `always:` mirrors try/except/finally: tasks under `block:` run normally, `rescue:` runs only if a `block:` task failed, and `always:` runs after either path — making it the idiomatic way to express \"try X, fall back to Y on error, clean up either way.\" Hand-rolling the same logic with `ignore_errors` + `register` + `when:` works but is verbose and easy to get wrong. `failed_when: false` only suppresses failure on one task and doesn't branch. `rescue_tasks:` is not a real keyword."
              },
              {
                "q": "An engineer wants the `webapp` role's variables in `defaults/main.yml` to be overridable from `group_vars/webservers.yml`, but its variables in `vars/main.yml` to take precedence over everything except command-line `-e`. Which statement matches Ansible's documented precedence?",
                "choices": [
                  "`defaults/main.yml` sits near the bottom of the precedence chain (overridable by inventory and group_vars), while `vars/main.yml` sits near the top (only `-e` and a few task-level keywords beat it)",
                  "`defaults/main.yml` and `vars/main.yml` are equivalent — they're merged at role load time",
                  "`defaults/main.yml` always wins because Ansible loads it last",
                  "Both are overridden by host facts, regardless of role-internal scope"
                ],
                "correctIndex": 0,
                "explanation": "Role `defaults/main.yml` is at the very bottom of variable precedence — it exists precisely so callers (inventory, group_vars, host_vars, play vars) can override role defaults without editing the role. Role `vars/main.yml` is near the top — only `-e` (extra vars), task `vars:`, `include_vars`, `set_fact`, and a few task-level keywords beat it. This split is the recommended pattern: defaults are configurable knobs, `vars/` are role-internal constants. Host facts have their own (mid-range) tier and do not override either uniformly."
              },
              {
                "q": "An engineer runs `ansible-playbook site.yml --syntax-check` and gets `ok`, then runs `--check` and gets `ok` again, but the actual run fails because a templated path doesn't exist on disk. Why didn't `--check` catch it?",
                "choices": [
                  "`--check` only validates YAML, just like `--syntax-check`; the two are aliases",
                  "Check mode skips fact gathering, so templated paths that depend on facts are never evaluated",
                  "Check mode reports what *would* change without making changes — modules that don't support check mode are skipped, and operations like `file: state: directory` against a parent path that doesn't exist won't be created during the dry run, so dependent checks may falsely pass",
                  "`--check` requires `--diff` to actually exercise templates; without it, templates are not rendered"
                ],
                "correctIndex": 2,
                "explanation": "`--syntax-check` is a parse-only pass (YAML + Ansible keywords). `--check` is dry-run/check mode: Ansible reports the changes each module *would* make, without applying them. Crucially, modules that don't implement check mode are skipped, and any task whose success depends on side effects of a *previous* task (e.g., a directory created earlier in the play) will not see those side effects in check mode — so a later task that templates a file into that not-yet-created directory may pass the dry run but fail for real. Fact gathering still runs in check mode (B is wrong), and templates render normally without `--diff` (D is wrong)."
              }
            ]
          },
          "title": "Part 3 — Lightning Round"
        },
        {
          "id": "c3-boss",
          "type": "subboss",
          "title": "Boss: Part 3",
          "questionIds": [
            "rhce-009",
            "rhce-010",
            "rhce-011",
            "rhce-012"
          ]
        },
        {
          "id": "c3-chest",
          "type": "chest",
          "title": "Treasure Chest",
          "rewardXp": 30,
          "cosmeticKey": "chapter-3"
        }
      ]
    }
  ],
  "finalBoss": {
    "id": "final-boss",
    "type": "finalboss",
    "title": "Final Exam: Red Hat Certified Engineer (RHCE EX294)",
    "questionCount": 15,
    "timeMinutes": 23
  },
  "meta": {
    "generatedAt": "2026-07-03T14:50:26.502Z",
    "totalNodes": 16,
    "totalQuestions": 12,
    "nodeTypes": [
      "concept",
      "quiz",
      "minigame",
      "subboss",
      "finalboss"
    ]
  }
}
{
  "version": "1.0.0",
  "last_updated": "2026-05-16",
  "categories": [
    {
      "id": "linux",
      "name": "Linux",
      "icon": "🐧",
      "description": "Shell, files, permissions, processes, networking — the daily-driver fluency every cert exam expects."
    },
    {
      "id": "aws",
      "name": "AWS CLI",
      "icon": "☁️",
      "description": "Essential aws-cli commands for EC2, S3, IAM, and the services you'll touch on SAA / DVA / SOA."
    },
    {
      "id": "kubernetes",
      "name": "Kubernetes",
      "icon": "☸️",
      "description": "kubectl and YAML basics that come up on CKA, CKAD, CKS, and on the job."
    },
    {
      "id": "docker",
      "name": "Docker",
      "icon": "🐳",
      "description": "Containers, images, volumes, networks — the building blocks underneath Kubernetes."
    },
    {
      "id": "git",
      "name": "Git",
      "icon": "🌿",
      "description": "The version-control commands that come up in every DevOps and SRE interview."
    },
    {
      "id": "networking",
      "name": "Networking",
      "icon": "🌐",
      "description": "Ports, protocols, and the diagnostic tools (netstat, nmap, curl, dig) you'll lean on under pressure."
    }
  ],
  "entries": [
    {"id": "lin-ls", "cat": "linux", "cmd": "ls -lah", "desc": "List all files (including dotfiles) with human-readable sizes.", "example": "ls -lah /var/log"},
    {"id": "lin-cd", "cat": "linux", "cmd": "cd -", "desc": "Switch back to the previous working directory.", "example": null},
    {"id": "lin-pwd", "cat": "linux", "cmd": "pwd", "desc": "Print the absolute path of the current directory.", "example": null},
    {"id": "lin-find", "cat": "linux", "cmd": "find . -name '*.log' -mtime -7", "desc": "Find files matching a pattern modified in the last 7 days.", "example": "find /var/log -name '*.gz' -mtime +30 -delete"},
    {"id": "lin-grep", "cat": "linux", "cmd": "grep -RIn 'pattern' .", "desc": "Recursively grep for a pattern, line-numbered, skipping binary files.", "example": "grep -RIn 'TODO' src/"},
    {"id": "lin-chmod", "cat": "linux", "cmd": "chmod 644 file", "desc": "Set file permissions to rw-r--r-- (owner can write, group/others read).", "example": "chmod 755 script.sh   # executable"},
    {"id": "lin-chown", "cat": "linux", "cmd": "chown user:group file", "desc": "Change file owner and group.", "example": "chown -R www-data:www-data /var/www"},
    {"id": "lin-ps", "cat": "linux", "cmd": "ps aux | grep nginx", "desc": "List every process and grep for one. Use 'pgrep -fa' on modern systems.", "example": "pgrep -fa nginx"},
    {"id": "lin-top", "cat": "linux", "cmd": "top", "desc": "Live view of CPU/memory usage by process. Press P (CPU) or M (memory) to sort.", "example": null},
    {"id": "lin-htop", "cat": "linux", "cmd": "htop", "desc": "Friendlier top alternative with mouse support and tree view (F5).", "example": null},
    {"id": "lin-kill", "cat": "linux", "cmd": "kill -9 PID", "desc": "Force-terminate a process. Try kill PID first — SIGTERM lets the process clean up.", "example": "kill -TERM 1234"},
    {"id": "lin-df", "cat": "linux", "cmd": "df -h", "desc": "Show disk usage per mount point in human-readable units.", "example": "df -h /var"},
    {"id": "lin-du", "cat": "linux", "cmd": "du -sh *", "desc": "Show the size of each item in the current directory (sum subdirectories).", "example": "du -sh /var/* | sort -h"},
    {"id": "lin-ss", "cat": "linux", "cmd": "ss -tulnp", "desc": "List listening TCP/UDP ports with the process holding each one. Replaces netstat.", "example": null},
    {"id": "lin-systemctl", "cat": "linux", "cmd": "systemctl status nginx", "desc": "Show a systemd unit's state, recent logs, and process tree.", "example": "systemctl restart nginx && systemctl status nginx"},
    {"id": "lin-journalctl", "cat": "linux", "cmd": "journalctl -u nginx -f", "desc": "Tail the logs of a systemd unit in real time.", "example": "journalctl -u sshd --since='1 hour ago'"},
    {"id": "lin-tar", "cat": "linux", "cmd": "tar -czf backup.tgz dir/", "desc": "Create a gzip-compressed tarball of a directory.", "example": "tar -xzf backup.tgz -C /tmp"},
    {"id": "lin-rsync", "cat": "linux", "cmd": "rsync -avz src/ user@host:dest/", "desc": "Mirror a directory over SSH, preserving permissions, compressing in flight.", "example": null},
    {"id": "lin-awk", "cat": "linux", "cmd": "awk '{print $1}'", "desc": "Print the first field of each line. Brilliant for column-shaped data.", "example": "ps aux | awk '{print $11}' | sort -u"},
    {"id": "lin-sed", "cat": "linux", "cmd": "sed -i 's/foo/bar/g' file", "desc": "In-place replace 'foo' with 'bar' across a file.", "example": null},

    {"id": "aws-s3-ls", "cat": "aws", "cmd": "aws s3 ls s3://bucket/", "desc": "List objects in an S3 bucket. Add --recursive to descend.", "example": "aws s3 ls s3://my-bucket/logs/ --recursive --human-readable --summarize"},
    {"id": "aws-s3-cp", "cat": "aws", "cmd": "aws s3 cp file s3://bucket/key", "desc": "Upload a local file to S3. Add --acl public-read for static-site assets.", "example": "aws s3 cp dist/ s3://my-site/ --recursive"},
    {"id": "aws-s3-sync", "cat": "aws", "cmd": "aws s3 sync ./local s3://bucket", "desc": "Sync a local directory to a bucket, transferring only changed files.", "example": "aws s3 sync ./public s3://my-site --delete"},
    {"id": "aws-ec2-desc", "cat": "aws", "cmd": "aws ec2 describe-instances", "desc": "List EC2 instances in the current region. Use --filters to narrow.", "example": "aws ec2 describe-instances --filters 'Name=tag:Env,Values=prod'"},
    {"id": "aws-ec2-start", "cat": "aws", "cmd": "aws ec2 start-instances --instance-ids i-...", "desc": "Power on one or more stopped instances.", "example": null},
    {"id": "aws-iam-whoami", "cat": "aws", "cmd": "aws sts get-caller-identity", "desc": "Show the IAM identity (user, account, ARN) used by the current credentials.", "example": null},
    {"id": "aws-iam-policy", "cat": "aws", "cmd": "aws iam list-attached-user-policies --user-name alice", "desc": "List the managed IAM policies attached to a given user.", "example": null},
    {"id": "aws-logs", "cat": "aws", "cmd": "aws logs tail /aws/lambda/myfn --follow", "desc": "Tail a CloudWatch log group in real time, much like 'tail -f'.", "example": "aws logs tail /aws/lambda/myfn --since 1h"},
    {"id": "aws-region", "cat": "aws", "cmd": "aws configure set region us-east-1", "desc": "Pin the default region for subsequent CLI calls (or use AWS_REGION env var).", "example": "AWS_REGION=eu-west-1 aws ec2 describe-instances"},

    {"id": "k8s-get-pods", "cat": "kubernetes", "cmd": "kubectl get pods -A", "desc": "List every pod across all namespaces. Add -o wide for node + IP.", "example": "kubectl get pods -A -o wide"},
    {"id": "k8s-describe", "cat": "kubernetes", "cmd": "kubectl describe pod NAME", "desc": "Show a pod's events, conditions, container statuses — the first stop when something is wrong.", "example": "kubectl describe pod my-app-abc -n production"},
    {"id": "k8s-logs", "cat": "kubernetes", "cmd": "kubectl logs -f pod/NAME", "desc": "Follow logs from a pod. Add -c CONTAINER to pick a sidecar.", "example": "kubectl logs -f deploy/nginx --tail=100"},
    {"id": "k8s-exec", "cat": "kubernetes", "cmd": "kubectl exec -it pod/NAME -- sh", "desc": "Open an interactive shell inside a running container.", "example": null},
    {"id": "k8s-apply", "cat": "kubernetes", "cmd": "kubectl apply -f manifest.yaml", "desc": "Create or update resources declared in a YAML manifest.", "example": "kubectl apply -k ./overlays/prod   # with kustomize"},
    {"id": "k8s-port-forward", "cat": "kubernetes", "cmd": "kubectl port-forward svc/NAME 8080:80", "desc": "Tunnel a local port to a service for ad-hoc access.", "example": null},
    {"id": "k8s-rollout", "cat": "kubernetes", "cmd": "kubectl rollout status deploy/NAME", "desc": "Watch a deployment roll out; exits when stable. Use 'undo' to revert.", "example": "kubectl rollout undo deploy/api"},
    {"id": "k8s-ns", "cat": "kubernetes", "cmd": "kubectl config set-context --current --namespace=prod", "desc": "Pin the current kubectl context to a namespace so you can stop typing -n.", "example": null},
    {"id": "k8s-top", "cat": "kubernetes", "cmd": "kubectl top pod -A", "desc": "Show per-pod CPU/memory usage (requires metrics-server).", "example": "kubectl top node"},
    {"id": "k8s-events", "cat": "kubernetes", "cmd": "kubectl get events --sort-by=.lastTimestamp", "desc": "List recent cluster events — invaluable for figuring out why a pod won't schedule.", "example": null},

    {"id": "dk-ps", "cat": "docker", "cmd": "docker ps -a", "desc": "List every container (running and stopped) with status and ports.", "example": null},
    {"id": "dk-run", "cat": "docker", "cmd": "docker run -it --rm nginx", "desc": "Run a container interactively and delete it on exit.", "example": "docker run -p 8080:80 nginx"},
    {"id": "dk-build", "cat": "docker", "cmd": "docker build -t myapp:latest .", "desc": "Build an image from the Dockerfile in the current directory.", "example": "docker build --target=runtime -t myapp:1.2 ."},
    {"id": "dk-exec", "cat": "docker", "cmd": "docker exec -it NAME sh", "desc": "Attach a shell inside a running container.", "example": null},
    {"id": "dk-logs", "cat": "docker", "cmd": "docker logs -f --tail=100 NAME", "desc": "Tail a container's stdout/stderr.", "example": null},
    {"id": "dk-prune", "cat": "docker", "cmd": "docker system prune -a", "desc": "Reclaim disk by removing stopped containers, unused networks, dangling images.", "example": "docker volume prune"},

    {"id": "git-status", "cat": "git", "cmd": "git status", "desc": "Show working-tree state: staged, unstaged, untracked.", "example": null},
    {"id": "git-log", "cat": "git", "cmd": "git log --oneline --graph --decorate -20", "desc": "Compact branch-aware history for the last 20 commits.", "example": null},
    {"id": "git-diff", "cat": "git", "cmd": "git diff --staged", "desc": "Show what's queued for the next commit.", "example": "git diff HEAD~3..HEAD"},
    {"id": "git-branch", "cat": "git", "cmd": "git switch -c feature/x", "desc": "Create and switch to a new branch (modern alternative to 'git checkout -b').", "example": null},
    {"id": "git-stash", "cat": "git", "cmd": "git stash", "desc": "Save uncommitted changes and clean the working tree. Restore with 'git stash pop'.", "example": "git stash push -m 'wip refactor'"},
    {"id": "git-rebase", "cat": "git", "cmd": "git rebase main", "desc": "Replay the current branch on top of main. Use after pulling to keep history linear.", "example": "git pull --rebase origin main"},
    {"id": "git-reset", "cat": "git", "cmd": "git reset HEAD~1", "desc": "Undo the last commit, keeping changes in the working tree (--hard discards them).", "example": null},
    {"id": "git-blame", "cat": "git", "cmd": "git blame -L 30,60 file", "desc": "Show who last touched lines 30-60 of a file and in which commit.", "example": null},

    {"id": "net-curl", "cat": "networking", "cmd": "curl -v https://example.com", "desc": "Verbose HTTP request — surfaces redirects, headers, TLS handshake.", "example": "curl -I https://example.com   # headers only"},
    {"id": "net-dig", "cat": "networking", "cmd": "dig +short example.com", "desc": "Resolve a DNS name; +short prints just the answer.", "example": "dig MX gmail.com +short"},
    {"id": "net-nslookup", "cat": "networking", "cmd": "nslookup example.com 8.8.8.8", "desc": "Query a specific DNS resolver (here Google's 8.8.8.8) instead of the system default.", "example": null},
    {"id": "net-traceroute", "cat": "networking", "cmd": "traceroute example.com", "desc": "Show every hop between you and the target. Use 'mtr' for a live view.", "example": "mtr example.com"},
    {"id": "net-nmap", "cat": "networking", "cmd": "nmap -sV -p 1-1024 host", "desc": "Service-version scan of low ports. -sS for stealth SYN scan (root).", "example": "nmap -A 10.0.0.0/24"},
    {"id": "net-tcpdump", "cat": "networking", "cmd": "tcpdump -i any -nn port 443", "desc": "Capture packets on any interface for one port. -w saves to a pcap file.", "example": "tcpdump -i any -nn -w out.pcap port 443"},
    {"id": "net-iptables", "cat": "networking", "cmd": "iptables -L -n -v", "desc": "List firewall rules with packet/byte counters and numeric addresses.", "example": null},
    {"id": "net-openssl", "cat": "networking", "cmd": "openssl s_client -connect host:443 -servername host", "desc": "Open a raw TLS session — surfaces the certificate chain and supported ciphers.", "example": null}
  ]
}

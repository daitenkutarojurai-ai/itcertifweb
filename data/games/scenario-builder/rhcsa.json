{
  "mode": "scenario-builder",
  "title": "Order the RHEL boot sequence",
  "explanation": "A RHEL system boots: UEFI/BIOS firmware runs POST and hands off to GRUB2, which loads the kernel and initramfs; the kernel starts systemd (PID 1), which brings up the default target (multi-user or graphical).",
  "data": {
    "scenario": "Power on a RHEL 9 server. Order what runs during boot.",
    "pool": [
      "UEFI/BIOS firmware",
      "GRUB2 bootloader",
      "Kernel + initramfs",
      "systemd (PID 1)",
      "Default target",
      "Cron job",
      "fstab mount"
    ],
    "correctOrder": [
      "UEFI/BIOS firmware",
      "GRUB2 bootloader",
      "Kernel + initramfs",
      "systemd (PID 1)",
      "Default target"
    ]
  }
}

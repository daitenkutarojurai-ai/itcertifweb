{
  "mode": "scenario-builder",
  "title": "Deploy a VM: build the dependency chain",
  "explanation": "An Azure VM depends on a chain: a resource group holds everything, a virtual network and subnet provide addressing, a network interface attaches the VM to the subnet, and finally the VM is created on that NIC.",
  "data": {
    "scenario": "Create the resources an Azure VM needs, in dependency order (each step needs the one before it).",
    "pool": [
      "Resource group",
      "Virtual network",
      "Subnet",
      "Network interface",
      "Virtual machine",
      "Availability set",
      "Public IP address"
    ],
    "correctOrder": [
      "Resource group",
      "Virtual network",
      "Subnet",
      "Network interface",
      "Virtual machine"
    ]
  }
}

{
  "mode": "scenario-builder",
  "title": "Expose a web app in Kubernetes",
  "explanation": "A Deployment manages the pod replicas, a Service gives them one stable virtual IP and load-balances across them, and an Ingress exposes the Service to external HTTP(S) traffic with host/path routing.",
  "data": {
    "scenario": "Expose a stateless web app to external HTTPS traffic. Order the objects you create.",
    "pool": [
      "Deployment",
      "Service",
      "Ingress",
      "ConfigMap",
      "PersistentVolumeClaim",
      "Namespace"
    ],
    "correctOrder": [
      "Deployment",
      "Service",
      "Ingress"
    ]
  }
}

{
  "meta": {
    "id": "rhcsa",
    "name": "Red Hat Certified System Administrator (RHCSA)",
    "vendor": "Red Hat",
    "version": "2.0.3",
    "question_count": 61,
    "last_updated": "2026-05-13",
    "notes": " [rhcsa batch 1: 18 scenario rewrites] [rhcsa batch 2: 21 scenario rewrites]"
  },
  "questions": [
    {
      "id": "rhcsa-001",
      "question": "A sysadmin sets file permissions for an audit-logged shell script: OWNER must have full read+write+execute, GROUP must have read+execute (no write), OTHER must have read only (no execute, no write). The mode is written as a 3-digit octal. Which command sets the permissions correctly?",
      "options": [
        "`chmod 755 file` — sets `rwxr-xr-x` (group + other can execute); doesn't match (other should NOT execute)",
        "`chmod 777 file` — sets `rwxrwxrwx` (everyone can write); too permissive",
        "`chmod 644 file` — sets `rw-r--r--` (no exec for owner); too restrictive",
        "`chmod 754 file` — owner 7 (rwx) + group 5 (r-x) + other 4 (r--) → exactly the rwxr-xr-- target"
      ],
      "correct": 3,
      "explanation": "Octal permissions: owner(rwx=7), group(r-x=5), others(r--=4) = 754. chmod 754 file sets this. Alternatively: chmod u=rwx,g=rx,o=r file. Use ls -l to verify.",
      "tags": [
        "filesystem",
        "chmod"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-002",
      "question": "A custom systemd unit `myapp.service` is installed in `/etc/systemd/system/`. It's started manually with `systemctl start myapp` and works fine, BUT after reboot it doesn't start. The admin wants it to start automatically at boot. Which command creates the symlink in the WantedBy target so it starts at boot?",
      "options": [
        "`systemctl activate myapp.service` — invalid; `activate` is not a systemctl verb",
        "`systemctl enable myapp.service` — creates the symlink under `/etc/systemd/system/<target>.wants/` so the unit starts at boot",
        "`systemctl start myapp.service` — starts ONCE for the current session; doesn't persist across reboots",
        "`service myapp enable` — SysVinit-style, not systemd-native; on RHEL systemd is the init"
      ],
      "correct": 1,
      "explanation": "systemctl enable service: creates symlinks in /etc/systemd/system/multi-user.target.wants/ so service starts at boot. systemctl enable --now service: enable AND start immediately. systemctl disable: remove autostart. systemctl is-enabled: check status.",
      "tags": [
        "systemd",
        "enable"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-003",
      "question": "A volume group `vg0` has 50 GB of free extents. The admin needs to create a LOGICAL VOLUME of 5 GB named `data` from `vg0`. Which command is the canonical syntax?",
      "options": [
        "`lvcreate -L 5G -n data vg0` — creates a 5 GiB LV named `data` in VG `vg0` (canonical)",
        "`lvmkdir -s 5G vg0/data` — not a real lvm command",
        "`mkvolume -L 5G -n data vg0` — not a real lvm command",
        "`lvcreate -s 5G data vg0` — `-s` is for snapshots, and arguments are out of order"
      ],
      "correct": 0,
      "explanation": "lvcreate -L 5G -n data vg0: -L size, -n name, vg0 is the volume group. After: mkfs.xfs /dev/vg0/data then mount. -l for LE units: -l 100%FREE uses all free space. lvs: list logical volumes.",
      "tags": [
        "lvm",
        "logical-volumes"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-004",
      "question": "An XFS filesystem on `/dev/vg0/data` is currently mounted on `/mnt/data` via the command line. To survive a reboot, the mount must be PERSISTENT. Which configuration file should the admin edit, and what line?",
      "options": [
        "Append to `/etc/fstab`: `/dev/vg0/data /mnt/data xfs defaults 0 0` — the canonical persistent mount entry",
        "`mountd --persistent` — not a real command",
        "Append `mount …` to `/etc/rc.local` — discouraged on RHEL 7+; fstab is the canonical method",
        "Run `mount /dev/vg0/data /mnt/data` — does NOT persist across reboots"
      ],
      "correct": 0,
      "explanation": "/etc/fstab format: device mountpoint filesystem options dump pass. defaults = rw,suid,dev,exec,auto,nouser,async. 0 0 = no dump, no fsck. After editing: mount -a to test without reboot. Use blkid to get UUID for more reliable mounting.",
      "tags": [
        "filesystem",
        "fstab"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-005",
      "question": "RHEL ships SELinux enabled by default. SELinux enforces MANDATORY ACCESS CONTROL (MAC) on top of DAC — every process + file has a label (`type:role:user`) and policy decides which transitions are allowed. SELinux has THREE modes set via `/etc/selinux/config` or `setenforce`. Which describes SELinux and its modes?",
      "options": [
        "A firewall — false; SELinux is a MAC layer, not a packet filter",
        "Mandatory access control with three modes: Enforcing (block + log violations), Permissive (log only, allow), Disabled (off entirely)",
        "Kernel module manager — false; that's lsmod / modprobe",
        "An authentication system — false; SELinux is authorization, not authentication"
      ],
      "correct": 1,
      "explanation": "SELinux: mandatory access control beyond DAC (discretionary). Enforcing: production mode, violations blocked. Permissive: troubleshooting, logs denials but doesn't block. Disabled: requires reboot to change. /etc/selinux/config for persistent change. semanage, restorecon, ausearch -m avc: key tools. Enforcing, Permissive, Disabled",
      "tags": [
        "security",
        "selinux"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-006",
      "question": "A sysadmin needs to find EVERY file under `/var` owned by user `alice` (not group, but owner uid mapping to alice). Which command is correct?",
      "options": [
        "`find /var -user alice` — searches recursively under /var for files whose owner is the user `alice`",
        "`grep -r alice /var` — searches file CONTENTS for the literal string 'alice', not ownership",
        "`ls -la /var | grep alice` — only the top-level directory of /var, doesn't recurse, and matches any literal text including filename",
        "`locate -user alice /var` — locate doesn't have a `-user` flag"
      ],
      "correct": 0,
      "explanation": "find /var -user alice: finds all files owned by alice. find /var -group devs: by group. find /var -perm /4000: find SUID files. find /var -mtime -7: modified in last 7 days. find /var -size +100M: files > 100MB. -exec rm {} \\; to act on results.",
      "tags": [
        "filesystem",
        "find"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-007",
      "question": "A RHEL host runs `firewalld`. The admin needs to LIST the active firewall config: enabled zones, allowed services/ports, sources, masquerade settings — all in one human-readable view. Which `firewall-cmd` invocation produces this?",
      "options": [
        "`iptables -L` — shows iptables rules; firewalld has its own data model on top",
        "`netstat -tuln` — shows LISTENING sockets, not firewall config",
        "`firewall-cmd --list-all` — dumps the default zone's full active firewalld config (services / ports / masquerade / sources)",
        "`ss -tulnp` — shows sockets + processes, not firewall rules"
      ],
      "correct": 2,
      "explanation": "firewall-cmd --list-all: shows active zone, interfaces, sources, services, ports. --list-ports: ports only. --list-services: services only. Add port: firewall-cmd --add-port=8080/tcp --permanent then --reload. firewall-cmd --zone=public --add-service=http --permanent.",
      "tags": [
        "security",
        "firewalld"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-008",
      "question": "An XFS filesystem mounted at `/mnt/data` lives on an LV that was just extended by 10 GiB (`lvextend -L +10G vg0/data`). The filesystem itself still reports the old size. Which command grows the XFS to fill the LV?",
      "options": [
        "`xfs_growfs /mnt/data` — extends the XFS filesystem to use the new LV size (xfs can only grow, never shrink)",
        "`xfs_expand /mnt/data` — not a real command",
        "`resize2fs /mnt/data` — wrong filesystem; resize2fs is for ext2/3/4",
        "`fsck -r /mnt/data` — filesystem check, doesn't resize"
      ],
      "correct": 0,
      "explanation": "Expand LV: lvextend -L +10G /dev/vg0/data. Extend XFS: xfs_growfs /mnt/data (use mount point, not device). XFS can be grown while mounted. ext4: resize2fs /dev/vg0/data (use device). Always expand LV first, then filesystem.",
      "tags": [
        "filesystem",
        "xfs-growfs"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-009",
      "question": "A sysadmin wants a `nightly-backup.sh` to run every night at 02:30. The user-level scheduling subsystem in RHEL is `cron`. Which describes the purpose AND the canonical command to edit the current user's crontab?",
      "options": [
        "Backup-only scheduler — false; cron schedules any command",
        "Schedule recurring tasks (minute / hour / day-of-month / month / day-of-week + command); edit with `crontab -e` (per-user) or `/etc/crontab` (system-wide)",
        "Init-system replacement — false; cron is a scheduler, not an init",
        "Network-only scheduler — false; cron is general-purpose"
      ],
      "correct": 1,
      "explanation": "crontab format: */5 * * * * /path/to/script (every 5 min). Fields: min(0-59) hour(0-23) day(1-31) month(1-12) weekday(0-7). crontab -l: list. crontab -r: remove. /etc/crontab: system cron with user field. /etc/cron.d/: additional cron files. systemd timers: modern alternative. crontab -e, format: min hour day month weekday cmd",
      "tags": [
        "scheduling",
        "cron"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-010",
      "question": "Create a new user `bob` with UID 1500, default shell `/bin/bash`, and a home directory `/home/bob`. The home directory should be created automatically with the user. Which `useradd` invocation is correct?",
      "options": [
        "`useradd bob --uid 1500 --home /home/bob` — `--uid` and `--home` aren't the standard short flags; missing shell + create-home",
        "`useradd -u 1500 -m -s /bin/bash bob` — `-u` UID, `-m` create home, `-s` shell, then the username; canonical RHEL invocation",
        "`adduser -uid 1500 bob` — `adduser` is a wrapper on some distros; flag isn't `-uid`",
        "`usermod -u 1500 -s /bin/bash bob` — modifies an existing user; `useradd` creates"
      ],
      "correct": 1,
      "explanation": "useradd -u 1500 -m -s /bin/bash bob: -u UID, -m create home, -s shell. Set password: passwd bob. -g primary group: -g groupname. -G supplementary groups: -G wheel,docker. View: id bob. Modify existing user: usermod. Delete: userdel -r bob (remove home).",
      "tags": [
        "users",
        "useradd"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-011",
      "question": "A shared `/tmp` directory has world-writable permissions (1777). Without an extra flag, any user could delete any other user's files in `/tmp`. The 'sticky bit' on a directory restricts this so ONLY the file's owner (or root) can delete it. Which describes the sticky bit's effect?",
      "options": [
        "Only the owner (or root) can DELETE files in that directory — even with world-write, others can't `rm` them",
        "Prevents anyone from writing — false; that's a permission, not the sticky bit",
        "Sets SGID on new files — that's the SGID bit, not sticky",
        "Makes contents immutable — false; that's `chattr +i`"
      ],
      "correct": 0,
      "explanation": "Sticky bit (chmod +t or mode 1xxx): files in directory can only be deleted by: file owner, directory owner, or root. Common on /tmp (world-writable but users can't delete each other's files). ls -ld shows 't' in others execute position. chmod 1777 /shared sets sticky+rwxrwxrwx.",
      "tags": [
        "filesystem",
        "sticky-bit"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-012",
      "question": "Set a STATIC IP `192.168.1.10/24` with gateway `192.168.1.1` and DNS `8.8.8.8` on interface `eth0` using NetworkManager. The change must persist across reboots. Which commands?",
      "options": [
        "`nmcli con mod eth0 ipv4.addresses 192.168.1.10/24 ipv4.gateway 192.168.1.1 ipv4.dns 8.8.8.8 ipv4.method manual` then `nmcli con up eth0` — persistent via NetworkManager",
        "`ip addr add 192.168.1.10/24 dev eth0` — temporary, lost on reboot",
        "`ifconfig eth0 192.168.1.10 netmask 255.255.255.0` — `ifconfig` is deprecated, temporary",
        "`vi /etc/network/interfaces` — Debian/Ubuntu file, not used by RHEL/NetworkManager"
      ],
      "correct": 0,
      "explanation": "nmcli con mod CONNECTION ipv4.addresses IP/MASK ipv4.gateway GW ipv4.dns DNS ipv4.method manual. Then nmcli con up CONNECTION. Persistent. nmcli con show: list connections. nmcli device status: device states. Alternatively: edit /etc/NetworkManager/system-connections/CONNECTION.nmconnection.",
      "tags": [
        "networking",
        "nmcli"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-013",
      "question": "A systemd-running RHEL host. The admin wants LIVE tailing of ALL systemd journal entries (kernel + services + user) as they happen — so they can watch a service starting in another terminal. Which command?",
      "options": [
        "`tail -f /var/log/messages` — legacy rsyslog file; doesn't cover all systemd-journald entries on modern RHEL",
        "`journalctl -f` — follow mode on systemd-journald; live tailing of ALL units + kernel + user entries",
        "`dmesg --follow` — only kernel ring buffer, not systemd units",
        "`cat /var/log/syslog` — Debian/Ubuntu file, not RHEL"
      ],
      "correct": 1,
      "explanation": "journalctl -f: follow (like tail -f). journalctl -u nginx: logs for specific unit. journalctl -xe: recent logs with explanations. journalctl --since '1 hour ago': time filter. journalctl -p err: error level and above. journalctl --disk-usage: storage used. /var/log/messages: traditional syslog, may still exist.",
      "tags": [
        "logging",
        "journalctl"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-014",
      "question": "Schedule a ONE-OFF (not recurring) task to run a backup script at 15:00 today. After running, it should NOT re-run. Which utility is correct?",
      "options": [
        "`systemd-run --on-calendar='15:00' …` — runs once but creates a transient unit; works but isn't the classical RHCSA answer",
        "`crontab -e` + `0 15 * * *` — that's RECURRING every day at 15:00, not one-off",
        "`once --time=15:00 …` — not a real command",
        "`at 15:00` then enter the command and press Ctrl+D — the canonical one-off scheduler (`at` daemon)"
      ],
      "correct": 3,
      "explanation": "at command: at 15:00 (enter commands, Ctrl+D to finish). at now + 1 hour. atq: list scheduled at jobs. atrm JOB_NUM: remove. Service: atd must be running. For recurring: use cron. at is for one-time delayed execution.",
      "tags": [
        "scheduling",
        "at"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-015",
      "question": "The `/etc/sudoers` file defines which users can run which commands as root. Editing it directly with `vi` is DANGEROUS — a syntax error locks out sudo. A wrapper tool opens the file in an editor, validates syntax on save, and rejects bad edits. Which tool, and what's `/etc/sudoers` for?",
      "options": [
        "Firewall — wrong file",
        "PAM auth — different file",
        "SSH authorized_keys — different file",
        "`/etc/sudoers` defines sudo privileges per user / group; ALWAYS edit with `visudo` which validates syntax before saving"
      ],
      "correct": 3,
      "explanation": "visudo: opens /etc/sudoers in editor with syntax checking — prevents saving invalid syntax that would break sudo for all users. %wheel ALL=(ALL) ALL: all wheel group members can sudo. User bob can restart nginx without password: bob ALL=(ALL) NOPASSWD: /bin/systemctl restart nginx. /etc/sudoers.d/: drop-in files.",
      "tags": [
        "security",
        "sudoers"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-016",
      "question": "An SELinux policy admin wants to LIST every SELinux boolean related to `httpd` (e.g., `httpd_can_network_connect`, `httpd_enable_homedirs`). The output must include current ON/OFF state. Which command?",
      "options": [
        "`selinux list httpd` — not a real subcommand",
        "`getsebool -a | grep httpd` — lists ALL SELinux booleans + state; grep filters to httpd-related",
        "`sestatus --booleans httpd` — not a sestatus flag",
        "`ls /etc/selinux/booleans/httpd` — wrong path / wrong layout"
      ],
      "correct": 1,
      "explanation": "getsebool -a: list all booleans with current values. getsebool httpd_can_network_connect: specific boolean. setsebool -P httpd_can_network_connect on: set persistently (-P). Common booleans: httpd_can_network_connect (allow Apache to make outbound connections), httpd_use_nfs (allow serving NFS-mounted content).",
      "tags": [
        "selinux",
        "booleans"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-017",
      "question": "An admin needs to add user `alice` to the existing supplementary group `wheel` (so she gains sudo privileges) WITHOUT removing her from any other groups. Which command preserves existing group memberships and adds wheel?",
      "options": [
        "`groupadd wheel alice` — `groupadd` creates groups, doesn't add users",
        "`addgroup alice wheel` — Debian/Ubuntu syntax, not RHEL",
        "`usermod -aG wheel alice` — `-a` appends, `-G` group; preserves existing supplementary memberships",
        "`gpasswd -a wheel alice` — argument order reversed; correct order is `gpasswd -a alice wheel`"
      ],
      "correct": 2,
      "explanation": "usermod -aG wheel username: -a (append, don't replace other groups), -G (supplementary group). Without -a: replaces ALL supplementary groups! gpasswd -a username wheel: alternative. id username: verify. User needs to re-login for group change to take effect (or: newgrp wheel in current session).",
      "tags": [
        "users",
        "usermod"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-018",
      "question": "An admin partitions a spare disk as `/dev/sdb2` and wants to add it as additional swap space — both ACTIVATED NOW and PERSISTED across reboots. Which sequence?",
      "options": [
        "`swapformat /dev/sdb2 && swapon` — not a real command",
        "`fdisk /dev/sdb2 --swap` — fdisk doesn't have `--swap`",
        "`mkswap /dev/sdb2` (format as swap), `swapon /dev/sdb2` (activate now), then add `/dev/sdb2 swap swap defaults 0 0` to `/etc/fstab` (persistent)",
        "`mkswap` then `systemctl restart swapd` — `swapd` is not a service"
      ],
      "correct": 2,
      "explanation": "Steps: (1) fdisk/parted to create partition with type 82 (Linux swap). (2) mkswap /dev/sdb2. (3) swapon /dev/sdb2 (immediate). (4) /etc/fstab: '/dev/sdb2 swap swap defaults 0 0'. swapon -s: show active swap. free -h: memory + swap stats. swapfile: dd + mkswap + swapon works too. add to /etc/fstab",
      "tags": [
        "storage",
        "swap"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-019",
      "question": "Linux name-to-IP resolution consults a CONFIGURABLE list of sources (by default `files` then `dns`, defined in `/etc/nsswitch.conf`). The `files` source reads from `/etc/hosts`. An entry in `/etc/hosts` like `10.0.5.42 internal.example.com` overrides DNS for that name. What is `/etc/hosts`?",
      "options": [
        "Replaces /etc/resolv.conf — false; resolv.conf configures DNS servers, hosts maps names locally",
        "Localhost-only — false; can store any local override",
        "Hostname-only — false; that's `/etc/hostname`",
        "Local hostname → IP override map; checked BEFORE DNS based on the order in `/etc/nsswitch.conf`"
      ],
      "correct": 3,
      "explanation": "/etc/hosts: IP FQDN aliases per line. /etc/nsswitch.conf hosts: files dns — 'files' means check /etc/hosts first. For testing: add temporary entry. /etc/resolv.conf: DNS server configuration (nameserver IP, domain, search). hostnamectl: set system hostname. dnsdomainname: show domain. checked before DNS per /etc/nsswitch.conf",
      "tags": [
        "networking",
        "hosts-file"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-020",
      "question": "A RHEL server should boot DIRECTLY into the multi-user (non-graphical) target — equivalent to the old SysVinit runlevel 3 — without a GUI. The change must persist across reboots. Which systemd command?",
      "options": [
        "`init 3` — switches the CURRENT runlevel; doesn't persist",
        "`systemctl set-default multi-user.target` — sets the persistent default target for future boots",
        "`runlevel set 3` — `runlevel` only reports current/previous; can't `set`",
        "`systemctl default --target=multi-user` — wrong invocation; `default` is a verb that switches now, not the persistent setter"
      ],
      "correct": 1,
      "explanation": "systemctl set-default multi-user.target: non-graphical multi-user (like runlevel 3). graphical.target: desktop (runlevel 5). rescue.target: single-user maintenance. systemctl isolate multi-user.target: switch immediately (kills graphical). systemctl get-default: check current default target.",
      "tags": [
        "systemd",
        "default-target"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-021",
      "question": "An admin archives `/etc/myapp/` into a single compressed file `archive.tar.gz` and verifies what's in it. Which command CREATES the gzipped tar archive?",
      "options": [
        "`tar -czvf archive.tar.gz /etc/myapp/` — `c` create, `z` gzip, `v` verbose, `f` filename; the canonical create+compress",
        "`tar -xzvf archive.tar.gz` — `x` is EXTRACT, not create",
        "`gzip -r ... > archive.tar.gz` — gzip alone doesn't produce a tar; produces individual .gz files",
        "`zip -r archive.tar.gz ...` — uses ZIP format, not tar.gz; misleading filename"
      ],
      "correct": 0,
      "explanation": "tar -czvf: -c create, -z gzip compression, -v verbose, -f filename. Extract: tar -xzvf archive.tar.gz. List contents: tar -tzvf. To specific dir: tar -xzvf archive.tar.gz -C /target/. .tar.bz2: use -j instead of -z. .tar.xz: use -J. --exclude=PATTERN to exclude files.",
      "tags": [
        "archives",
        "tar"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-022",
      "question": "A new RHEL host needs to access Red Hat repos for `dnf update`. Without a subscription, only the local install ISO repos are available. The customer's Red Hat account has a subscription pool. Which describes a RHEL subscription and the registration command?",
      "options": [
        "Network-only license — false",
        "Firewall license — false",
        "Entitlement granting access to Red Hat repos + security updates; register with `subscription-manager register --username … --password …` then `subscription-manager attach --auto`",
        "License file in `/etc/redhat-release` — false; that file is just OS version info"
      ],
      "correct": 2,
      "explanation": "RHEL subscription: required for yum/dnf updates and support. subscription-manager register: authenticate with RHSM (Red Hat Subscription Management) or Satellite. subscription-manager status: check. subscription-manager repos --list: see available repos. In RHEL 8+: insights-client --register for Red Hat Insights. register with subscription-manager register",
      "tags": [
        "administration",
        "subscription"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-023",
      "question": "A web server's DocumentRoot was moved from `/var/www/html` to `/srv/web`. Apache refuses to read it because the new path has the wrong SELinux file context. The admin wants to APPLY the correct default contexts for `/srv/web` based on the SELinux policy database. Which command?",
      "options": [
        "`restorecon -Rv /srv/web` — recursively re-applies default SELinux contexts from the policy database; verbose",
        "`selinux --restore /srv/web` — not a real command",
        "`chcon --default /srv/web` — `chcon` can apply contexts but `--default` isn't standard; `restorecon` is canonical",
        "`sestatus --restore` — `sestatus` only shows status; no restore flag"
      ],
      "correct": 0,
      "explanation": "restorecon -Rv /path: -R recursive, -v verbose. Restores contexts based on policy defaults (/etc/selinux/targeted/contexts/files/). Use after: moving files (context doesn't follow move), creating new files in wrong location. matchpathcon -V: verify contexts. semanage fcontext: add custom file context rules.",
      "tags": [
        "selinux",
        "restorecon"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-024",
      "question": "Two ways to create 'a link' on Linux. Hard link: SECOND directory entry pointing at the SAME inode (same data); requires same filesystem; can't link directories. Symbolic link: SEPARATE file whose content is a TEXTUAL path to the target; can cross filesystems and link directories. Which describes the difference?",
      "options": [
        "Symlinks share inode — false; hard links share inode",
        "Hard links share the inode (same data), constrained to one filesystem, can't link directories; symlinks are separate inodes containing a path, cross-filesystem, can link directories",
        "Hard links cross filesystems — false; opposite",
        "No difference — false"
      ],
      "correct": 1,
      "explanation": "Hard link: ln source dest. Same inode. Deleting original doesn't break link. Cannot link directories (normally). Same filesystem only. Soft link: ln -s /path/to/source dest. Different inode (stores path). Breaks if source deleted/moved. Can cross filesystems, link directories, point to non-existent paths. symlinks store a path and can cross filesystems",
      "tags": [
        "filesystem",
        "links"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-025",
      "question": "RHEL 8+ uses `chrony` (not `ntpd`) as the default time-sync daemon. The admin wants to ADD an upstream NTP server `time.nist.gov` to chrony's config and reload. Which approach?",
      "options": [
        "`systemctl start ntpd` — wrong daemon",
        "`ntpdate -u …` — legacy one-shot, not chrony",
        "`timedatectl set-ntp true` — enables NTP sync but doesn't pick a SPECIFIC server",
        "Add `server time.nist.gov iburst` to `/etc/chrony.conf`, then `systemctl restart chronyd`; runtime: `chronyc add server time.nist.gov`"
      ],
      "correct": 3,
      "explanation": "chrony (RHEL 7+): /etc/chrony.conf — server ntp.example.com iburst. systemctl restart chronyd. chronyc sources: show NTP sources. chronyc tracking: show sync status. timedatectl: set timezone and check sync status. timedatectl list-timezones. timedatectl set-timezone America/New_York. chronyd preferred over ntpd in RHEL 7+.",
      "tags": [
        "time",
        "chrony"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-026",
      "question": "Standard UNIX file permissions allow ONE owner + ONE group + other. A team of 5 users needs DIFFERENT permissions per user on the same file — beyond the rwx/owner/group/other model. A POSIX feature lets you set per-user / per-group permissions on a file via `setfacl`. Which feature?",
      "options": [
        "SELinux policy — different layer (MAC, not granular DAC)",
        "firewalld zone — wrong category",
        "iptables rule — wrong category",
        "POSIX Access Control Lists (ACLs) — extended file permissions via `setfacl -m u:alice:rw file` / `getfacl file` for per-user/per-group access"
      ],
      "correct": 3,
      "explanation": "ACL: setfacl -m u:USER:PERMS file, setfacl -m g:GROUP:PERMS file, setfacl -m o::PERMS file. -x: remove entry. -b: remove all ACLs. -R: recursive. Default ACL for directory (-d): inherited by new files. getfacl file: shows all ACL entries. Filesystem needs acl mount option (default on ext4/xfs in RHEL). setfacl -m u:alice:rw file grants per-user access",
      "tags": [
        "filesystem",
        "acls"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-027",
      "question": "A router shows `10.2.0.0/24 via 192.168.1.254` is not reachable. The admin needs to add a route for that network. They want both: TEMPORARY (current session, lost on reboot) and PERSISTENT (survives reboot). Which approach?",
      "options": [
        "`netstat route add` — `netstat` doesn't add routes",
        "`route add NETWORK/MASK GW` — `route` is deprecated on modern RHEL; doesn't persist",
        "Temporary: `ip route add 10.2.0.0/24 via 192.168.1.254`; Persistent: add to NetworkManager via `nmcli con mod CONN +ipv4.routes \"10.2.0.0/24 192.168.1.254\"` then `nmcli con up CONN`",
        "`ifconfig route add` — ifconfig doesn't manage routes"
      ],
      "correct": 2,
      "explanation": "Temporary: ip route add 192.168.10.0/24 via 10.0.0.1 (lost on reboot). Permanent (NetworkManager): nmcli con mod CONNECTION ipv4.routes '192.168.10.0/24 10.0.0.1' then nmcli con up CONNECTION. Or edit connection file. ip route show: display routing table. Permanent: nmcli con mod CON ipv4.routes",
      "tags": [
        "networking",
        "routes"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-028",
      "question": "A user creates a new file with `touch /tmp/test`. The default permissions are NOT 0666 (rw-rw-rw-) — they're filtered by a shell setting that defines which bits are SUBTRACTED. With umask 022, new files default to 0644 (rw-r--r--), new directories to 0755. What is `umask`?",
      "options": [
        "Which users can execute — wrong concept",
        "SELinux context — different mechanism",
        "Minimum password length — different setting",
        "Default permission bits SUBTRACTED from 0666 (files) / 0777 (directories) when CREATING new files / directories"
      ],
      "correct": 3,
      "explanation": "umask: subtracted from max (files=666, dirs=777). umask 022: files=644, dirs=755 (common default). umask 027: files=640, dirs=750 (more secure). Set in: /etc/profile (system), ~/.bashrc (user). umask without args: shows current. Doesn't affect existing files.",
      "tags": [
        "filesystem",
        "umask"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-029",
      "question": "An admin needs to find the `httpd` package and install it on RHEL 8/9 (which uses `dnf` as package manager — `yum` is now a compat-symlink). Which command pair?",
      "options": [
        "`yum search httpd; yum install httpd` — works (yum is a compatibility wrapper to dnf on RHEL 8/9), but `dnf` is the canonical answer",
        "`rpm -search httpd` — `rpm` doesn't search remote repos",
        "`apt search httpd` — Debian/Ubuntu tool, not RHEL",
        "`dnf search httpd` then `dnf install httpd` — the canonical RHEL 8/9 invocation"
      ],
      "correct": 3,
      "explanation": "dnf search httpd: find packages. dnf info httpd: details. dnf install httpd: install. dnf remove httpd: uninstall. dnf update: update all. dnf upgrade: update + obsoletes. dnf list installed: installed packages. dnf provides /usr/sbin/httpd: which package provides file. dnf history: transaction history.",
      "tags": [
        "packaging",
        "dnf"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-030",
      "question": "A disk-space investigator wants to know HOW MUCH disk space the `/var/log` directory is consuming. `df` shows free space per FILESYSTEM, which doesn't directly answer the per-DIRECTORY question. Which command answers per-directory?",
      "options": [
        "`df -h /var/log` — filesystem-level usage; says nothing per directory",
        "`du -sh /var/log` — summarises (`-s`) human-readable (`-h`) directory total; the canonical per-directory disk-usage tool",
        "`fdisk -l` — partition layout, no usage",
        "`ls -lh /var/log` — lists files (not recursive size of subdirectories)"
      ],
      "correct": 1,
      "explanation": "du -sh /path: -s summary (total), -h human readable. du -h --max-depth=1 /var: one level deep. du -sh *: all items in current dir. df -h: filesystem disk space (mounted filesystems). df -i: inode usage. lsblk: block devices. fdisk -l: partition table.",
      "tags": [
        "filesystem",
        "du"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-031",
      "question": "An admin needs to enforce a PASSWORD-AGING policy on user `alice`: max age 90 days, warn 7 days before expiry. The command modifies `/etc/shadow` aging fields. Which?",
      "options": [
        "`passwd --expire alice` — IMMEDIATELY expires, doesn't set max age policy",
        "`chage -M 90 -W 7 alice` — `-M` max days, `-W` warn days; the canonical password-aging tool",
        "`usermod --password-age` — `usermod` doesn't have password-aging flags; that's chage's job",
        "`passwdage --set` — not a real command"
      ],
      "correct": 1,
      "explanation": "chage -M 90: max days before password must change. -m 1: minimum days between changes. -W 7: warn 7 days before expiry. -l username: list current aging info. -E 2025-12-31: account expiry date. -I 30: days after expiry before account locked. /etc/login.defs: PASS_MAX_DAYS for system default.",
      "tags": [
        "users",
        "chage"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-032",
      "question": "A sysadmin reads `/proc/cpuinfo`, `/proc/meminfo`, `/proc/<pid>/status`, `/proc/sys/...`. None of these files exist on disk — they're generated ON DEMAND by the kernel exposing live state via a virtual filesystem. Which describes `/proc`?",
      "options": [
        "Core-dump area — false; that's `/var/lib/systemd/coredump` or `/var/crash`",
        "Persistent log storage — false; not persistent",
        "Startup-script directory — false; that's `/etc/init.d` or systemd units",
        "Virtual / synthetic filesystem exposing live kernel + process info as files; entries are generated on read, not stored on disk"
      ],
      "correct": 3,
      "explanation": "/proc: read kernel state. /proc/sys/net/ipv4/ip_forward: IP forwarding. sysctl -w net.ipv4.ip_forward=1: change via sysctl. /etc/sysctl.conf: persistent changes. /proc/PID/: process memory maps, open files, status. cat /proc/cpuinfo: CPU details. /proc/mounts: current mounts.",
      "tags": [
        "fundamentals",
        "proc-filesystem"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-033",
      "question": "Schedule `backup.sh` to run every WEEKDAY (Mon-Fri) at 08:00. Cron's 5-field schedule is `minute hour day-of-month month day-of-week`. Day-of-week 1-5 = Mon-Fri. Which crontab entry?",
      "options": [
        "`systemd-timer --daily --weekdays 8am` — not a real syntax",
        "`0 8 * * 1-5 /path/to/script` — minute 0, hour 8, any day-of-month, any month, day-of-week 1-5 (Mon-Fri); the canonical answer in `crontab -e`",
        "`schedule --daily ...` — not a real command",
        "`8:00 weekdays …` — not the crontab syntax"
      ],
      "correct": 1,
      "explanation": "Cron format: MIN HOUR DOM MON DOW COMMAND. 0 8 * * 1-5 = minute 0, hour 8, any day, any month, Mon-Fri (1-5). crontab -e: edit user crontab. Redirect output: 0 8 * * 1-5 /script >> /var/log/script.log 2>&1. Run as root: use /etc/crontab with user field.",
      "tags": [
        "scheduling",
        "cron"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-034",
      "question": "An admin needs to see (a) every TCP/UDP socket in LISTEN state, (b) the local + remote address + port, (c) the process / pid / executable that owns each socket. Which command provides all of this in ONE invocation?",
      "options": [
        "`netstat -r` — routing table only, no sockets/processes",
        "`ss -tulnp` — `-t` TCP, `-u` UDP, `-l` listening, `-n` no DNS, `-p` show process; the modern netstat replacement",
        "`ip link show` — interface state, no sockets",
        "`ifconfig -a` — interface config, no sockets"
      ],
      "correct": 1,
      "explanation": "ss -tulnp: -t TCP, -u UDP, -l listening, -n numeric (no name resolution), -p show process. Shows: port, state, PID, process name. Requires root for all PIDs. netstat -tulnp: older equivalent (net-tools package). lsof -i :80: what's using port 80. firewall-cmd --list-ports: what's open in firewall.",
      "tags": [
        "networking",
        "ss"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-035",
      "question": "An admin sets a tunable `net.ipv4.ip_forward=1` temporarily via `sysctl -w net.ipv4.ip_forward=1` or `echo 1 > /proc/sys/net/ipv4/ip_forward`. Both are LOST on reboot. To make the change persistent: which file + reload?",
      "options": [
        "`sysctl --permanent` — not a real flag",
        "`echo … > /proc/sys/…` — runtime only, lost on reboot",
        "`modprobe --persist` — `modprobe` loads modules, not sysctl values",
        "Add `net.ipv4.ip_forward=1` to `/etc/sysctl.conf` (or `/etc/sysctl.d/99-custom.conf`), then `sysctl -p` to reload"
      ],
      "correct": 3,
      "explanation": "Temporary: sysctl -w net.ipv4.ip_forward=1 (or echo 1 > /proc/sys/net/ipv4/ip_forward). Persistent: add 'net.ipv4.ip_forward = 1' to /etc/sysctl.conf or create /etc/sysctl.d/99-custom.conf. Apply without reboot: sysctl -p (reads /etc/sysctl.conf) or sysctl --system (reads all sysctl.d files).",
      "tags": [
        "kernel",
        "sysctl"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-036",
      "question": "Two common `ps` flag conventions exist on Linux. `ps aux` (BSD-style, no leading hyphen) shows all processes including TTY, CPU %, MEM %, START, TIME, COMMAND. `ps -ef` (SysV-style, leading hyphen) shows UID, PID, PPID, STIME, TTY, TIME, CMD. Which describes the difference?",
      "options": [
        "`ps aux` — BSD style: shows %CPU + %MEM + START + TIME + COMMAND; `ps -ef` — SysV style: shows UID + PID + PPID + STIME + TIME + CMD; both list all processes",
        "`ps aux` BSD-only — false; works on Linux too",
        "`ps -ef` shows only root processes — false; shows all unless filtered",
        "Identical output — false; different column sets and origins (BSD vs SysV)"
      ],
      "correct": 0,
      "explanation": "ps aux: a=all users, u=user format, x=no terminal. Shows CPU%, MEM%, virtual/resident size. ps -ef: e=all processes, f=full format. Shows PPID (parent PID). Both useful: aux for resource usage, -ef for parent-child relationships. ps auxf: combined with process tree. ps -ef uses UNIX syntax showing PPID",
      "tags": [
        "processes",
        "ps"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-037",
      "question": "An admin wants to set `PROJECT_HOME=/srv/proj` for ALL users system-wide. The setting must be PERSISTENT (survive reboot) and available to logins via any shell (bash, zsh, sh). Which file should they edit?",
      "options": [
        "`systemctl set-env` — only for systemd's environment, not user shells",
        "`env --system` — not a real subcommand",
        "`~/.bashrc` — per-user, bash-only, not system-wide",
        "Add `export PROJECT_HOME=/srv/proj` to `/etc/environment` (PAM, all shells) OR `/etc/profile.d/custom.sh` (login shells)"
      ],
      "correct": 3,
      "explanation": "/etc/environment: simple KEY=VALUE format (no bash syntax), loaded for all users. /etc/profile.d/*.sh: shell scripts sourced for all login shells (can use export, functions). ~/.bashrc: user-specific, non-login shells. ~/.bash_profile: user-specific, login shells. systemd services: set in service unit [Service] Environment=.",
      "tags": [
        "shell",
        "environment"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-038",
      "question": "An admin wants to run a BATCH JOB at LOWER CPU scheduling priority so it doesn't slow down interactive users. Standard niceness scale: -20 (highest priority) to +19 (lowest). The default is 0. A wrapper command launches a process with adjusted niceness. Which command?",
      "options": [
        "Network reachability test — that's `ping`",
        "`nice -n 10 long-running-job.sh` — runs the command with adjusted niceness (here +10 = lower CPU priority); `renice` adjusts running processes",
        "SIGTERM — that's `kill -TERM`",
        "Friendly-message display — wrong command"
      ],
      "correct": 1,
      "explanation": "Niceness: -20 (highest priority) to 19 (lowest). nice -n 5 ./heavy_job: run with niceness +5. nice -n -5 ./critical: higher priority (root only). renice 5 PID: change running process. Useful for: background batch jobs (high nice), critical services (low nice). Affects CPU scheduler only, not I/O.",
      "tags": [
        "processes",
        "nice"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-039",
      "question": "A RHEL host needs to mount an NFS share `nfsserver:/share` at `/mnt/nfs`. The mount must be PERSISTENT (survives reboot). Which sequence?",
      "options": [
        "`mount -t nfs nfsserver:/share /mnt/nfs` (temporary) then add `nfsserver:/share /mnt/nfs nfs defaults 0 0` to `/etc/fstab` (persistent)",
        "`mount --type=nfs nfsserver/share /mnt` — wrong syntax (missing colon)",
        "`smbmount` — SMB, not NFS",
        "`nfsmount` — not a real command"
      ],
      "correct": 0,
      "explanation": "Temporary: mount -t nfs nfsserver:/share /mnt/nfs. Persistent /etc/fstab: nfsserver:/share /mnt/nfs nfs defaults,_netdev 0 0. _netdev: wait for network before mounting. nfsstat: show NFS stats. showmount -e nfsserver: list server exports. Install nfs-utils first: dnf install nfs-utils. add to /etc/fstab for persistence",
      "tags": [
        "networking",
        "nfs"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "rhcsa-040",
      "question": "What command kills a process by name?",
      "options": [
        "terminate processname",
        "kill -name processname",
        "stop processname",
        "killall processname or pkill processname"
      ],
      "correct": 3,
      "explanation": "killall httpd: send SIGTERM to all processes named httpd. pkill -f 'python script.py': match by full command line. kill PID: by process ID. kill -9 PID: SIGKILL (force, cannot be caught). kill -l: list signals. pkill -u username: kill all processes by user. SIGTERM (15): graceful. SIGKILL (9): force.",
      "tags": [
        "processes",
        "signals",
        "kill"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "rhcsa-041",
      "question": "How do you check and repair a filesystem before mounting?",
      "options": [
        "mkfs --check /dev/sdb1",
        "fsck /dev/sdb1 while mounted",
        "fsck -y /dev/sdb1 (unmounted only)",
        "tune2fs --repair /dev/sdb1"
      ],
      "correct": 2,
      "explanation": "CRITICAL: fsck only on unmounted filesystems. -y: auto-answer yes to all repair prompts. For root filesystem: run from rescue mode or use e2fsck (ext4). xfs_repair /dev/sdb1: for XFS. fsck on mounted FS will corrupt data. Boot to rescue: systemctl rescue. e2fsck -p: auto-fix safe issues.",
      "tags": [
        "filesystem",
        "fsck",
        "maintenance"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "rhcsa-042",
      "question": "What is the purpose of /etc/passwd vs /etc/shadow?",
      "options": [
        "/etc/passwd stores user info (world-readable);.",
        "They are identical files with redundant user data",
        "/etc/shadow is deprecated and unused in RHEL 8+",
        "Both files store plaintext passwords in different formats"
      ],
      "correct": 0,
      "explanation": "/etc/passwd format: username:x:UID:GID:comment:home:shell. /etc/shadow: username:HASH:lastchange:min:max:warn:inactive:expire. HASH format: $6$ = SHA-512. chage: modify shadow aging. pwck: check passwd consistency. unshadow: combine for cracking (John the Ripper). /etc/shadow stores hashed passwords (root-only)",
      "tags": [
        "users",
        "security",
        "passwd"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-043",
      "question": "How do you configure SSH key-based authentication?",
      "options": [
        "ssh-agent user@server --keyfile",
        "sshd --add-key user@server",
        "ssh-keygen -t rsa -b 4096;.",
        "ssh --generate-keys user@server"
      ],
      "correct": 2,
      "explanation": "Steps: (1) ssh-keygen -t ed25519 (modern) or -t rsa -b 4096. (2) ssh-copy-id user@server: copies ~/.ssh/id_*.pub to server's ~/.ssh/authorized_keys. (3) Test: ssh user@server (no password). Server: /etc/ssh/sshd_config PubkeyAuthentication yes (default). ~/.ssh permissions: 700 (dir), 600 (private key), 644 (pub key).",
      "tags": [
        "ssh",
        "authentication",
        "security"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-044",
      "question": "What does the 'stat' command show?",
      "options": [
        "Overall system CPU and memory statistics",
        "File metadata: inode, size.",
        "Network interface packet and error statistics",
        "Per-CPU usage statistics and load averages"
      ],
      "correct": 1,
      "explanation": "stat file: inode, size, device, permission (0644), links, UID, GID, atime (last access), mtime (last modification), ctime (last metadata change). stat -f /mnt: filesystem statistics. Use for: audit trails, inode investigation, checking when file was modified vs accessed. permissions, owner, and access/modify/change timestamps",
      "tags": [
        "filesystem",
        "stat",
        "metadata"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "rhcsa-045",
      "question": "How do you configure a local DNF/YUM repository?",
      "options": [
        "createrepo /path then dnf install",
        "localrepo --add /path",
        "cp packages to /var/cache/yum",
        "Create /etc/yum.repos.d/local.repo with baseurl=file:///path/to/repo; dnf clean all"
      ],
      "correct": 3,
      "explanation": "Create repo file: [local-repo] name=Local Repository baseurl=file:///mnt/iso/Packages enabled=1 gpgcheck=0. dnf clean all && dnf repolist. createrepo_c /path: create repo metadata from RPMs. Mount ISO: mount -o loop rhel.iso /mnt/iso. Useful for: air-gapped systems, offline patching.",
      "tags": [
        "dnf",
        "repositories",
        "offline"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "rhcsa-046",
      "question": "What is a firewalld zone and which zone is default?",
      "options": [
        "A single global rule set — only one zone exists at a time",
        "Zones apply only to virtual interfaces, not physical NICs",
        "A named trust level for network connections.",
        "The default zone is 'home' on all fresh RHEL installs"
      ],
      "correct": 2,
      "explanation": "Zones: trusted→public→home→work→dmz→drop (descending trust). firewall-cmd --get-default-zone. firewall-cmd --set-default-zone=trusted. firewall-cmd --zone=dmz --add-service=http. firewall-cmd --get-active-zones: zones + assigned interfaces. Per-zone rules apply to assigned interface's traffic. default zone is 'public'",
      "tags": [
        "firewalld",
        "zones",
        "networking"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-047",
      "question": "How do you boot into rescue mode to reset root password?",
      "options": [
        "systemctl set-password root",
        "Boot from USB only — no in-place recovery possible",
        "Interrupt GRUB → append 'rd.break' to kernel line → chroot /sysroot → passwd root",
        "grub2-rescue --reset-password"
      ],
      "correct": 2,
      "explanation": "Reset root password: (1) GRUB screen → 'e' to edit. (2) Find linux line → append 'rd.break' (interrupts before root mount). (3) mount -o remount,rw /sysroot. (4) chroot /sysroot. (5) passwd root. (6) touch /.autorelabel (SELinux relabel). (7) exit twice → reboot. SELinux relabel required or login fails.",
      "tags": [
        "grub",
        "recovery",
        "root-password"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "rhcsa-048",
      "question": "What is the difference between /etc/rc.local and systemd services?",
      "options": [
        "rc.local is a legacy boot script;.",
        "rc.local is newer and preferred over systemd unit files",
        "systemd cannot run shell scripts — only binary executables",
        "They are functionally identical — rc.local is just a systemd alias"
      ],
      "correct": 0,
      "explanation": "rc.local: /etc/rc.d/rc.local must be executable (chmod +x). Runs after all services. No error handling, no parallelism. Systemd service: create /etc/systemd/system/myscript.service [Service] ExecStart=/path/script [Install] WantedBy=multi-user.target. Enable: systemctl enable myscript. Better for production. systemd services offer dependency and recovery management",
      "tags": [
        "systemd",
        "boot",
        "services"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-049",
      "question": "How do you configure virtual hosted websites in Apache (httpd)?",
      "options": [
        "Edit /etc/httpd/conf/httpd.conf main block only",
        "Only one website per Apache instance is supported",
        "Use nginx instead — Apache does not support virtual hosting",
        "Create /etc/httpd/conf.d/mysite.conf with <VirtualHost *:80> block"
      ],
      "correct": 3,
      "explanation": "VirtualHost: <VirtualHost *:80> ServerName www.example.com DocumentRoot /var/www/example ErrorLog /var/log/httpd/example-error.log </VirtualHost>. Each site: separate .conf in /etc/httpd/conf.d/. httpd -t: syntax check. systemctl reload httpd: apply. SELinux: chcon -Rt httpd_sys_content_t /var/www/example or use restorecon.",
      "tags": [
        "httpd",
        "apache",
        "web-server"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "rhcsa-050",
      "question": "What does 'journalctl --vacuum-size=500M' do?",
      "options": [
        "Removes oldest journal entries until total storage is under 500MB",
        "Increases the maximum journal size limit to 500MB",
        "Backs up journal data to /var/log/journal-backup within 500MB",
        "Compresses journal files to fit within 500MB using gzip"
      ],
      "correct": 0,
      "explanation": "Journal management: journalctl --vacuum-size=500M (immediate cleanup). Persistent size limit: /etc/systemd/journald.conf → SystemMaxUse=500M → systemctl restart systemd-journald. journalctl --rotate: rotate log files. RuntimeMaxUse: volatile storage limit. Logs stored in /var/log/journal/ (persistent) or /run/log/journal/ (volatile).",
      "tags": [
        "journalctl",
        "logging",
        "storage"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "rhcsa-051",
      "question": "How do you create a Stratis storage pool and filesystem on RHEL 8+?",
      "options": [
        "stratis pool create mypool /dev/sdb;.",
        "lvcreate --stratis -n myfs mypool /dev/sdb",
        "stratisd --create mypool /dev/sdb; mkfs.stratis myfs",
        "pvcreate /dev/sdb; stratis pool attach mypool"
      ],
      "correct": 0,
      "explanation": "Stratis: thin-provisioned storage with snapshots. Install: dnf install stratisd stratis-cli; systemctl enable --now stratisd. Create pool: stratis pool create mypool /dev/sdb. Create filesystem: stratis filesystem create mypool myfs. Mount: mount /stratis/mypool/myfs /mnt. List: stratis pool list; stratis filesystem list.",
      "tags": [
        "stratis",
        "storage",
        "rhel8"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-052",
      "question": "What command creates a VDO (Virtual Data Optimizer) device on RHEL 8?",
      "options": [
        "mkvdo /dev/sdb --size 20G --name myvdo",
        "vdo create --name=myvdo --device=/dev/sdb --vdoLogicalSize=20G",
        "vdoadm create myvdo /dev/sdb 20G",
        "lvcreate --vdo -L 20G -n myvdo /dev/sdb"
      ],
      "correct": 1,
      "explanation": "VDO provides deduplication and compression. Install: dnf install vdo kmod-kvdo. Create: vdo create --name=myvdo --device=/dev/sdb --vdoLogicalSize=20G. Format: mkfs.xfs -K /dev/mapper/myvdo. Mount with discard: mount -o discard /dev/mapper/myvdo /mnt. vdostats --human-readable: check savings.",
      "tags": [
        "vdo",
        "storage",
        "rhel8"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "rhcsa-053",
      "question": "How do you run a container from an image using Podman on RHEL 8?",
      "options": [
        "podman run -d --name myapp -p 8080:80 nginx",
        "podman start nginx --port 8080:80 --detach",
        "container run -it nginx --name myapp",
        "docker run -d --name myapp -p 8080:80 nginx"
      ],
      "correct": 0,
      "explanation": "Podman: daemonless, rootless container engine. podman run -d: detached. --name: container name. -p host:container: port mapping. podman ps: list running containers. podman images: local images. podman pull registry.access.redhat.com/ubi8: pull image. podman stop/rm: stop and remove. No daemon required unlike Docker.",
      "tags": [
        "podman",
        "containers",
        "rhel8"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "rhcsa-054",
      "question": "How do you list available tuned profiles and apply one?",
      "options": [
        "tuned-adm list; tuned-adm profile throughput-performance",
        "systemctl list tuned-profiles; tuned-set throughput-performance",
        "tuned list; tuned apply throughput-performance",
        "tune2fs --list-profiles; tune2fs --apply throughput-performance"
      ],
      "correct": 0,
      "explanation": "tuned: dynamic tuning daemon. tuned-adm list: show all profiles. tuned-adm active: current profile. tuned-adm profile virtual-guest: apply profile. tuned-adm recommend: suggest profile for system type. Common profiles: balanced, throughput-performance, latency-performance, powersave, virtual-guest. systemctl enable --now tuned.",
      "tags": [
        "tuned",
        "performance",
        "optimization"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "rhcsa-055",
      "question": "How do you configure autofs to auto-mount an NFS share on access?",
      "options": [
        "mount -t autofs nfsserver:/share /mnt; enable in /etc/rc.local",
        "Edit /etc/fstab with auto,nfs options; systemctl restart autofs",
        "Add master entry to /etc/auto.master; create /etc/auto.nfs map file;.",
        "autofs --add nfsserver:/share /mnt/auto; systemctl reload autofs"
      ],
      "correct": 2,
      "explanation": "autofs: mounts filesystems on demand. /etc/auto.master: /mnt/nfs /etc/auto.nfs --timeout=60. /etc/auto.nfs: data -rw,soft nfsserver:/export/data. systemctl enable --now autofs. Access /mnt/nfs/data to trigger mount. Unmounts after timeout. Useful for large NFS environments — only mounts what is accessed. systemctl enable autofs",
      "tags": [
        "autofs",
        "nfs",
        "mounting"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-056",
      "question": "What command creates a LUKS-encrypted partition?",
      "options": [
        "cryptsetup luksFormat /dev/sdb1",
        "openssl enc -aes-256 /dev/sdb1",
        "cryptsetup encrypt --luks /dev/sdb1",
        "mkluks /dev/sdb1 --format"
      ],
      "correct": 0,
      "explanation": "LUKS encryption: cryptsetup luksFormat /dev/sdb1 (prompts for passphrase). Open: cryptsetup luksOpen /dev/sdb1 mydata → creates /dev/mapper/mydata. Format: mkfs.xfs /dev/mapper/mydata. Mount: mount /dev/mapper/mydata /mnt. Persistent: add to /etc/crypttab and /etc/fstab. cryptsetup luksDump: show header info.",
      "tags": [
        "luks",
        "encryption",
        "security"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "rhcsa-057",
      "question": "How do you create a network bond using nmcli?",
      "options": [
        "bonding --create bond0 eth0 eth1",
        "nmcli con add type bond ifname bond0;.",
        "nmcli bond create bond0 --slaves eth0,eth1",
        "ip link add bond0 type bond; ip link set eth0 master bond0"
      ],
      "correct": 1,
      "explanation": "Create bond: nmcli con add type bond ifname bond0 bond.options 'mode=active-backup'. Add slaves: nmcli con add type ethernet ifname eth0 master bond0; nmcli con add type ethernet ifname eth1 master bond0. Activate: nmcli con up bond0. cat /proc/net/bonding/bond0: check status. Modes: active-backup, balance-rr, 802.3ad (LACP). add slaves with nmcli ethernet con",
      "tags": [
        "networking",
        "bonding",
        "nmcli"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-058",
      "question": "What is Cockpit and how do you access it on RHEL?",
      "options": [
        "A network monitoring daemon — access via SNMP on port 161",
        "A command-line dashboard — run cockpit --start to launch in terminal",
        "A graphical desktop environment — installed with dnf install cockpit-desktop",
        "A web-based admin console."
      ],
      "correct": 3,
      "explanation": "Cockpit: browser-based system administration. Install: dnf install cockpit. Enable: systemctl enable --now cockpit.socket. Access: https://hostname:9090. Features: services, storage, networking, logs, terminal, updates, SELinux. Firewall: firewall-cmd --add-service=cockpit --permanent. Supports multi-server management from one UI. enable with systemctl enable --now cockpit.socket then browse port 9090",
      "tags": [
        "cockpit",
        "administration",
        "web-console"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "rhcsa-059",
      "question": "How do you list and enable a DNF application module stream?",
      "options": [
        "dnf appstream list; dnf appstream install nodejs:18",
        "dnf stream list nodejs; dnf stream enable nodejs:18",
        "dnf module list; dnf module enable nodejs:18;.",
        "subscription-manager repos --enable nodejs-18; dnf install nodejs"
      ],
      "correct": 2,
      "explanation": "DNF modules (RHEL 8 AppStream): dnf module list: see all modules and streams. dnf module list nodejs: check available streams. dnf module enable nodejs:18: switch to version 18 stream. dnf module install nodejs:18/common: install default profile. dnf module reset nodejs: reset to default. Allows multiple versions of software coexisting. dnf install nodejs",
      "tags": [
        "dnf",
        "modules",
        "appstream"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "rhcsa-060",
      "question": "How do you sync a local directory to a remote host using rsync?",
      "options": [
        "rsync --copy /local/dir remote:/remote/dir",
        "rsync -avz /local/dir/ user@remote:/remote/dir/",
        "ftp put /local/dir user@remote:/remote/dir",
        "scp -r /local/dir user@remote:/remote/dir"
      ],
      "correct": 1,
      "explanation": "rsync -avz: -a archive mode (preserves permissions, timestamps, symlinks), -v verbose, -z compress during transfer. Trailing slash on source copies contents only (not parent dir). --delete: remove files at destination not in source. --dry-run: preview without changes. rsync over SSH by default. --exclude='*.log': skip patterns. Efficient: only transfers changed blocks.",
      "tags": [
        "rsync",
        "file-transfer",
        "backup"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "rhcsa-multi-1",
      "question": "You added a new disk and created an XFS filesystem on /dev/sdb1. Which TWO steps are required to mount it persistently at /data after reboot?",
      "options": [
        "Add an entry for /dev/sdb1 (or its UUID) to /etc/fstab",
        "Run `systemctl daemon-reload` so systemd picks up the new fstab entry",
        "Reboot the system to confirm the mount unit fires correctly",
        "Run `mkdir /data` if the mount point doesn't exist"
      ],
      "correct": [
        0,
        3
      ],
      "explanation": "fstab plus an existing mount point are the two prerequisites. `daemon-reload` is needed if you write a *.mount unit by hand, not for fstab edits (systemd-fstab-generator picks them up on boot or `systemctl daemon-reload`). Rebooting is a verification step, not a requirement — `mount -a` confirms it works without a reboot.",
      "tags": [
        "filesystems",
        "storage"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    }
  ]
}
{
  "meta": {
    "id": "terraform-003",
    "name": "HashiCorp Terraform Associate 003",
    "vendor": "HashiCorp",
    "version": "1.0.3",
    "question_count": 161,
    "last_updated": "2026-05-13",
    "notes": " [bulk-tag pass: added `terraform-003` tag to 161 questions] [terraform batch 1: 14 scenario rewrites] [terraform batch 2: 16 scenario rewrites]"
  },
  "questions": [
    {
      "id": "tf-001",
      "question": "A platform team wants to define their AWS + Azure infrastructure DECLARATIVELY in version-controlled files — VMs, networks, RDS, IAM. They run a CLI that diffs the file-described state against the real cloud and applies the changes. The same tool works across multiple cloud + on-prem providers. Which describes Terraform?",
      "options": [
        "Container orchestration — wrong category (that's Kubernetes / Swarm)",
        "Infrastructure-as-Code tool that provisions + manages cloud + on-prem resources declaratively — define desired state in HCL, Terraform computes a plan + applies it; multi-cloud via providers",
        "Monitoring infrastructure — wrong category (Prometheus / Grafana / Datadog)",
        "Configuration management like Ansible — Ansible is IMPERATIVE config, Terraform is DECLARATIVE provisioning (different layer)"
      ],
      "correct": 1,
      "explanation": "Terraform: provision infrastructure (VMs, networks, databases, DNS) across multiple providers. Declarative: describe what you want, Terraform figures out how. Different from Ansible/Chef (configuration management) and Kubernetes (container orchestration).",
      "tags": [
        "fundamentals",
        "iac",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-002",
      "question": "A Terraform configuration manages AWS + Azure + Cloudflare + Datadog + PagerDuty resources. Each platform's API is exposed to Terraform via a plugin that translates Terraform resource declarations into provider API calls. The plugin is registered in the configuration and downloaded by `terraform init`. Which Terraform concept?",
      "options": [
        "Workspace — different concept (state isolation)",
        "Cloud vendor — too generic; providers ARE often cloud vendors but the term is more specific",
        "Provider — a plugin that implements resource types + data sources for a specific platform; translates Terraform resource declarations into API calls (`hashicorp/aws`, `azurerm`, `cloudflare`, etc.)",
        "Module — different concept (reusable group of resources)"
      ],
      "correct": 2,
      "explanation": "Providers: interface between Terraform and APIs. Each provider: plugin binary, resource types (aws_instance), data sources (aws_ami). Download automatically from Terraform Registry. Version-pinned in required_providers block. Published by HashiCorp, vendors, and community.",
      "tags": [
        "fundamentals",
        "providers",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-003",
      "question": "An engineer runs `terraform init` in a fresh clone of an infrastructure-as-code repo. Terraform does several things atomically. (1) Downloads provider plugins listed in the configuration. (2) Configures the backend (e.g., S3 + DynamoDB for remote state). (3) Initialises modules (downloads child modules from the registry / VCS). Which describes `terraform init`?",
      "options": [
        "Shows planned changes — that's `terraform plan`",
        "Creates infrastructure — that's `terraform apply`",
        "Applies configuration — that's `terraform apply`",
        "Initialises a working directory: downloads provider plugins, configures the backend (state store), initialises modules; must be run before plan / apply"
      ],
      "correct": 3,
      "explanation": "terraform init: always first command. Downloads: providers (to .terraform/providers), modules (to .terraform/modules). Backend init: configure remote state. Safe to run multiple times (idempotent). -upgrade flag: update providers to latest matching version. -backend=false: skip backend init.",
      "tags": [
        "cli",
        "init",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-004",
      "question": "A team wants to PREVIEW Terraform changes before applying them. The command builds an EXECUTION PLAN: reads current state, refreshes against the real infra, computes diff vs desired, shows each resource action (create / update / replace / destroy). Outputs `+` / `-` / `~` markers. Does NOT mutate infra. Which command?",
      "options": [
        "Applies immediately — `apply`",
        "Refreshes state only — `refresh` (now superseded by plan's auto-refresh)",
        "`terraform plan` — builds an execution plan; shows + / - / ~ per resource; does NOT mutate infra; can save to a file with `-out=tfplan` for `apply tfplan`",
        "Validates syntax only — `terraform validate`"
      ],
      "correct": 2,
      "explanation": "terraform plan: dry-run. Reads state, reads config, queries providers for current resource state, computes diff. Output: +create, -destroy, ~update in-place, -/+replace. Save plan: -out=plan.tfplan. Apply saved plan: terraform apply plan.tfplan. Use in CI/CD for review before applying.",
      "tags": [
        "cli",
        "plan",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-005",
      "question": "Terraform tracks the mapping between configuration resources and real-world infrastructure IDs in a STATE file (default `terraform.tfstate`). Without state, Terraform couldn't (a) know what's already created, (b) detect drift, (c) plan deletions. Best practice: store state REMOTELY (S3, Terraform Cloud) with locking + encryption. Which describes state?",
      "options": [
        "Just a log file — false",
        "JSON file mapping config resources → real-world IDs + attributes; enables drift detection + planning; SHOULD be stored remotely with locking (S3 + DynamoDB, Terraform Cloud, etc.) for team use",
        "Provider cache — that's `.terraform/`",
        "Config backup — different concept"
      ],
      "correct": 1,
      "explanation": "State: Terraform's source of truth. Contains: resource IDs (aws_instance.web → i-1234567890), dependencies, provider metadata, module structure. Remote state (S3+DynamoDB, Terraform Cloud) enables team collaboration and locking. Never manually edit state — use terraform state commands.",
      "tags": [
        "state",
        "fundamentals",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-006",
      "question": "Two related Terraform files: (A) DECLARES input variables — their type, default, description, validation, sensitivity. (B) ASSIGNS values to those variables (or `*.auto.tfvars` files auto-load). The pair lets configurations be reusable across environments. Which mapping?",
      "options": [
        "`variables.tf` DECLARES variables (type, default, description, validation, sensitive); `terraform.tfvars` ASSIGNS values; `*.auto.tfvars` files auto-loaded; CLI flags `-var-file` / `-var` override",
        "variables.tf assigns — reversed",
        "Same file different names — false",
        "tfvars only for secrets — false; tfvars assigns ANY variable values; secrets should be marked `sensitive = true` and ideally come from `TF_VAR_*` env vars or Vault, not tfvars"
      ],
      "correct": 0,
      "explanation": "Variables: declared in variables.tf (or any .tf file): variable 'region' { type=string, default='us-east-1' }. Values in: terraform.tfvars (auto-loaded), *.auto.tfvars (auto-loaded), -var flag, TF_VAR_name env var, interactive prompt. .tfvars not committed to git if contains secrets. descriptions, defaults, and validation rules. terraform.tfvars: assigns actual values to those variables. Separation of concerns: declare structure separately from values. .tfvars files can be environment-specific (dev.tfvars, prod.tfvars)",
      "tags": [
        "variables",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "tf-007",
      "question": "A team has 5 VPCs with identical 'web-tier + app-tier + DB-tier' patterns. Copying the resource blocks across 5 directories is unmaintainable. They package the pattern as a REUSABLE GROUP of resources with INPUT variables (VPC name, CIDR) and OUTPUTS (subnet IDs), versioned in a registry. Other configs call `module \"web_tier\" { source = … version = … }`. Which Terraform concept?",
      "options": [
        "Provider plugin — different layer (Terraform-to-platform API)",
        "Workspace — different concept (state isolation)",
        "Module — reusable self-contained group of resources with input variables + outputs; published via the Terraform Registry / git / S3; called via `module \"name\" { source = … version = … }`",
        "Single resource block — too narrow"
      ],
      "correct": 2,
      "explanation": "Modules: package reusable configurations. Call module: module 'vpc' { source='terraform-aws-modules/vpc/aws', version='3.0' }. Inputs: module variables. Outputs: module outputs accessible in root as module.vpc.vpc_id. Local modules: relative paths. Benefits: DRY principle, tested patterns, team standardization.",
      "tags": [
        "modules",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-008",
      "question": "A Terraform configuration needs to REFERENCE an existing AWS VPC (created outside Terraform or by a separate config). It must read the VPC's id + cidr_block without managing it (no creates / deletes). Terraform supports this via `data \"aws_vpc\" \"shared\" { filter { … } }`. Which concept?",
      "options": [
        "Resource import — different (`terraform import` brings into management)",
        "Variable type — different concept",
        "Data source — READS info from existing infrastructure or external systems without managing it; Terraform queries the provider's API at plan / apply time; results referenced as `data.<type>.<name>.<attr>`",
        "Provider config — different concept"
      ],
      "correct": 2,
      "explanation": "Data sources: data 'aws_ami' 'ubuntu' { filter { name='name', values=['ubuntu-20.04-*'] } }. Returns attributes of existing resources. Use: reference existing VPC, latest AMI, existing DNS zone. Different from resource (which manages lifecycle). Cross-state: terraform_remote_state data source reads outputs from another state file.",
      "tags": [
        "data-sources",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-009",
      "question": "`terraform destroy` deletes EVERY resource managed by the current configuration. It is IRREVERSIBLE for most resources (DBs, instances). Best-practice safer alternatives: (a) `terraform plan -destroy` to PREVIEW first, (b) `lifecycle { prevent_destroy = true }` on critical resources, (c) terraform state `rm` to UN-manage without deleting. Which describes destroy + alternatives?",
      "options": [
        "Manual confirmation always — Terraform DOES prompt (`-auto-approve` to skip)",
        "Removes state file only — false; it deletes real infra",
        "Destroys all resources managed by the current config (real cloud infrastructure, irreversible for stateful resources); safer alternatives: `terraform plan -destroy` for preview, `prevent_destroy` lifecycle, `terraform state rm` to un-manage without deleting",
        "Only shows what would be destroyed — that's `terraform plan -destroy`"
      ],
      "correct": 2,
      "explanation": "terraform destroy: removes all resources in state. Asks confirmation. Useful for: dev environments, test cleanup, cost savings. Risky: production environments. Safer approach: use lifecycle { prevent_destroy = true } on critical resources, use -target for selective destruction, review plan output carefully before confirming.",
      "tags": [
        "cli",
        "destroy",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-010",
      "question": "A Terraform configuration references `source = \"hashicorp/aws\"` for the AWS provider AND `source = \"terraform-aws-modules/vpc/aws\"` for a public VPC module. Both are downloaded from a public HashiCorp-hosted catalogue at `registry.terraform.io`. Providers come from `hashicorp/*` (official) or third-party namespaces; modules can be published by anyone. Which describes the Terraform Registry?",
      "options": [
        "License server — wrong concept",
        "Local module repository — Terraform supports local + git + S3 sources too, but the REGISTRY refers to the public catalogue",
        "Public repository at `registry.terraform.io` of Terraform providers + modules; providers (official by HashiCorp, verified third-party, community) + modules (anyone can publish); private registry available via Terraform Cloud / Enterprise",
        "State storage — different service (Terraform Cloud / S3)"
      ],
      "correct": 2,
      "explanation": "Terraform Registry: discovery hub. Provider source: hashicorp/aws → registry.terraform.io/providers/hashicorp/aws. Module source: terraform-aws-modules/vpc/aws → shorthand for registry module. Verified providers: blue checkmark. Usage stats visible. Browse documentation, inputs/outputs, submodules. Also: private registry in Terraform Cloud/Enterprise.",
      "tags": [
        "registry",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-011",
      "question": "Terraform configurations are written in a DECLARATIVE, JSON-compatible-but-human-readable language. It supports: blocks (`resource \"aws_instance\" \"web\" { … }`), expressions, functions (`length()`, `toset()`), conditionals (`a ? b : c`), iteration (`for_each`, `for`), references (`aws_vpc.main.id`). Which language?",
      "options": [
        "Programming language — too vague; HCL is declarative",
        "YAML variant — false; HCL is its own syntax",
        "Markup language like XML — false; HCL is a configuration language",
        "HCL (HashiCorp Configuration Language) — declarative, human-readable, JSON-interchangeable; supports expressions, functions, conditionals, iteration, references"
      ],
      "correct": 3,
      "explanation": "HCL: designed for infrastructure. Key features: interpolation (${var.name}), expressions, built-in functions (formatdate, cidrsubnet), loops (count, for_each), conditionals (condition ? true : false), heredoc strings, comments (# or //). Terraform files: .tf (HCL or JSON), .tfvars (variable values).",
      "tags": [
        "hcl",
        "fundamentals",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-012",
      "question": "Two ways to create MULTIPLE instances of a resource in Terraform. (A) `count = 3` — creates 3 identical instances indexed by [0], [1], [2]. Removing from the MIDDLE renumbers and DESTROYS-RECREATES instances. (B) `for_each = toset([\"a\",\"b\",\"c\"])` — creates 3 instances keyed by string; removing a specific key removes ONLY that instance. Which describes count vs for_each?",
      "options": [
        "count = N indexed by [0..N-1] (removing from middle destroys-recreates the renumbered tail); for_each = keyed by map/set (removing a specific key only destroys that instance; safer for changing collections)",
        "count deprecated — false",
        "Identical — false; different identity model",
        "for_each maps only — false; works on sets + maps (must be hashable)"
      ],
      "correct": 0,
      "explanation": "count: simple when creating identical resources. Issues: remove index 0 of 3 → destroys index 0, updates index 1 to 0, updates index 2 to 1. for_each with map: remove key 'web' → only removes web instance. Use for_each with toset() for sets. for_each cannot use unknown values at plan time (count sometimes can).",
      "tags": [
        "meta-arguments",
        "count-vs-for-each",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-013",
      "question": "Terraform automatically writes the PREVIOUS state to a sibling file before each state-mutating operation. If a state save is corrupted or accidentally rolled back, the previous version can be restored. The file name uses a `.backup` suffix. Which file?",
      "options": [
        "Provider download cache — different (`.terraform/`)",
        "`terraform.tfstate.backup` — auto-written by Terraform before each state modification; contains the PREVIOUS state; one level of auto-backup; restore by copying back over `terraform.tfstate`",
        "Config backup — different concept",
        "Manual backup — false; it's automatic, not user-initiated"
      ],
      "correct": 1,
      "explanation": "tfstate.backup: created before terraform apply modifies state. Contains state from before this apply. One-level safety net. For teams: use remote state with versioning (S3 with versioning enabled, Terraform Cloud). terraform state commands modify state — always backup first with: terraform state pull > backup.tfstate.",
      "tags": [
        "state",
        "backup",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-014",
      "question": "An AWS S3 bucket was created in the AWS console MANUALLY before the team started using Terraform. Now they want Terraform to MANAGE it without recreating it. The command WRITES the existing resource's state into Terraform's state file under the specified address. The configuration block must still be written by hand (Terraform 1.5+ adds `import` blocks for the config too). Which command?",
      "options": [
        "Creates a new resource — wrong; import doesn't create",
        "`terraform import aws_s3_bucket.legacy my-existing-bucket` — writes the existing AWS resource into Terraform state under the address `aws_s3_bucket.legacy`; you still must write the resource block in HCL (Terraform 1.5+: `import` blocks can be used in code)",
        "Exports state — wrong direction (that's `terraform show`)",
        "Downloads a module — wrong concept"
      ],
      "correct": 1,
      "explanation": "Import: terraform import aws_instance.web i-1234567890. After import: resource exists in state. Must manually write matching resource config (future: terraform import with config generation in v1.5+). Use for: adopting existing infrastructure, migrating from manual to IaC. Verify: terraform plan should show no changes after import + config writing.",
      "tags": [
        "cli",
        "import",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-015",
      "question": "A platform team has 5 engineers all running `terraform apply` against the same configuration. With LOCAL state, two simultaneous applies corrupt the state file. The fix: store state in a SHARED REMOTE BACKEND (S3 + DynamoDB lock, or Terraform Cloud, etc.) with encryption + access control. Which describes remote state + its value?",
      "options": [
        "Requires Terraform Enterprise — false; open-source backends include S3, GCS, Azure, Consul, etc.",
        "CI/CD only — false; any team should use remote state",
        "Local state with git — anti-pattern; state contains secrets and can't be safely git-committed",
        "Store `terraform.tfstate` in a shared backend (S3, GCS, Azure Blob, Consul, Terraform Cloud) with locking + encryption + access control; required for safe team use"
      ],
      "correct": 3,
      "explanation": "Remote state backends: S3+DynamoDB (AWS), Azure Blob + Table, GCS, Terraform Cloud, Consul. DynamoDB: state locking (prevents concurrent applies). S3 versioning: state history. terraform_remote_state: read outputs from another state. Configure: terraform { backend 'S3' { bucket, key, region, dynamodb_table } }. Azure Storage, GCS, Terraform Cloud) instead of locally — enables: team collaboration (all members access same state), state locking (prevent concurrent applies), state versioning, access control, and cross-workspace state sharing via remote state data source",
      "tags": [
        "state",
        "remote-backend",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "tf-016",
      "question": "Before commit, the CI pipeline runs `terraform validate`. This command does NOT touch the cloud or compute a plan. It only checks: HCL syntax is valid; resource types + provider attributes are recognised; required arguments are present; references resolve. Faster than `plan`. Which describes `terraform validate`?",
      "options": [
        "Creates a plan — that's `terraform plan`",
        "Downloads providers — that's `terraform init`",
        "Applies configuration — that's `terraform apply`",
        "Validates HCL syntax + resource type names + required arguments + reference resolution; does NOT contact providers / cloud; ideal in CI pre-commit / pre-push hooks"
      ],
      "correct": 3,
      "explanation": "terraform validate: syntax and semantic validation. Does NOT: contact providers, check credentials, verify resource configuration validity against provider API (e.g., invalid region name passes validate). Use in CI/CD for fast syntax checking before more expensive plan. Prereq: must run init first (providers must be downloaded).",
      "tags": [
        "cli",
        "validate",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-017",
      "question": "A Terraform configuration creates a VPC and a downstream config (deploying ECS) needs the VPC ID. The producing configuration declares `output \"vpc_id\" { value = aws_vpc.main.id }`. The consuming config reads it via `terraform_remote_state` data source. Which describes outputs?",
      "options": [
        "A log message — false",
        "State file export — partial truth; outputs ARE part of state, but the concept is broader",
        "Monitoring metric — wrong category",
        "Outputs expose values from the configuration: shown to the user after apply; available to other configurations via `terraform_remote_state` / Terraform Cloud workspace outputs; useful for module composition + cross-config dependencies"
      ],
      "correct": 3,
      "explanation": "Outputs: expose values from configuration. Uses: display IP addresses after deploy, pass values between modules (child module outputs accessible as module.child.output_name), share data between workspaces via terraform_remote_state. Mark sensitive: output 'password' { value=..., sensitive=true } — redacted in CLI output but visible in state.",
      "tags": [
        "outputs",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-018",
      "question": "A `resource \"aws_db_instance\"` block declares `lifecycle { prevent_destroy = true; create_before_destroy = true; ignore_changes = [tags] }`. The block governs HOW Terraform handles the lifecycle of the resource: whether to recreate before destroying (zero-downtime), prevent destruction, ignore drift on specific attributes. Which meta-argument?",
      "options": [
        "State management — wrong concept",
        "Resource scheduling — wrong concept",
        "Provider configuration — wrong concept",
        "`lifecycle` meta-argument — controls resource lifecycle: `create_before_destroy` (create replacement before destroying old, zero-downtime), `prevent_destroy` (refuse destroy in plan), `ignore_changes` (don't detect drift on specified attrs), `replace_triggered_by`"
      ],
      "correct": 3,
      "explanation": "lifecycle block: inside resource. create_before_destroy: avoids downtime for stateful resources. prevent_destroy = true: prevents accidental deletion (must set to false to destroy). ignore_changes: [tags, user_data] — don't update resource when these change in config. replace_triggered_by: [aws_instance.other] — replace this resource when other changes.",
      "tags": [
        "meta-arguments",
        "lifecycle",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-019",
      "question": "A resource needs to be RECREATED on the next apply (e.g., a corrupted EC2 instance). Two approaches: (A) `terraform taint <addr>` — marks the resource for recreation; deprecated in 0.15.4+. (B) `terraform apply -replace=<addr>` — modern equivalent; doesn't mutate state until apply. Which describes the difference?",
      "options": [
        "Taint newer — reversed",
        "Same — false",
        "`terraform taint` (deprecated since 0.15.4) marked a resource as tainted in state, forcing recreation on next apply; the modern replacement is `terraform apply -replace=<address>` which is non-mutating in plan and only acts on apply",
        "`-replace` only with count — false"
      ],
      "correct": 2,
      "explanation": "terraform taint aws_instance.web: marks in state, next apply destroys+recreates. Deprecated in v0.15+. Modern: terraform apply -replace=aws_instance.web — same effect, but integrated into plan/apply workflow. Shows in plan as '-/+' replace. Use for: corrupted instances, force new AMI, rotate certificates embedded in resource.",
      "tags": [
        "cli",
        "taint-replace",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-020",
      "question": "HashiCorp Terraform Cloud is a managed SaaS that provides BEYOND open-source Terraform: (1) shared remote state with locking, (2) remote plan + apply execution (so secrets stay in the platform), (3) policy as code (Sentinel / OPA), (4) cost estimation, (5) VCS integration (auto-plan on PR, apply on merge), (6) team-based RBAC + audit, (7) private module registry. Which describes Terraform Cloud?",
      "options": [
        "Just hosting — too narrow",
        "Managed Terraform service providing remote state + locking, remote run execution, policy-as-code (Sentinel/OPA), cost estimation, VCS-driven workflows, team RBAC + audit, private module registry; free tier for small teams",
        "Enterprise only — false; free tier exists",
        "Monitoring platform — wrong category"
      ],
      "correct": 1,
      "explanation": "Terraform Cloud (free tier available): remote state, remote execution (runs in HashiCorp infrastructure), VCS integration (GitHub/GitLab), cost estimation, private module registry. Teams: per-workspace access control, review and approval workflows. Terraform Enterprise: self-hosted version for air-gapped environments or compliance requirements.",
      "tags": [
        "terraform-cloud",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-021",
      "question": "A SINGLE Terraform configuration must manage `dev`, `staging`, and `prod` environments. Each environment needs its own state file. Terraform supports this via NAMED instances of state within the same configuration; switching with `terraform workspace select <name>`. Each workspace has its own state but they share configuration code. Which concept?",
      "options": [
        "Named instances of state within a single configuration — each workspace has its OWN state file; useful for similar-but-separate environments; access current name via `terraform.workspace`; NOT a strong env separation (still shares the same backend + code)",
        "VCS branch — different concept",
        "Separate Terraform installation — different concept",
        "Module container — different concept"
      ],
      "correct": 0,
      "explanation": "Workspaces: terraform workspace new prod, terraform workspace select prod, terraform workspace list. State stored separately per workspace. Access current workspace in config: terraform.workspace. Limitations: same backend config for all workspaces. For complex multi-env: consider separate directories with shared modules instead.",
      "tags": [
        "workspaces",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-022",
      "question": "Two engineers `terraform apply` simultaneously against the SAME remote state. Without protection their applies could race and corrupt state. The backend supports a LOCK — first apply acquires it (via DynamoDB lease, GCS atomic write, etc.), the second apply WAITS or fails. Lock released on apply completion. Which describes state locking?",
      "options": [
        "Team workspaces only — false; any backend supporting locks",
        "Permanent — false; auto-released",
        "Terraform Cloud only — false; S3+DynamoDB, GCS, Azure, Consul all support locking",
        "Triggered automatically on any state-mutating operation (plan, apply, refresh, destroy); first acquirer wins; backend-dependent (DynamoDB, GCS native locking, etc.); released on completion; `-lock=false` overrides (dangerous); `terraform force-unlock <ID>` to clear stuck locks"
      ],
      "correct": 3,
      "explanation": "State locking: supported by most remote backends (S3+DynamoDB, Azure Blob, GCS, Terraform Cloud). Local backend: no locking. Lock timeout: -lock-timeout=10m. If lock not released (crash): force-unlock with ID from error message. Concurrent applies: second apply waits or fails depending on backend configuration. destroy, state operations), Terraform acquires a lock — prevents concurrent operations from corrupting state. With DynamoDB backend: creates lock entry. Lock released after operation completes. Force unlock: terraform force-unlock LOCK_ID (use with caution)",
      "tags": [
        "state",
        "locking",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "tf-023",
      "question": "Terraform AUTOMATICALLY infers dependencies between resources from attribute references (`aws_security_group.web.id` → SG must be created first). But sometimes a dependency is REAL but not reflected in any attribute reference (e.g., IAM policy must exist before a Lambda even though Lambda doesn't reference the policy directly). The meta-argument `depends_on = [aws_iam_policy.lambda_policy]` makes the dependency explicit. Which describes it?",
      "options": [
        "Parallel — opposite",
        "Module order — too narrow",
        "Explicitly declares that one resource (or module / data source) depends on another — Terraform handles implicit dependencies from attribute references; `depends_on` is for cases where the dependency exists but isn't visible in references",
        "Implicit only — false"
      ],
      "correct": 2,
      "explanation": "depends_on: use when dependency isn't captured by references. Example: EC2 instance that needs IAM instance profile to be fully propagated before it launches (even though it references the profile, the IAM propagation delay isn't captured). Module-level depends_on: all resources in module depend on specified resources. Use sparingly — hurts parallelism.",
      "tags": [
        "meta-arguments",
        "depends-on",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-024",
      "question": "A `terraform` block declares `required_providers { aws = { source = \"hashicorp/aws\"; version = \"~> 5.0\" } }`. The version constraint uses operators: `=` exact, `!=` exclude, `>`, `>=`, `<`, `<=`, `~>` (pessimistic — major.minor pinned, patch free). Which describes Terraform provider version constraints?",
      "options": [
        "Auto-selected — false; constraints must be specified for reproducibility",
        "Major only — false; all of major / minor / patch can be constrained",
        "No pinning — false",
        "Constraint operators: `=` exact, `!=` exclude, `>` `>=` `<` `<=` comparison, `~>` pessimistic (major.minor pinned, patch may float). Use `~> 5.0` to allow 5.x but not 6.x; lockfile (`.terraform.lock.hcl`) records the actual version selected"
      ],
      "correct": 3,
      "explanation": "Version constraints: ~>3.0 (>=3.0, <4.0 — allow minor but not major). ~>3.2.0 (>=3.2.0, <3.3.0 — allow patch only). Best practice: pin major version (~>3.0), allow minor updates. Lock file: .terraform.lock.hcl records exact version used — commit to VCS for reproducibility. terraform init -upgrade: update to latest matching.",
      "tags": [
        "providers",
        "version-constraints",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-025",
      "question": "The `terraform state` subcommand family provides operations on the state file: list resources, show attributes, move (rename) resources, remove a resource from state, pull (download from remote), push (overwrite remote). Used for refactoring + recovery from broken state. Which describes it?",
      "options": [
        "Backup — wrong concept",
        "Replaces plan — wrong concept",
        "Only show — too narrow",
        "Family of state operations: `state list`, `state show <addr>`, `state mv <from> <to>` (rename / move), `state rm <addr>` (un-manage without deleting), `state pull` (download from remote backend), `state push` (overwrite remote, DANGEROUS), `state replace-provider`"
      ],
      "correct": 3,
      "explanation": "State commands: state list (all resources), state show aws_instance.web (full attributes), state mv aws_instance.old aws_instance.new (rename without destroying), state rm aws_instance.web (stop managing, resource persists), state pull (download remote state), state push (upload local state to remote — dangerous). Use carefully, always backup first.",
      "tags": [
        "cli",
        "state-subcommand",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-026",
      "question": "A Terraform config needs to RUN A LOCAL COMMAND that has no real-cloud counterpart (e.g., generate a TLS key, run a script that mutates external systems, force a re-apply trigger). The `null_resource` is a resource that does NOTHING in the cloud but supports `triggers` (to detect changes) and provisioners (`local-exec`, `remote-exec`). It's a common escape hatch. Which describes it?",
      "options": [
        "Empty module — wrong concept",
        "`null_resource` — Terraform resource that creates no real infrastructure but supports provisioners + triggers; common for running local-exec / remote-exec hooks; Terraform 1.4+ deprecates much of this in favour of `terraform_data` resource",
        "Placeholder — partial; null_resource has specific behaviour",
        "Deprecated — partially; `terraform_data` is the modern replacement for many use cases, but null_resource is still supported"
      ],
      "correct": 1,
      "explanation": "null_resource: resource with no real infrastructure. Common use: run a script after db creation (remote-exec), invalidate CloudFront cache, run database migrations. triggers = { cluster_instance_ids = join(',', aws_instance.cluster[*].id) } — re-runs provisioner when instances change. terraform_data in Terraform 1.4+ is the modern replacement.",
      "tags": [
        "resources",
        "null-resource",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-027",
      "question": "Terraform's PROVISIONERS execute commands during resource lifecycle. Two flavours: `local-exec` runs on the machine RUNNING Terraform (your laptop / CI runner). `remote-exec` opens an SSH (or WinRM) connection to a TARGET host (often a new VM) and runs commands inside. Both should be a LAST RESORT — prefer cloud-init / Packer / config-management. Which describes the difference?",
      "options": [
        "`local-exec` runs on the machine running Terraform; `remote-exec` opens SSH / WinRM to a target host (often the newly-created VM) and runs commands inside; both are provisioners and considered last-resort by HashiCorp",
        "remote-exec more secure — context-dependent",
        "local-exec needs SSH — false; runs locally",
        "Same — false"
      ],
      "correct": 0,
      "explanation": "Provisioners: use only when no better alternative. local-exec: run local commands (Ansible playbook, shell script). remote-exec: configure the resource after creation. Problems: if provisioner fails, resource is tainted. Retries are complex. HashiCorp recommends: use user_data (EC2), custom images, or separate CM tool. If you must: use connection block for remote-exec (SSH key, user, host).",
      "tags": [
        "provisioners",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-028",
      "question": "Two ways Terraform learns dependencies between resources. (A) IMPLICIT — when one resource references another's attribute (`subnet_id = aws_subnet.web.id`), Terraform sees the reference and builds a dependency edge automatically. (B) EXPLICIT — `depends_on = [resource_address]` for cases where the dependency is REAL but not reflected in any attribute reference. Which mapping?",
      "options": [
        "Implicit: auto-detected from attribute references between resources; Explicit: declared via `depends_on` meta-argument when a dependency is real but isn't reflected in any reference (e.g., IAM policy needs to exist before Lambda, but Lambda doesn't reference the policy)",
        "Implicit less reliable — false; implicit is reliable when it applies",
        "No difference — false",
        "Always need explicit — false; rarely needed"
      ],
      "correct": 0,
      "explanation": "Implicit dependencies: most common and preferred. resource B uses aws_vpc.main.id → B depends on main VPC. Terraform builds dependency graph, creates resources in correct order, destroys in reverse order. Explicit: when operational dependency isn't reflected in configuration references (IAM propagation, data plane readiness). Explicit depends_on forces sequential processing — reduces parallelism.",
      "tags": [
        "dependencies",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-029",
      "question": "A `terraform { backend \"s3\" { bucket = … key = … } }` block defines WHERE Terraform stores state and (optionally) WHERE plan / apply RUN. Default is `local` (state in `terraform.tfstate`). Remote backends include `s3`, `azurerm`, `gcs`, `consul`, `kubernetes`, `remote` (Terraform Cloud / Enterprise). Which describes a backend?",
      "options": [
        "Cloud provider API — that's the provider",
        "Module registry — different concept",
        "Provider API endpoint — different concept",
        "Backend = where Terraform stores state and (with `remote` / Terraform Cloud) where plan + apply execute; configured in the `terraform { backend \"<name>\" { … } }` block; default is `local`; remote backends provide locking + collaboration"
      ],
      "correct": 3,
      "explanation": "Backends: local backend (default — state in working directory). Remote backends: store state centrally. Enhanced backends (Terraform Cloud): also run operations remotely. Partial configuration: sensitive backend config (credentials) via env vars or -backend-config file (not committed to VCS). Migration: terraform init -migrate-state copies state to new backend. for some backends, where operations (plan/apply) run. Types: local (default, .tfstate file), remote (Terraform Cloud), S3, Azure Blob, GCS, Consul, HTTP. Backend configuration in terraform block. Switching backends: terraform init -reconfigure or -migrate-state",
      "tags": [
        "state",
        "backends",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true,
      "rewritten": true
    },
    {
      "id": "tf-030",
      "question": "A team refactors their Terraform code: moving an `aws_instance.web` resource into a `module.web_tier`. Without intervention, `terraform plan` shows DESTROY + CREATE. Adding a `moved { from = aws_instance.web; to = module.web_tier.aws_instance.web }` block tells Terraform: 'this is the SAME resource, just renamed'. State is updated in place — no real destruction. Which feature?",
      "options": [
        "Workspace migration — different concept",
        "Generic resource migration — too vague",
        "Moves files between directories — wrong concept",
        "`moved` block (Terraform 1.1+) — declares that a resource has been renamed / refactored; Terraform updates state in place on apply (no destroy + create); supersedes manual `terraform state mv`"
      ],
      "correct": 3,
      "explanation": "moved block (Terraform 1.1+): moved { from = aws_instance.web, to = module.web.aws_instance.main }. Apply: Terraform updates state (renames resource) without API calls. Use when: extracting resources into modules, renaming for consistency, reorganizing code. No API changes made — purely state manipulation. Include moved blocks in code, remove after all users have applied.",
      "tags": [
        "refactoring",
        "moved-block",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "rewritten": true
    },
    {
      "id": "tf-031",
      "question": "What is 'terraform fmt'?",
      "options": [
        "Validates configuration",
        "Creates documentation",
        "Formats Terraform configuration files to canonical style: consistent indentation, alignment of equals signs, newline between blocks. Run in CI to enforce style. -check flag: exit with error if files aren't formatted (don't modify). -recursive: format all .tf files in subdirectories",
        "Generates module scaffolding"
      ],
      "correct": 2,
      "explanation": "terraform fmt: style enforcer. Benefits: readable diffs (no whitespace noise), consistent codebase, automated in pre-commit hooks. Run: terraform fmt -recursive . (format current dir + subdirs). -check mode: CI/CD validation (fails if unformatted). -diff: show what would change. IDEs: Terraform extensions run fmt on save.",
      "tags": [
        "cli",
        "formatting",
        "workflow",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-032",
      "question": "What is the Terraform .terraform.lock.hcl file?",
      "options": [
        "A debug log file",
        "Local configuration override",
        "A state cache file",
        "Dependency lock file: records exact provider versions and checksums used — ensures consistent provider versions across machines and over time. Commit to VCS. Update: terraform init -upgrade recalculates lock file. Contains: version, constraints, hashes (for integrity verification)"
      ],
      "correct": 3,
      "explanation": "Lock file: introduced in Terraform 0.14. Records: provider version, constraints, checksums (sha256 hashes for all platforms). Commit to version control — ensures all team members and CI use identical provider versions. terraform init -upgrade: updates to latest matching versions, regenerates lock file. Never manually edit.",
      "tags": [
        "versions",
        "lock-file",
        "reproducibility",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-033",
      "question": "What are Terraform built-in functions and give examples?",
      "options": [
        "Functions require provider plugins",
        "Only arithmetic functions",
        "Terraform has no functions",
        "Built-in functions for transforming values: string (format, replace, lower, upper, trimspace, split, join), numeric (max, min, ceil, floor), collection (length, lookup, merge, concat, flatten, toset, tolist, tomap), filesystem (file, filebase64), encoding (base64encode, jsonencode)"
      ],
      "correct": 3,
      "explanation": "Functions: called in expressions. Examples: cidrsubnet(var.vpc_cidr, 8, 0) (subnet calculation), format('Name: %s', var.name), lookup(var.ami_ids, var.region, 'default'), merge(var.default_tags, var.resource_tags), jsonencode({key='value'}), file('${path.module}/config.yaml'), toset([...]). No user-defined functions in HCL (use locals for complex expressions).",
      "tags": [
        "functions",
        "hcl",
        "expressions",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-034",
      "question": "What is a Terraform local value?",
      "options": [
        "A local variable type",
        "A local file reference",
        "Named expression results that can be referenced multiple times in a configuration — avoids repeating complex expressions. Defined in locals block, referenced as local.name. Used for: computed values, DRY principle, readable intermediate values",
        "A local module path"
      ],
      "correct": 2,
      "explanation": "Locals: locals { common_tags = merge(var.tags, { env = var.environment }), full_name = '${var.project}-${var.environment}' }. Reference: resource 'aws_instance' 'web' { tags = local.common_tags }. Benefits: compute once (not re-evaluated), named expressions improve readability, avoid repetition of complex logic.",
      "tags": [
        "locals",
        "configuration",
        "hcl",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-035",
      "question": "What is 'terraform console'?",
      "options": [
        "A debugging tool for errors only",
        "A provider configuration tool",
        "A web UI",
        "Interactive REPL for evaluating Terraform expressions — test functions, check variable values, validate expressions without running apply. Reads current state. Examples: run cidrsubnet(\"10.0.0.0/16\", 8, 1), lookup(var.region_amis, \"us-east-1\"), merge(var.tags, {\"env\":\"dev\"})"
      ],
      "correct": 3,
      "explanation": "terraform console: interactive eval. Type expressions, see results immediately. Useful for: testing complex expressions before using in config, understanding function behavior, exploring resource attributes from state. Loads state (terraform.tfstate or remote). Type 'exit' or Ctrl+D to quit. No changes to infrastructure or state.",
      "tags": [
        "cli",
        "debugging",
        "expressions",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-036",
      "question": "What is dynamic block in Terraform?",
      "options": [
        "Generates repeated nested blocks based on a collection — replaces manually duplicating nested blocks. Used when a resource needs variable number of nested blocks (security group rules, dynamic ingress rules). Iterates over collection, creates one block per element",
        "A runtime configuration block",
        "A loop for resources",
        "A conditional resource creation"
      ],
      "correct": 0,
      "explanation": "dynamic block: for nested blocks (not top-level resources — use count/for_each for those). resource 'aws_security_group' 'sg' { dynamic 'ingress' { for_each = var.ingress_rules; content { from_port = ingress.value.from_port, to_port = ingress.value.to_port, protocol = ingress.value.protocol } } }. iterator: optional alias for the current element.",
      "tags": [
        "dynamic-blocks",
        "hcl",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-037",
      "question": "What is the difference between Terraform resource and data source in configuration?",
      "options": [
        "Data sources create resources",
        "Resource: manages full lifecycle (create,.",
        "Resources are read-only",
        "Identical syntax"
      ],
      "correct": 1,
      "explanation": "Resource: resource 'aws_instance' 'web' { ami = ... } → Terraform creates, updates, and destroys this. Data: data 'aws_ami' 'ubuntu' { ... filter ... } → queries existing AMIs, returns attributes, no management. Data sources: always read on plan, can use existing values. data.aws_ami.ubuntu.id references the result. update, destroy) — changes are applied to infrastructure. Data source: read-only query of existing infrastructure — returns information without managing lifecycle. Resource: 'resource TYPE NAME', Data: 'data TYPE NAME'",
      "tags": [
        "resources",
        "data-sources",
        "fundamentals",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-038",
      "question": "What is Terraform's 'sensitive' variable and output attribute?",
      "options": [
        "Marks values as sensitive: Terraform redacts them from console output and plan display (shows as '(sensitive value)'). Values still stored in plaintext in state. For secrets: use Vault provider or environment variables instead of tfvars. Output sensitive = true: still in state but not shown",
        "Encrypts values",
        "Only hides from logs",
        "Encrypts state file"
      ],
      "correct": 0,
      "explanation": "sensitive = true: prevents display in CLI. Variable: variable 'db_password' { type = string, sensitive = true }. Output: output 'password' { value = random_password.db.result, sensitive = true }. WARNING: values still in state — if state compromised, secrets exposed. Best practice: use Vault secrets provider, SSM Parameter Store, or don't put secrets in Terraform at all.",
      "tags": [
        "sensitive",
        "security",
        "variables",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-039",
      "question": "What is terraform graph?",
      "options": [
        "A billing report",
        "A performance metric",
        "Generates a visual representation of the dependency graph in DOT format — shows relationships between resources, data sources, and modules. Pipe to Graphviz (dot -Tsvg terraform.dot > graph.svg) for visual. Useful for understanding complex configurations",
        "A state visualization"
      ],
      "correct": 2,
      "explanation": "terraform graph: outputs DOT language. Install Graphviz: brew install graphviz. Generate: terraform graph | dot -Tpng > graph.png. Shows: resource dependencies (edges), resource types (nodes), modules (subgraphs). Use for: understanding large configurations, finding circular dependencies, documentation.",
      "tags": [
        "cli",
        "visualization",
        "debugging",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-040",
      "question": "What are Terraform's variable validation rules?",
      "options": [
        "Only string validation",
        "Only type checking",
        "Custom validation conditions in variable definitions — validate block with condition (boolean expression) and error_message. Runs during terraform validate and plan. Examples: validate CIDR format, ensure string length, check value in allowed set",
        "Validation requires paid version"
      ],
      "correct": 2,
      "explanation": "Validation: variable 'environment' { validation { condition = contains(['dev','staging','prod'], var.environment), error_message = 'Must be dev, staging, or prod.' } }. Condition: any expression returning bool. Can use regexall, length, can function. Multiple validation blocks allowed. Replaces error-prone runtime failures with clear validation messages.",
      "tags": [
        "validation",
        "variables",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-041",
      "question": "What is 'terraform refresh' and when is it useful?",
      "options": [
        "Restarts infrastructure",
        "Recreates resources",
        "Updates state to match actual infrastructure without making any configuration changes — reconciles state with real world (e.g., someone manually changed something). In Terraform 0.15+: refresh now happens automatically before plan. Standalone refresh: rarely needed",
        "Downloads new provider versions"
      ],
      "correct": 2,
      "explanation": "terraform refresh (deprecated in v1.0 as standalone): updates state with real resource attributes. Now embedded in plan/apply via -refresh=true (default). terraform plan -refresh-only: shows what would be changed in state without changing infrastructure. Use for: state drift detection, after manual infrastructure changes, understanding current state.",
      "tags": [
        "cli",
        "state",
        "operations",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-042",
      "question": "What is 'terraform output' command?",
      "options": [
        "Displays output values from the current state — shows values without running apply. terraform output (all outputs), terraform output vpc_id (specific), terraform output -json (JSON format for scripting), terraform output -raw (raw value without formatting)",
        "Exports state to CSV",
        "Runs terraform apply",
        "Shows resource details"
      ],
      "correct": 0,
      "explanation": "terraform output: read output values. Use cases: scripts that consume Terraform outputs (get DB endpoint, IP addresses), CI/CD pipelines, cross-stack integration. -json: machine-readable. -raw: single string value without quotes (pipe to clipboard or script). Reads from remote state if remote backend configured.",
      "tags": [
        "cli",
        "outputs",
        "workflow",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-043",
      "question": "What is the purpose of the required_version constraint?",
      "options": [
        "Specifies minimum/maximum Terraform CLI version required to use this configuration — prevents running with incompatible versions that might behave differently. Define in terraform block: required_version = '>=1.0, <2.0'",
        "Sets workspace version",
        "Downloads specific Terraform version",
        "Pins provider versions"
      ],
      "correct": 0,
      "explanation": "required_version: ensures compatibility. terraform { required_version = '>=1.3.0' }. If running older version: error message with requirement. Use in modules: ensure callers use compatible versions. Version constraints: same syntax as provider versions. CI/CD: use specific Terraform version matching required_version.",
      "tags": [
        "versions",
        "configuration",
        "compatibility",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-044",
      "question": "What is 'terraform apply -auto-approve'?",
      "options": [
        "A more thorough apply",
        "Skips the interactive approval prompt and applies changes immediately — use in CI/CD pipelines where interactive input isn't available. Risky: no human review of plan. Best practice: run plan separately, review plan output, then apply with -auto-approve or from saved plan file",
        "Skips resource validation",
        "Approves without planning"
      ],
      "correct": 1,
      "explanation": "Auto-approve: terraform apply -auto-approve. In CI: terraform plan -out=tfplan && terraform apply tfplan (apply saved plan — safer, exact plan is applied). Or: terraform apply -auto-approve with prior plan review in separate step. Never use -auto-approve for production without prior plan review. Combine with -var-file and -target as needed.",
      "tags": [
        "cli",
        "automation",
        "workflow",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-045",
      "question": "What is the 'locals' block used for vs 'variables'?",
      "options": [
        "Locals replace variables",
        "Variables are computed internally",
        "Identical purpose",
        "Variables: input values from outside the configuration (user, tfvars, env vars). Locals: internal computed values within configuration — cannot be set by users. Locals: DRY principle, computed expressions, complex transformations. Variables: user customization points"
      ],
      "correct": 3,
      "explanation": "Distinction: variables are configuration inputs (users provide values). Locals are internal computations (users can't override). locals { name = '${var.project}-${var.env}', tags = merge(var.tags, {terraform='true'}) }. Use locals for: complex expressions used multiple times, computed values derived from inputs. Use variables for: environment-specific values, secrets, configurables.",
      "tags": [
        "locals",
        "variables",
        "design-patterns",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-046",
      "question": "What is Terraform's role in a GitOps workflow?",
      "options": [
        "Manual approval not possible in GitOps",
        "Infrastructure code in git (source of truth) → VCS webhook triggers CI/CD → terraform plan output reviewed in PR → merge triggers terraform apply. GitOps for IaC: branch per environment, PR-based change review, automated applies, rollback via git revert",
        "GitOps is only for Kubernetes",
        "Terraform doesn't support GitOps"
      ],
      "correct": 1,
      "explanation": "Terraform + GitOps: IaC stored in git. Atlantis: popular open-source GitOps tool for Terraform (plan on PR, apply on merge). Terraform Cloud: built-in VCS integration. GitHub Actions: terraform plan in PR checks, apply on push to main. Benefits: audit trail (git log), peer review (PR), rollback (git revert + apply).",
      "tags": [
        "gitops",
        "workflow",
        "collaboration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-047",
      "question": "What is the 'templatefile' function in Terraform?",
      "options": [
        "Generates Terraform configuration",
        "Creates a template resource",
        "Only for JSON templates",
        "Reads a template file and renders it with variable substitutions — templates use ${var} or %{ if ... } / %{ for ... } syntax. More readable than inline heredoc. Used for: user_data scripts, config files, complex string generation"
      ],
      "correct": 3,
      "explanation": "templatefile: templatefile('${path.module}/user_data.sh', { db_endpoint = aws_db_instance.main.endpoint, app_version = var.app_version }). Template syntax: ${var_name} for substitution, %{ if condition }...%{ endif }, %{ for item in list }${item}%{ endfor }. File must be valid template syntax. Cleaner than inline heredoc for complex templates.",
      "tags": [
        "functions",
        "templates",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-048",
      "question": "What is Terraform's 'path.module' and 'path.root'?",
      "options": [
        "Only for Windows paths",
        "path.module: filesystem path of the module currently being processed — use for: loading files relative to module, templatefile, file functions. path.root: filesystem path of root module. path.cwd: current working directory. Essential for portable modules that reference local files",
        "Filesystem paths in your OS",
        "External URL references"
      ],
      "correct": 1,
      "explanation": "path references: path.module (e.g., /modules/vpc — current module location), path.root (/main — root module), path.cwd (terraform working directory). Use: file('${path.module}/policies/policy.json') — works correctly regardless of where module is called from. Without path.module: file path is relative to working directory — breaks when module is reused.",
      "tags": [
        "paths",
        "modules",
        "functions",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-049",
      "question": "What is Terraform Sentinel policy?",
      "options": [
        "A security scanner",
        "HashiCorp Sentinel: policy-as-code framework for Terraform Enterprise/Cloud — define governance policies that run between plan and apply. Examples: enforce tagging, restrict expensive instance types, require security group rules. Block: policy violations prevent apply",
        "A monitoring policy",
        "An open-source tool"
      ],
      "correct": 1,
      "explanation": "Sentinel: policy engine. Write policies in Sentinel language (Python-like). Policies: soft-mandatory (can override), hard-mandatory (cannot override), advisory (warn only). Checks: resource types, costs, module sources, attribute values. Use: enforce organizational standards, compliance requirements. Requires Terraform Enterprise or Terraform Cloud Plus tier.",
      "tags": [
        "sentinel",
        "policy",
        "governance",
        "enterprise",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-050",
      "question": "What is the 'try' function in Terraform?",
      "options": [
        "Evaluates a sequence of expressions and returns the result of the first one that doesn't produce an error — handles situations where an attribute may or may not exist. try(resource.optional_attr, 'default') — returns 'default' if optional_attr produces an error",
        "A programming construct",
        "Error handling for provisioners",
        "A conditional statement"
      ],
      "correct": 0,
      "explanation": "try(expr1, expr2, ...): evaluates left-to-right, returns first non-error result. Use for: optional attributes that may not exist on older resources, handling null values, defensive programming. Example: try(module.database.endpoint, 'localhost'). can() function: returns bool — can(expression) returns true if expression doesn't error. Both useful for optional/conditional configurations.",
      "tags": [
        "functions",
        "error-handling",
        "hcl",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-051",
      "question": "What are Terraform provider aliases?",
      "options": [
        "Version aliases",
        "Alternate provider names",
        "Allow using same provider type multiple times with different configurations in the same module — example: deploy to multiple AWS regions simultaneously. Provider with alias: provider 'aws' { alias='us-east', region='us-east-1' }. Resource uses: provider = aws.us-east",
        "Optional provider names"
      ],
      "correct": 2,
      "explanation": "Provider aliases: default provider (no alias), aliased providers (explicit alias). Resources use aliased provider: resource 'aws_instance' 'replica' { provider = aws.us-west }. Use cases: multi-region deployments, different accounts (dev/prod), different organizations. Modules inherit provider from root unless configured with providers argument.",
      "tags": [
        "providers",
        "multi-region",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-052",
      "question": "What is Terraform's 'for' expression?",
      "options": [
        "A loop for resources",
        "A function for iteration",
        "A list comprehension that transforms or filters collections — [for s in var.list : upper(s)] creates list. {for k, v in var.map : k => upper(v)} creates map. Can add conditional filter: [for s in var.list : s if s != 'exclude']. Used in locals, variable defaults, resource attributes",
        "Same as for_each"
      ],
      "correct": 2,
      "explanation": "for expressions: transform data. List: [for ip in aws_instance.servers[*].private_ip : ip]. Map: {for s in aws_s3_bucket.buckets : s.id => s.arn}. Filter: [for s in var.subnets : s if s.type == 'private']. String: '%{for s in list}${s}%{endfor}'. Powerful for reshaping data structures.",
      "tags": [
        "expressions",
        "hcl",
        "for-expression",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-053",
      "question": "What is the 'one' function in Terraform?",
      "options": [
        "Takes a list with 0 or 1 elements and returns either null (empty list) or the single element — avoids errors when referencing element of conditional resource. one(aws_instance.optional) where instance may or may not exist",
        "A mathematical function",
        "Returns first element of list",
        "Returns minimum value"
      ],
      "correct": 0,
      "explanation": "one(): one([]) = null, one(['value']) = 'value', one(['a','b']) = error (more than 1). Use when count-based resource creates 0 or 1 instances: one(aws_eip.nat[*].public_ip) — gets IP if NAT exists, null if count=0. Replaces index 0 access which errors if empty. Also: flatten, toset, tolist for collection manipulation.",
      "tags": [
        "functions",
        "collections",
        "conditional",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-054",
      "question": "What is Terraform's backend 'partial configuration'?",
      "options": [
        "An incomplete backend setup",
        "Separating sensitive backend configuration from the main backend block — store non-sensitive values in terraform block (bucket name, region, key), pass sensitive values (credentials) via -backend-config flag or separate file (not committed to VCS). Prevents secrets in git",
        "A development backend mode",
        "A broken backend setup"
      ],
      "correct": 1,
      "explanation": "Partial backend config: terraform { backend 'S3' { bucket='my-bucket', key='prod/terraform.tfstate', region='us-east-1' } }. Credentials via env vars (AWS_ACCESS_KEY_ID) or -backend-config='profile=myprofile'. Or: terraform init -backend-config=backend.conf (file with sensitive values, gitignored). Principle: infrastructure code in git, credentials in secure store.",
      "tags": [
        "backends",
        "security",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-055",
      "question": "What are Terraform module input/output best practices?",
      "options": [
        "No documentation needed",
        "Module inputs: type all variables,.",
        "Outputs should expose everything",
        "Pass all variables always"
      ],
      "correct": 1,
      "explanation": "Best practices: (1) Type all variables (string, number, bool, list, map, object). (2) Provide descriptions (visible in terraform-docs). (3) Set defaults for optional variables. (4) Validate expected values. (5) Outputs: expose IDs and ARNs callers need. (6) README: usage examples. (7) versions.tf: required_providers and required_version. (8) CHANGELOG for updates. provide descriptions, validate inputs, use sensible defaults. Module outputs: expose only what callers need (not everything), use sensitive attribute for secrets. Module structure: variables.tf, main.tf, outputs.tf, versions.tf, README.md",
      "tags": [
        "modules",
        "best-practices",
        "design",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-056",
      "question": "What is 'terraform state mv' used for?",
      "options": [
        "Migrates state to new backend",
        "Moves resource to different region",
        "Moves tfstate file to new location",
        "Moves a resource within state."
      ],
      "correct": 3,
      "explanation": "state mv: terraform state mv aws_instance.web module.web.aws_instance.main. Renames in state only, no API calls. Prerequisite: update configuration to use new name/path, then terraform state mv to match. After mv: terraform plan should show 0 changes. Alternative: moved block (declarative approach in 1.1+). changes the address Terraform uses to track it. Use cases: rename resource in code (avoid destroy+recreate), move resource into a module, adopt imported resources. Run before applying code changes that rename resources",
      "tags": [
        "state",
        "refactoring",
        "operations",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-057",
      "question": "What is the 'nonsensitive' function?",
      "options": [
        "Makes variables public",
        "Forces output display",
        "Removes encryption",
        "Explicitly marks a value as non-sensitive."
      ],
      "correct": 3,
      "explanation": "nonsensitive(): override sensitivity marking. Use when: need to use sensitive value in expression that doesn't support sensitive values. Example: length(nonsensitive(var.secret_list)). Risk: could accidentally expose sensitive value in plan output. Only use when absolutely necessary and the specific use doesn't expose the sensitive content. allows using a sensitive value in a context that would otherwise error (like a resource attribute count). Use carefully: only when you're sure the value can be safely displayed",
      "tags": [
        "sensitive",
        "functions",
        "security",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-058",
      "question": "What is 'terraform show' command?",
      "options": [
        "Lists all resources",
        "Shows provider documentation",
        "Shows human-readable output of state file or saved plan — terraform show (current state), terraform show plan.tfplan (saved plan). Shows all resource attributes. Useful for: reviewing state, verifying what apply did, reading plan before applying",
        "Shows variable values"
      ],
      "correct": 2,
      "explanation": "terraform show: human-readable state. terraform show -json (machine-readable). terraform show plan.tfplan (show saved plan). terraform show -json plan.tfplan | jq (parse plan with jq). Use in CI: terraform plan -out=tfplan && terraform show -json tfplan | policy-check-script. Shows full resource attributes including computed values.",
      "tags": [
        "cli",
        "state",
        "operations",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-059",
      "question": "What is 'terraform providers lock' command?",
      "options": [
        "Prevents provider updates",
        "Disables provider auto-update",
        "Adds/updates provider checksums in .terraform.lock.hcl — specify platforms to lock for (enables using lock file on different OS/arch than where init was run). terraform providers lock -platform=linux_amd64 -platform=darwin_arm64",
        "Locks Terraform version"
      ],
      "correct": 2,
      "explanation": "terraform providers lock: update lock file with checksums for specified platforms. Use when: team uses Mac (arm64) + CI uses Linux (amd64) — run providers lock for both platforms so lock file works everywhere. Also: providers mirror (cache providers), providers schema (show schema), providers list (show configured providers).",
      "tags": [
        "providers",
        "lock-file",
        "compatibility",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-060",
      "question": "What is Terraform's 'check' block?",
      "options": [
        "Validates syntax only",
        "A testing framework",
        "Creates monitoring alerts",
        "New in Terraform 1.5: defines custom assertions about infrastructure state that generate warnings (not errors) during plan/apply. Different from variable validation (which blocks apply) — check blocks report warnings but don't block. Use for: post-deploy validation, health checks, compliance monitoring"
      ],
      "correct": 3,
      "explanation": "check block: assert { condition = ..., error_message = '...' }. Warning (not error) if condition fails. Use for: 'this S3 bucket should have versioning enabled', 'this URL should return 200'. Combine with data sources to check external state. Alternative to TestInfra/InSpec for lightweight assertions without separate tool.",
      "tags": [
        "testing",
        "validation",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-061",
      "question": "What is the terraform.io module registry module source format?",
      "options": [
        "<NAMESPACE>/<MODULE>/<PROVIDER>.",
        "Only official modules supported",
        "Full GitHub URL required",
        "Requires local paths only"
      ],
      "correct": 0,
      "explanation": "Module sources: (1) Registry: 'terraform-aws-modules/vpc/aws' with optional version. (2) GitHub: 'github.com/org/repo' or 'git::https://...'. (3) S3: 's3::https://bucket/module.zip'. (4) Local: './modules/vpc'. (5) Terraform Cloud private registry: 'app.terraform.io/ORG/vpc/aws'. Version pinning: always use version for registry modules in production. example: terraform-aws-modules/vpc/aws (namespace=terraform-aws-modules, module name=vpc, provider=aws). Version: version='~>3.0'. Download: terraform init downloads from registry automatically",
      "tags": [
        "modules",
        "registry",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-062",
      "question": "What is Terraform's 'object' variable type?",
      "options": [
        "A resource reference",
        "Same as map",
        "A JSON object only",
        "Structured type with named attributes of specific types — object({ name=string, age=number, tags=map(string) }). More strict than map (all keys same type). Supports optional attributes. Use when variable has mixed-type structured data"
      ],
      "correct": 3,
      "explanation": "object type: variable 'config' { type=object({ instance_type=string, instance_count=number, enable_monitoring=bool }) }. Access: var.config.instance_type. Nested: object with object. optional(TYPE, DEFAULT): mark attribute optional with default value (Terraform 1.3+). More readable than multiple separate variables for related configuration.",
      "tags": [
        "variables",
        "types",
        "hcl",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-063",
      "question": "What is Terraform's 'any' type constraint?",
      "options": [
        "Special type constraint that accepts any value — Terraform infers the type from the actual value provided. Use when: module should accept flexible input types, creating general-purpose modules. Reduces type safety — prefer explicit types when possible",
        "Disables type checking",
        "An object type",
        "A list type"
      ],
      "correct": 0,
      "explanation": "any: variable 'config' { type=any }. Accepts any type — list, map, string, object, etc. Use sparingly: loses type checking benefits. Useful for: pass-through variables that just forward to another module, complex dynamic structures where type varies. Prefer specific types with optional() for structured optional fields.",
      "tags": [
        "variables",
        "types",
        "flexibility",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-064",
      "question": "What is Terraform's 'precondition' and 'postcondition'?",
      "options": [
        "Configuration validation only",
        "Same as variable validation",
        "Only for modules",
        "precondition: validates resource inputs before creating — fails plan if condition is false. postcondition: validates resource outputs after creating — fails if condition is false after apply. Both: in lifecycle block. Example: ensure AMI is in right region before creating instance"
      ],
      "correct": 3,
      "explanation": "lifecycle { precondition { condition = var.environment == 'prod' || var.instance_type != 't3.nano', error_message = 'Production requires larger than t3.nano' } }. postcondition: check after creation — validate that what was created meets requirements. Better than variable validation for resource-context checks. Terraform 1.2+.",
      "tags": [
        "validation",
        "lifecycle",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-065",
      "question": "What is 'terraform test' framework?",
      "options": [
        "Validates syntax only",
        "A CI/CD pipeline feature",
        "Third-party testing tool",
        "Built-in testing framework (Terraform 1.6+) for writing unit and integration tests for Terraform modules — .tftest.hcl files with run blocks. Spins up real infrastructure (or uses mock providers), runs assertions, destroys after. Replaces external tools like Terratest for simple cases"
      ],
      "correct": 3,
      "explanation": "terraform test: test files (.tftest.hcl). run { command=apply, variables={env='test'}, assert { condition=output.vpc_id != '', error_message='VPC must be created' } }. Mock providers: test without real cloud calls. Real infrastructure: applies, runs assertions, destroys. More lightweight than Terratest for simple module validation.",
      "tags": [
        "testing",
        "advanced",
        "workflow",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-066",
      "question": "What are Terraform's resource 'timeouts' block?",
      "options": [
        "Retry configuration",
        "Session timeout settings",
        "Network timeout settings",
        "Configures maximum time to wait for resource operations — timeouts { create='10m', update='10m', delete='20m' }. If operation exceeds timeout, Terraform errors (may leave resource in unknown state). Useful for slow-provisioning resources (large ECS services, RDS instances)"
      ],
      "correct": 3,
      "explanation": "timeouts: resource-specific. Support varies by resource and provider. Example: aws_db_instance has create/update/delete timeouts. Increase for: slow databases, large clusters, complex network provisioning. Default timeouts set by provider — may need increase in slow environments. Timeout doesn't cancel the operation, just stops waiting.",
      "tags": [
        "timeouts",
        "resources",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-067",
      "question": "What is the difference between 'terraform apply' and 'terraform apply -target'?",
      "options": [
        "Target is always faster",
        "Target is safer for production",
        "terraform apply: applies all planned changes to bring configuration to desired state. terraform apply -target=RESOURCE: applies changes only to specified resource (and its dependencies) — partial apply. Risk: leaves configuration in inconsistent state. Use only for testing or emergencies, not standard workflow",
        "Identical behavior"
      ],
      "correct": 2,
      "explanation": "-target: scoped apply. Use cases: testing specific resource change, emergency fix of single resource, recovering from partial failure. Risks: creates state drift (other resources not updated), implicit dependencies might not be considered. Plan with -target first: terraform plan -target=aws_instance.web. Remove -target when issue resolved.",
      "tags": [
        "cli",
        "operations",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-068",
      "question": "What is Terraform's 'yamldecode' function?",
      "options": [
        "Parses YAML string and converts to Terraform value (maps, lists, strings, numbers) — useful for reading YAML config files, parsing Kubernetes manifests, processing YAML secrets. Complement: yamlencode (convert to YAML string)",
        "Requires YAML provider",
        "A deprecated function",
        "Only for providers"
      ],
      "correct": 0,
      "explanation": "yamldecode: local config = yamldecode(file('${path.module}/config.yaml')). Converts YAML → Terraform types (map, list, string, number, bool). Use for: externalizing configuration in YAML (may be more readable than HCL for complex data), reading K8s manifests, consuming YAML APIs. jsonencode/jsondecode: JSON equivalent.",
      "tags": [
        "functions",
        "yaml",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-069",
      "question": "What is Terraform's 'replace_triggered_by' lifecycle argument?",
      "options": [
        "Forces resource replacement when specified resources or resource attributes change — even if the resource itself hasn't changed. Useful when: resource depends on external state not captured in configuration (IAM role content, certificate content)",
        "A depends_on variant",
        "Triggers module replacement",
        "Prevents replacement"
      ],
      "correct": 0,
      "explanation": "lifecycle { replace_triggered_by = [aws_iam_role.instance_role, aws_key_pair.deployer.key_name] }. Resource replaced (destroy+create) when any listed resource changes. Use: when resource is tightly coupled to another but doesn't reference it (IAM role for EC2 — if role changes, recreate instance to pick up new permissions). Terraform 1.2+.",
      "tags": [
        "lifecycle",
        "dependencies",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-070",
      "question": "What is the Terraform 'can' function?",
      "options": [
        "Checks variable types",
        "Checks permissions",
        "Validates IAM policies",
        "Returns true if an expression can be evaluated without error, false if it produces an error — complement to try(). Use for conditionals: can(resource.optional_attr) ? resource.optional_attr : 'default'. Useful for: optional resource attributes, conditional logic based on resource existence"
      ],
      "correct": 3,
      "explanation": "can(): can(data.aws_eks_cluster.main.endpoint) returns true if cluster exists and has endpoint, false if expression errors. Use in ternary: can(module.optional_component.id) ? module.optional_component.id : '' — safely get optional module output. Used with try() for safe attribute access patterns.",
      "tags": [
        "functions",
        "conditional",
        "hcl",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-071",
      "question": "What is 'terraform workspace select' and multi-workspace patterns?",
      "options": [
        "Creates a new workspace",
        "Switches between workspaces: terraform workspace select prod. Pattern: same configuration, different state per workspace. Variable overrides: terraform.workspace in config. Anti-pattern: using single workspace with -var-file instead is often cleaner for complex environments",
        "Selects a provider",
        "Imports a workspace"
      ],
      "correct": 1,
      "explanation": "Workspaces: terraform workspace new dev, terraform workspace select prod. terraform.workspace: current workspace name. locals { env_config = { dev={size='t3.micro'}, prod={size='m5.large'} }[terraform.workspace] }. Workspace per environment: simple, same code. Separate directories: more isolation, better for very different environments.",
      "tags": [
        "workspaces",
        "environments",
        "workflow",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-072",
      "question": "What is Terraform's 'override' file mechanism?",
      "options": [
        "Overrides module sources",
        "Files ending in _override.tf or named override.tf are merged last — override resource/module configurations without modifying original files. Use cases: local development overrides, testing, merging generated configurations with manual ones. Avoid in modules — confusing",
        "Overrides provider behavior",
        "Overrides variable values"
      ],
      "correct": 1,
      "explanation": "Override files: processed after regular .tf files, merge with them. Use for: temporary dev overrides (don't commit), automated code generation merging with manual config, test fixtures. Rarely needed in practice. terraform.tfvars and workspace approach preferred for environment differences. Include in .gitignore if dev-only overrides.",
      "tags": [
        "configuration",
        "advanced",
        "override",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-073",
      "question": "What is the Terraform 'toset' function?",
      "options": [
        "Creates a new set resource",
        "Sorts a list",
        "Converts to sorted list",
        "Converts a value to a set type."
      ],
      "correct": 3,
      "explanation": "toset: for_each requires set or map (not list). toset(var.region_list): converts list to set (removes duplicates, enables for_each). for_each = toset(['us-east-1','us-west-2']): creates resource instance per region. tolist: convert set to list. tomap: convert to map type. Type conversion functions essential for for_each patterns. removes duplicates, loses ordering. Use with for_each (requires set or map, not list): for_each = toset(var.availability_zones). Deduplication: toset(['a','b','a']) = {'a','b'}",
      "tags": [
        "functions",
        "for_each",
        "collections",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-074",
      "question": "What is Terraform Cloud's 'run' workflow?",
      "options": [
        "Runs only on weekdays",
        "Same as local CLI",
        "Requires HashiCorp Vault",
        "When using Terraform Cloud as backend: CLI triggers run in Terraform Cloud, plan executed remotely in secure environment, output streamed to CLI. VCS workflow: push to VCS → webhook → automatic plan. Apply: manually confirm or auto-apply on merge. State and credentials stored in Terraform Cloud (not local machine)"
      ],
      "correct": 3,
      "explanation": "Terraform Cloud runs: (1) CLI-driven: local terraform apply → run in TC → output streamed back. (2) VCS-driven: push to connected repo → automatic plan → manual/auto apply. (3) API-driven: trigger via API. Run queue: sequential per workspace. Logs: stored in TC for 6 months. Notifications: Slack, PagerDuty, email on state changes.",
      "tags": [
        "terraform-cloud",
        "workflow",
        "collaboration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-075",
      "question": "What is Terraform's 'import' block (v1.5+)?",
      "options": [
        "New declarative import approach.",
        "Only for specific providers",
        "Same as terraform import command",
        "Only in Terraform Cloud"
      ],
      "correct": 0,
      "explanation": "Import block (v1.5+): import { to = aws_s3_bucket.existing, id = 'my-bucket-name' }. Run terraform plan: shows import action + generated resource config (with -generate-config-out). Run terraform apply: imports resources. Benefits: declarative, reviewable in PR, no separate command needed. Still generates config that needs refinement. define import block in configuration, run terraform plan to see generated config, apply imports resource. import { to = aws_instance.web, id = 'i-1234567890' }. Generates configuration (experimental -generate-config-out flag). Replaces imperative terraform import command",
      "tags": [
        "import",
        "advanced",
        "v1.5",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-076",
      "question": "What is 'terraform providers mirror'?",
      "options": [
        "Creates a provider backup",
        "Clones a provider repository",
        "Downloads provider plugins to a local directory to create an air-gapped mirror — use in restricted networks where Terraform can't access registry.terraform.io. Then configure network_mirror or filesystem_mirror in CLI config for offline installs",
        "Duplicates provider configuration"
      ],
      "correct": 2,
      "explanation": "terraform providers mirror /path/to/mirror: downloads providers to local directory. Configure in ~/.terraformrc or %APPDATA%/terraform.rc: provider_installation { filesystem_mirror { path='/path/to/mirror', include=['registry.terraform.io/*/*'] }, direct { exclude=['registry.terraform.io/*/*'] } }. For air-gapped environments, regulated networks, or offline CI/CD.",
      "tags": [
        "providers",
        "offline",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-077",
      "question": "What are Terraform expressions and their types?",
      "options": [
        "Only function calls",
        "Expressions include: literal values (string,.",
        "No expressions in HCL",
        "Only literals supported"
      ],
      "correct": 1,
      "explanation": "Expression types: (1) Literals: 'string', 42, true, null. (2) References: var.name, local.x, resource.type.name.attr, data.type.name.attr, module.name.output. (3) Splat: aws_instance.web[*].id → list of all IDs. (4) For: [for v in list : upper(v)]. (5) Conditional: var.env == 'prod' ? 'large' : 'small'. (6) Functions: format('%s-%s', ...). number, bool, null), references (var.name, local.name, resource.type.name.attr), function calls (format(), merge()), operators (arithmetic, comparison, logical), template strings, for expressions, splat expressions (resource[*].id), conditionals (a ? b : c)",
      "tags": [
        "expressions",
        "hcl",
        "fundamentals",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-078",
      "question": "What is the difference between Terraform Open Source, Cloud, and Enterprise?",
      "options": [
        "Enterprise is just bigger Cloud",
        "OSS includes all Cloud features",
        "Only pricing differs",
        "OSS: free, local operations."
      ],
      "correct": 3,
      "explanation": "OSS: no cost, local terraform CLI, state in remote backend, team workflow via VCS. Terraform Cloud Free: 500 resource hours/month, 1 user, remote execution, state storage. Plus/Business: Sentinel, SSO, audit logs, more resources. Enterprise: self-hosted (runs in your data center), FIPS compliance, air-gapped, dedicated support, clustering for HA. basic collaboration via git+remote state. Cloud: free tier + paid tiers, remote execution, VCS integration, team access, Sentinel policies (paid), cost estimation, private registry. Enterprise: self-hosted Terraform Cloud, air-gapped, SAML SSO, audit logging, clustering, additional compliance features",
      "tags": [
        "licensing",
        "comparison",
        "products",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-079",
      "question": "What is 'terraform state pull' and 'terraform state push'?",
      "options": [
        "Downloads provider plugins",
        "state pull: downloads remote state to stdout.",
        "Pulls/pushes modules from registry",
        "Syncs local and remote state automatically"
      ],
      "correct": 1,
      "explanation": "state pull: terraform state pull > state-backup.json. Good for: backing up state before risky operations, reading state programmatically. state push: terraform state push state-backup.json — overwrites remote state. Dangerous: only use for: recovering from corrupted state, migrating state manually. Lock: pull/push respect state locking. Better alternative: use backend migration (terraform init -migrate-state). use for: inspection, backup, migration. state push: uploads local state to remote backend — use carefully, can overwrite remote state. Force push: -force flag if serial number mismatch",
      "tags": [
        "state",
        "cli",
        "operations",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-080",
      "question": "What is Terraform's 'cidrsubnet' function?",
      "options": [
        "Calculates a subnet address within a VPC CIDR block — cidrsubnet(prefix, newbits, netnum). prefix: parent CIDR. newbits: bits to add for subnets. netnum: subnet number (0-based). Example: cidrsubnet('10.0.0.0/16', 8, 1) = '10.0.1.0/24'",
        "A routing function",
        "Validates CIDR notation",
        "A VPC creation function"
      ],
      "correct": 0,
      "explanation": "cidrsubnet: essential for VPC design in Terraform. cidrsubnet('10.0.0.0/8', 8, 0) = '10.0.0.0/16'. cidrsubnet('10.0.0.0/16', 8, 2) = '10.0.2.0/24'. cidrsubnet('10.0.0.0/16', 4, 1) = '10.0.16.0/20'. Related: cidrhost (specific IP in CIDR), cidrnetmask, cidrrange. Use in for expressions to generate subnet CIDRs dynamically.",
      "tags": [
        "functions",
        "networking",
        "vpc",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-081",
      "question": "What is the 'flatten' function in Terraform?",
      "options": [
        "Removes duplicate values",
        "Sorts a list",
        "Compresses configuration",
        "Takes a list that may contain nested lists and flattens to a single-level list — essential when for expressions produce lists of lists. flatten([[1,2],[3,4]]) = [1,2,3,4]. Often used with for expressions over modules that return lists"
      ],
      "correct": 3,
      "explanation": "flatten: flatten([['subnet-1','subnet-2'], ['subnet-3']]) = ['subnet-1','subnet-2','subnet-3']. Use case: collect all subnets from multiple VPCs: flatten([for vpc in var.vpcs : vpc.subnets]). Often combined with for_each: for_each = toset(flatten([...])). Key for complex data transformations.",
      "tags": [
        "functions",
        "collections",
        "data-transformation",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-082",
      "question": "What is Terraform's 'concat' function?",
      "options": [
        "Merges maps",
        "Appends to a string",
        "String concatenation only",
        "Combines two or more lists into a single list — concat(['a','b'], ['c','d']) = ['a','b','c','d']. Also: concat can combine empty lists without error. Different from merge() which combines maps"
      ],
      "correct": 3,
      "explanation": "concat: combine lists. concat(var.public_subnets, var.private_subnets) → all subnets. For maps: merge() or can use for expressions. For strings: format() or interpolation. concat(['a'], []) = ['a'] (handles empty). Not to be confused with string interpolation: '${var.a}${var.b}'.",
      "tags": [
        "functions",
        "collections",
        "lists",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-083",
      "question": "What is a Terraform 'required_providers' block?",
      "options": [
        "Only for third-party providers",
        "Same as provider block",
        "Optional provider declaration",
        "Declares required providers and their sources/versions within the terraform configuration block — explicitly specifies which providers this configuration uses, their registry source (default registry.terraform.io), and version constraints. Required for: non-default sources, version pinning, team consistency"
      ],
      "correct": 3,
      "explanation": "required_providers: terraform { required_providers { aws = { source='hashicorp/aws', version='~>4.0' }, custom = { source='myorg.com/providers/custom', version='1.0' } } }. All providers should be declared. terraform init respects these constraints. Lock file records resolved versions. Without required_providers: Terraform may use any available version.",
      "tags": [
        "providers",
        "configuration",
        "versions",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-084",
      "question": "What is Terraform's 'lookup' function?",
      "options": [
        "Retrieves value from a map using a key with optional default — lookup(map, key, default). Returns map[key] if exists, default if not. Deprecated for simple cases: prefer map[key] directly or null coalescing. Use for dynamic key lookups where key may or may not exist",
        "A state query function",
        "A network function",
        "DNS lookup"
      ],
      "correct": 0,
      "explanation": "lookup: lookup(var.ami_by_region, var.region, 'ami-default'). Returns default if key absent (prevents error). Direct access: var.ami_by_region[var.region] (errors if key missing). Better: var.ami_by_region[var.region] with validate in variable to ensure valid regions. or try(var.ami_by_region[var.region], 'default'). lookup() with default is safe.",
      "tags": [
        "functions",
        "maps",
        "data-access",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-085",
      "question": "What is 'terraform plan -refresh=false'?",
      "options": [
        "Creates an offline plan",
        "Skips querying providers for current infrastructure state — uses last-known state without refresh. Faster plan (no API calls to check current state) but may produce inaccurate diff if infrastructure changed outside Terraform. Use with caution: only when you know state is accurate",
        "Prevents state file updates",
        "Prevents refreshing providers"
      ],
      "correct": 1,
      "explanation": "-refresh=false: skip resource state refresh. When to use: development iteration (you just applied, state accurate), CI where you're applying a saved plan immediately, performance (many resources), provider rate limiting. Risk: if someone changed infrastructure manually, plan won't detect drift. Default: refresh=true (accurate but slower).",
      "tags": [
        "cli",
        "performance",
        "operations",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-086",
      "question": "What is the 'coalesce' function in Terraform?",
      "options": [
        "Returns the first non-null.",
        "A null check only",
        "Returns all non-null values",
        "Combines two strings"
      ],
      "correct": 0,
      "explanation": "coalesce: coalesce('', 'second', 'third') = 'second' (skips empty string). coalesce(null, 'value') = 'value'. coalescelist: first non-empty list. Use for: optional inputs with fallback hierarchy. Example: coalesce(var.custom_sg_name, '${var.app}-sg') — use custom name if provided, else generate. Cleaner than nested conditionals. non-empty string argument — coalesce(var.name, local.default_name, 'fallback'). Useful for default value chains: try user input first, then computed default, then hardcoded fallback. coalescelist: same for lists",
      "tags": [
        "functions",
        "null-handling",
        "conditionals",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-087",
      "question": "What is the Terraform 'abs' and 'signum' math functions?",
      "options": [
        "abs(NUMBER): absolute value (abs(-5)=5). signum(NUMBER): sign of number (-1, 0, or 1). Along with: ceil(), floor(), min(), max(), log(), pow(), parseint(). Useful in: subnet calculations, index math, conditional expressions based on numeric values",
        "Only for financial calculations",
        "Deprecated in v1.0",
        "Math is not supported in Terraform"
      ],
      "correct": 0,
      "explanation": "Math functions: abs, signum, ceil, floor, log, pow, min, max. Practical: floor(length(var.azs) / 2) — calculate even split. ceil(var.count / 3) — round up for parallel batches. max(1, var.min_instances) — ensure minimum. min(var.max_count, length(data.aws_availability_zones.available.names)) — cap to available AZs.",
      "tags": [
        "functions",
        "math",
        "numeric",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-088",
      "question": "What is Terraform 'replace' function?",
      "options": [
        "Replaces a resource",
        "Taint equivalent",
        "Replaces provider version",
        "replace(string, search, replacement): replaces all occurrences of search in string. search can be a regex (with regex syntax). Example: replace('foo-bar-baz', '-', '_') = 'foo_bar_baz'. Useful for: normalizing names, formatting IDs"
      ],
      "correct": 3,
      "explanation": "replace: replace(var.name, '/', '-') — replace slashes with hyphens for naming. replace('test/env', regex, '') — remove unwanted chars. regexreplace: uses regex with capture groups. format: more flexible string building. trimprefix/trimsuffix: remove specific prefix/suffix. chomp: remove trailing newline.",
      "tags": [
        "functions",
        "strings",
        "naming",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-089",
      "question": "What is the Terraform 'element' function?",
      "options": [
        "Accesses set elements",
        "Returns element at given index in a list,.",
        "Adds to a list",
        "Gets first element only"
      ],
      "correct": 1,
      "explanation": "element: element(['a','b','c'], 0)='a', element(['a','b','c'], 3)='a' (wraps). Use for cycling: element(var.azs, count.index) — distributes resources across AZs cyclically. If 3 resources and 2 AZs: 0→az0, 1→az1, 2→az0. Modern alternative: for_each with explicit mapping. Wrapping behavior unique to element vs list[index]. with wrapping for out-of-bounds indices — element(list, index). If index >= length: wraps around (modulo). Deprecated for simple index access: list[index] is preferred. Useful for: cycling through a fixed list (cycling through AZs for multiple subnets)",
      "tags": [
        "functions",
        "lists",
        "cycling",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-090",
      "question": "What is 'terraform providers schema'?",
      "options": [
        "Downloads provider schemas",
        "Shows the schema of a provider's resources and data sources — json output of all attributes, types, descriptions for every resource. terraform providers schema -json | jq to explore. Useful for: understanding provider capabilities, writing dynamic configurations",
        "Shows installed providers",
        "Validates provider configuration"
      ],
      "correct": 1,
      "explanation": "terraform providers schema -json: outputs full schema for all configured providers. Useful for: understanding all configurable attributes, writing provider wrappers, automation that generates Terraform code, understanding deprecated vs recommended attributes. Requires: providers must be initialized (terraform init first).",
      "tags": [
        "providers",
        "cli",
        "documentation",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-091",
      "question": "What is the 'format' function in Terraform?",
      "options": [
        "A deprecated function",
        "Formats values using printf-style format strings — format('%s/%s', var.bucket, var.prefix). Supports: %s (string), %d (decimal), %f (float), %q (quoted string with escaping), %v (value in default format). Useful for: building strings from multiple parts, formatting IDs",
        "Only for numbers",
        "Formats the .tf files"
      ],
      "correct": 1,
      "explanation": "format: format('arn:aws:iam::%s:role/%s', var.account_id, var.role_name). format('%d%%', 75) = '75%'. format('%.2f', 3.14159) = '3.14'. formatdate: date formatting. For simple cases: '${var.a}-${var.b}' interpolation is cleaner. format() is better when you need printf features (padding, precision, quoted strings).",
      "tags": [
        "functions",
        "strings",
        "formatting",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-092",
      "question": "What is the 'split' and 'join' pair of functions?",
      "options": [
        "split(separator, string): splits string into list by separator. join(separator, list): joins list into string with separator. Example: split(',', 'a,b,c') = ['a','b','c']. join(',', ['a','b','c']) = 'a,b,c'. Useful for: parsing env vars, building comma-separated values for AWS resources, converting between formats",
        "Only for CSV data",
        "Deprecated functions",
        "Network splitting only"
      ],
      "correct": 0,
      "explanation": "split/join: split(',', var.cidr_list_string) → list to use with for_each. join(',', aws_subnet.private[*].id) → comma-separated IDs for AWS resource argument. Parse ARN: split(':', arn)[4] extracts account ID. Useful for interop between string-based and list-based configurations.",
      "tags": [
        "functions",
        "strings",
        "data-transformation",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-093",
      "question": "What is the 'merge' function for maps?",
      "options": [
        "Merges state files",
        "Merges two Terraform configs",
        "Combines multiple maps into a single map.",
        "Creates a new backend"
      ],
      "correct": 2,
      "explanation": "merge: merge({'env':'dev','team':'ops'}, {'env':'prod','owner':'alice'}) = {'env':'prod','team':'ops','owner':'alice'}. Later values override earlier. Use for: tag management (default tags + resource-specific tags), config merging (defaults + overrides), building complex maps from multiple variable inputs. later keys override earlier keys. merge(var.default_tags, var.resource_tags, { Name = var.instance_name }). Returns new map. Useful for: building final resource tags from multiple sources, combining default and override configurations",
      "tags": [
        "functions",
        "maps",
        "data-transformation",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-094",
      "question": "What does 'terraform state rm' do vs 'terraform destroy -target'?",
      "options": [
        "Same effect",
        "state rm is safer",
        "state rm: removes resource FROM STATE but leaves real infrastructure running — Terraform forgets it. destroy -target: destroys the actual infrastructure AND removes from state. Use state rm when: decommissioning Terraform management without destroying resource, resource already deleted manually",
        "Both delete infrastructure"
      ],
      "correct": 2,
      "explanation": "state rm: terraform state rm aws_instance.web → resource remains in cloud, Terraform no longer tracks it. Use: transitioning to different management, resource that can't/shouldn't be destroyed. destroy -target: DELETES the real resource then updates state. If you want to keep the resource but stop managing: state rm then resource configuration can be removed.",
      "tags": [
        "state",
        "operations",
        "destroy",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-095",
      "question": "What are Terraform's 'provisioners' and why should they be avoided?",
      "options": [
        "Provisioners are recommended",
        "Only for database setup",
        "Required for all resources",
        "Scripts/commands run during resource lifecycle: local-exec (local machine), remote-exec (resource itself), file (copy files to resource). Problems: no Terraform-native retry/error handling, makes plan unreliable, breaks idempotency, SSH requirements. Prefer: cloud-init, user_data, AMIs with software pre-installed, separate CM tool"
      ],
      "correct": 3,
      "explanation": "Provisioners: use as last resort. Problems: if provisioner fails after resource created: resource tainted (next apply destroys+recreates). No plan-time validation. Not repeatable (can't re-run without destroy). Better alternatives: AWS EC2 user_data (cloud-init), custom AMIs with Packer, Ansible separate run, AWS SSM Run Command, Lambda for automation.",
      "tags": [
        "provisioners",
        "best-practices",
        "anti-patterns",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-096",
      "question": "What is the difference between 'terraform.workspace' and Terraform Cloud workspaces?",
      "options": [
        "They are mapped 1:1",
        "terraform.workspace: CLI workspaces (local feature, state files with workspace suffix). Terraform Cloud workspaces: more complete concept — separate run environments, each with own state, variables, permissions, VCS connection. TC workspaces don't map to CLI workspaces (confusingly different concepts)",
        "CLI workspaces are deprecated",
        "Same concept"
      ],
      "correct": 1,
      "explanation": "Naming confusion: CLI workspaces = lightweight state isolation (same backend). TC workspaces = separate Terraform environments with own config, state, credentials, permissions, VCS. When using Terraform Cloud backend: terraform workspace commands map to TC workspaces. TC workspaces recommended over CLI workspaces for team use.",
      "tags": [
        "workspaces",
        "terraform-cloud",
        "concepts",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-097",
      "question": "What is 'terraform.tfstate.backup' vs 'terraform state pull'?",
      "options": [
        "state pull is a restore command",
        "tfstate.backup is for remote state",
        "Same mechanism",
        "tfstate.backup: local backup created automatically before each state modification (one level). state pull: downloads current state from remote backend to stdout — enables manual backup of remote state (terraform state pull > manual-backup.json). For teams: use remote state with versioning (S3 versioning) for comprehensive history"
      ],
      "correct": 3,
      "explanation": "Local backup (tfstate.backup): single automatic backup, overwritten each operation. state pull: current state from remote backend. For proper backup: terraform state pull > backup-$(date +%Y%m%d).json. Remote state with S3 versioning: automatic history of all state changes. Terraform Cloud: automatic state versioning with 6-month retention.",
      "tags": [
        "state",
        "backup",
        "operations",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-098",
      "question": "What is the 'file' function in Terraform?",
      "options": [
        "Only for binary files",
        "A data source for files",
        "Creates a file",
        "Reads a local file and returns its contents as a string — file('${path.module}/userdata.sh'). Use for: reading IAM policies from JSON files, loading certificates, user data scripts, passing file content to resources. Must exist at plan time"
      ],
      "correct": 3,
      "explanation": "file: file('${path.module}/policies/assume-role.json'). filebase64: base64-encode (for binary). filebase64sha256: checksum for change detection. fileexists: check if file exists before reading. path.module: ensures relative path from module location. Files must exist: Terraform errors at plan time if file not found.",
      "tags": [
        "functions",
        "files",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-099",
      "question": "What are Terraform's built-in types for variables?",
      "options": [
        "Types are optional",
        "Only string and number",
        "Primitive: string, number.",
        "Only primitive types"
      ],
      "correct": 2,
      "explanation": "Variable types: string ('text'), number (42 or 3.14), bool (true/false), list(string) (['a','b']), set(string) (like list, no duplicates, unordered), map(string) ({key='val'}), object({name=string, count=number}), tuple([string, number, bool]). any: no constraint. null: absent value. Types enable validation and documentation. bool. Collection: list(type), set(type), map(type). Structural: object({attr=type,...}), tuple([type,...]). Special: any (accepts any type), null (no value). Type constraints enforce input validation and enable IDE completion",
      "tags": [
        "variables",
        "types",
        "fundamentals",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-100",
      "question": "What is Terraform's 'tostring', 'tonumber', 'tobool' type conversion?",
      "options": [
        "Type conversion requires provider",
        "Functions don't exist in Terraform",
        "Only implicit conversion exists",
        "Explicit type conversion functions: tostring('42') or tostring(true) → string. tonumber('42') → 42. tobool('true') → true. Use when value type must match expected type. Terraform often converts implicitly, but explicit conversion improves clarity"
      ],
      "correct": 3,
      "explanation": "Type conversions: tostring(42) = '42', tostring(true) = 'true'. tonumber('3.14') = 3.14. tobool('true') = true, tobool(1) = error. Often needed when: variable input is string but resource expects number, working with JSON data, combining values of different types. tolist, toset, tomap for collection conversions.",
      "tags": [
        "functions",
        "types",
        "type-conversion",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-101",
      "question": "What is the Terraform 'timestamp' function?",
      "options": [
        "Returns state modification time",
        "Returns provider API timestamp",
        "Returns current UTC time as RFC 3339 string at the moment plan is generated — changes on every plan/apply (not idempotent). Use carefully: causes resource to update every apply if used in resource argument. Better: use timeRotating provider resource for expiry-based resources",
        "Returns hardware timestamp"
      ],
      "correct": 2,
      "explanation": "timestamp(): '2024-01-15T10:30:00Z'. Problem: new value every plan → resource shows update every apply. Use cases: non-idempotent tags (last_updated), one-time scripts that need current time. For rotation: hashicorp/time provider (time_rotating resource) is proper idempotent approach. formatdate(format, timestamp()): format timestamp.",
      "tags": [
        "functions",
        "time",
        "idempotency",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-102",
      "question": "What is the 'indent' function in Terraform?",
      "options": [
        "Adds spaces to the beginning of each line in a multi-line string (except the first line) — used for embedding formatted strings (YAML, JSON) inside template strings or heredocs while maintaining correct indentation",
        "Formats code indentation",
        "Applies code formatting",
        "Only for JSON formatting"
      ],
      "correct": 0,
      "explanation": "indent(num_spaces, string): adds N spaces to each line except first. Use in templatefile: 'config:\\n${indent(2, var.complex_config)}'. Produces properly indented output. Common with yamlencode/jsonencode: embed formatted sub-objects inside larger template files. Rarely needed in normal Terraform use but valuable for configuration generation.",
      "tags": [
        "functions",
        "strings",
        "templates",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-103",
      "question": "What is Terraform's 'compact' function?",
      "options": [
        "Reduces resource count",
        "Removes null and empty string values from a list — compact(['a', '', null, 'b']) = ['a', 'b']. Useful after conditional list building where some elements may be empty/null based on conditions",
        "Formats configuration",
        "Compresses state file"
      ],
      "correct": 1,
      "explanation": "compact: compact(['first', '', var.optional_value, 'last']) — removes empty strings and nulls. Use when building lists with conditional elements: [var.name, var.optional_suffix == '' ? '' : '-${var.optional_suffix}'] then wrap in compact. Works only on list of strings (not numbers or mixed types).",
      "tags": [
        "functions",
        "lists",
        "null-handling",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-104",
      "question": "What is Terraform's 'regexall' function?",
      "options": [
        "Replaces regex matches",
        "Tests regex performance",
        "Returns list of all matches of a regex pattern in a string — regexall('[0-9]+', 'foo123bar456') = ['123','456']. length(regexall(pattern, string)) > 0 is idiomatic 'string contains pattern' check. Also: regex (first match only, errors if no match)",
        "Validates regex syntax"
      ],
      "correct": 2,
      "explanation": "regexall: returns list (empty if no matches, never errors). length(regexall('[a-z]+', var.name)) > 0: check if name contains lowercase. regex: returns first match or errors. can(regex(pattern, string)): safe match test. Variable validation: condition = can(regex('^[a-z][a-z0-9-]*$', var.name)). Used for: input validation, pattern extraction.",
      "tags": [
        "functions",
        "regex",
        "validation",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-105",
      "question": "What is Terraform's 'keys' and 'values' function?",
      "options": [
        "Provider API functions",
        "Only for configuration access",
        "State access functions",
        "keys(map): returns sorted list of keys. values(map): returns list of values in key-sorted order. Use with for_each, count.index, or iteration. toset(keys(var.users)): set of user names for for_each. Complement: zipmap(keys, values) combines two lists into a map"
      ],
      "correct": 3,
      "explanation": "keys/values: keys({a=1, b=2}) = ['a','b']. values({a=1, b=2}) = [1,2]. Keys sorted alphabetically (consistent ordering). for_each = toset(keys(var.users)): iterate map by key. for k, v in var.users: direct iteration. zipmap(keys_list, values_list): rebuild map. transpose(map): swap keys and values (for maps of lists).",
      "tags": [
        "functions",
        "maps",
        "iteration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-106",
      "question": "What is the 'can' function combined with 'regex' for variable validation?",
      "options": [
        "Pattern: condition = can(regex('^[a-z][a-z0-9-]{2,19}$', var.name)). can(): returns true if expression doesn't error. regex(): errors if no match. Combined: validates that string matches pattern. Cleaner than regexall: length > 0 for single-match validation",
        "can() doesn't work with regex",
        "Not possible in Terraform",
        "Only basic string validation"
      ],
      "correct": 0,
      "explanation": "Best practice for regex validation: variable 'cluster_name' { validation { condition = can(regex('^[a-z][a-z0-9-]*$', var.cluster_name)), error_message = 'Name must start with lowercase letter, contain only lowercase alphanumeric and hyphens' } }. can(regex()) pattern: idiomatic Terraform validation. More readable than regexall([...]) length check.",
      "tags": [
        "validation",
        "regex",
        "variables",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-107",
      "question": "What is Terraform workspace state isolation?",
      "options": [
        "Each workspace has a completely separate state file — changes in one workspace don't affect others. Local: separate .tfstate files (terraform.tfstate.d/workspace_name/terraform.tfstate). Remote (S3): separate keys per workspace (env:/workspace_name/terraform.tfstate). Terraform Cloud: completely separate state per workspace",
        "Workspaces share variables",
        "Workspaces share state",
        "State isolation requires Enterprise"
      ],
      "correct": 2,
      "explanation": "Workspace state isolation: fundamentally separate states. Same configuration, different state = different infrastructure instances (dev database and prod database). terraform.workspace in code: customize per workspace. Variable values: set per workspace in Terraform Cloud, or use separate .tfvars files per workspace.",
      "tags": [
        "workspaces",
        "state",
        "fundamentals",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-108",
      "question": "What is a Terraform provider 'configuration_aliases'?",
      "options": [
        "Version aliases for providers",
        "Provider name aliases",
        "Allows a module to declare that it accepts multiple configurations of the same provider type — the root module passes different provider configurations to the module. Used for multi-region modules that need to deploy to different regions",
        "Default provider aliases"
      ],
      "correct": 2,
      "explanation": "configuration_aliases: module defines required_providers { aws = { source='hashicorp/aws', configuration_aliases=[aws.us-east, aws.eu-west] } }. Root module: module 'multi_region' { providers = { aws.us-east = aws.us-east, aws.eu-west = aws.eu-west } }. Module resources use provider = aws.us-east. Enables clean multi-region module design.",
      "tags": [
        "providers",
        "modules",
        "multi-region",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-109",
      "question": "What is 'terraform plan -detailed-exitcode'?",
      "options": [
        "Enables debug logging",
        "Shows more verbose output",
        "Changes exit codes: 0=no changes/success, 1=error, 2=success but changes present. Useful in CI/CD scripts to distinguish: no changes needed (0) vs changes to apply (2) vs error (1). Enables conditional apply: only run apply if plan returns exit code 2",
        "Sets timeout for plan"
      ],
      "correct": 2,
      "explanation": "-detailed-exitcode: standard exit=0 (success/no-changes or success/changes — can't distinguish). Detailed: 0=no changes, 1=error, 2=changes needed. CI script: terraform plan -detailed-exitcode; if [ $? -eq 2 ]; then terraform apply; fi. Automates: skip apply when no changes needed. Common in GitOps pipelines.",
      "tags": [
        "cli",
        "automation",
        "cicd",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-110",
      "question": "What are Terraform's 'preconditions' in data sources?",
      "options": [
        "Same as data filter",
        "Only for variables",
        "Data sources support lifecycle { precondition } blocks: validate conditions before data source is read. Example: data 'aws_ami' { filter {...} lifecycle { precondition { condition=var.region=='us-east-1', error_message='AMI lookup only works in us-east-1' } } }. Fails plan if condition is false",
        "Only in resources"
      ],
      "correct": 2,
      "explanation": "Data source preconditions: validate inputs before querying provider. Fail fast with useful error messages rather than confusing provider API errors. Example: ensure search criteria will return valid results. Postconditions: validate returned data meets expectations (e.g., AMI must be EBS-backed, must be encrypted). Terraform 1.2+.",
      "tags": [
        "data-sources",
        "validation",
        "lifecycle",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-111",
      "question": "What is Terraform's 'moved' block in a module?",
      "options": [
        "Moves module source",
        "Moves module to new location",
        "A deprecated feature",
        "Declares that a resource has moved to a different address — can be used within modules to handle refactoring. Module calling: moved { from=module.old_name.aws_instance.web, to=module.new_name.aws_instance.web }. Enables module renaming, resource extraction without destroy/recreate"
      ],
      "correct": 3,
      "explanation": "Module-level moved: rename a module in root config without recreating resources. module.database (old) → module.rds (new): add moved { from=module.database, to=module.rds }. Apply: state updated (resource addresses renamed), no infrastructure changes. Remove moved block after all consumers have applied. Terraform 1.1+.",
      "tags": [
        "modules",
        "refactoring",
        "moved",
        "advanced",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-112",
      "question": "What is the Terraform 'jsondecode' and 'jsonencode' function pair?",
      "options": [
        "Deprecated JSON functions",
        "Requires JSON provider",
        "jsondecode(string): parses JSON string into Terraform value. jsonencode(value): converts Terraform value to JSON string. Use cases: reading JSON policy documents, constructing JSON for API calls, parsing JSON responses from data sources",
        "Only for provider config"
      ],
      "correct": 2,
      "explanation": "jsonencode: jsonencode({Version='2012-10-17', Statement=[{Effect='Allow', Action='*', Resource='*'}]}). Creates valid JSON for IAM policies, container definitions, etc. jsondecode: jsondecode(data.aws_iam_policy_document.main.json) → Terraform map. Cleaner than heredoc JSON strings. Validate JSON syntax at plan time (not runtime).",
      "tags": [
        "functions",
        "json",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-113",
      "question": "What is Terraform module 'source' with git?",
      "options": [
        "Only registry modules allowed",
        "Git source: 'git::https://github.com/org/repo.git?ref=v1.0'. ref can be: tag, branch, commit hash. SSH: 'git::ssh://git@github.com/org/repo.git'. Subdirectory: 'git::https://github.com/org/repo.git//modules/vpc?ref=main'. Double slash separates repo from subdirectory path",
        "Git sources don't support versioning",
        "Only HTTPS is supported"
      ],
      "correct": 1,
      "explanation": "Git module sources: always pin ref (don't use HEAD/main — unstable). Prefer tags (semantic versioning) for production modules. SSH vs HTTPS: SSH requires key setup (good for CI), HTTPS requires token or PAC. Subdirectory: useful for monorepos with multiple modules. Private repos: configure git credentials before terraform init.",
      "tags": [
        "modules",
        "git",
        "versioning",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-114",
      "question": "What is Terraform's 'zipmap' function?",
      "options": [
        "Zips two archives",
        "Creates a map from two lists.",
        "A compression function",
        "Merges two maps"
      ],
      "correct": 1,
      "explanation": "zipmap: zipmap(var.subnet_names, aws_subnet.main[*].id) → map from subnet name to ID. Use with for expressions or resources: for_each = zipmap(keys(var.policies), values(var.policies)). Pairs: output = zipmap(keys(var.users), [for u in var.users : module.user[u.name].arn]) — combine with complex for expressions. one for keys, one for values. zipmap(['a','b','c'], [1,2,3]) = {a=1, b=2, c=3}. Lists must be same length. Complement to keys() and values(). Useful when you have parallel lists of names and values",
      "tags": [
        "functions",
        "maps",
        "data-transformation",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-115",
      "question": "What are Terraform 'experimental' features?",
      "options": [
        "Features enabled via language_experiments in terraform block — may change or be removed in future versions. Example: module_variable_optional_attrs (now stable in 1.3). Use only in development, not production. Enabled: terraform { experiments = [module_variable_optional_attrs] }",
        "Debug-only features",
        "Beta provider versions",
        "Unstable provider features"
      ],
      "correct": 0,
      "explanation": "Experiments: terraform { experiments = [example_feature] }. Each experiment: provisional feature being tested. May: change API, be removed, or graduate to stable feature. Check changelog: features graduate from experiments to stable. Production: avoid experiments — wait for stable release. Example graduated: optional variable attributes, module_variable_optional_attrs (now stable).",
      "tags": [
        "advanced",
        "experimental",
        "versioning",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-116",
      "question": "What is Terraform's 'setintersection', 'setunion', and 'setsubtract'?",
      "options": [
        "Requires special module",
        "Only list operations exist",
        "Set operations: setintersection([a,b,c],[b,c,d]) = {b,c}. setunion([a,b],[b,c]) = {a,b,c}. setsubtract([a,b,c],[b,c]) = {a}. Use for: comparing lists of resources, finding common/different elements, computing changes",
        "No set math in Terraform"
      ],
      "correct": 2,
      "explanation": "Set math: setintersection: elements in ALL sets. setunion: elements in ANY set. setsubtract: elements in first but not second. Use cases: find users in group A AND group B (intersection), all users in either group (union), users to remove (subtract current from desired). Useful with toset() for processing lists as sets.",
      "tags": [
        "functions",
        "sets",
        "data-processing",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-117",
      "question": "What is Terraform's 'null' value and how does it interact with resources?",
      "options": [
        "Same as empty string",
        "Represents absent/unknown values.",
        "Creates a null resource",
        "Deletes a resource"
      ],
      "correct": 1,
      "explanation": "null: explicitly omit optional attributes. some_resource { optional_attr = var.value == '' ? null : var.value }. When attribute = null: Terraform omits it from API call (provider uses default). Different from '' (empty string). Use for: optional nested blocks, conditional attribute inclusion. Ternary with null: common pattern for optional configurations. Set attribute to null: removes that attribute from API call (use provider default). In conditional: var.optional_attr == null ? null : var.optional_attr. Useful for optional attributes: some_attr = var.override != null ? var.override : null",
      "tags": [
        "null",
        "types",
        "configuration",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-118",
      "question": "What is the 'checkov' tool and its relationship to Terraform?",
      "options": [
        "A Terraform plugin",
        "Open-source static analysis tool for IaC security — scans Terraform configurations for misconfigurations and security issues (e.g., S3 bucket without encryption, security groups with 0.0.0.0/0 ingress, IAM wildcards). Runs before apply, catches issues early in pipeline",
        "A Terraform provider",
        "A state management tool"
      ],
      "correct": 1,
      "explanation": "Checkov: standalone CLI tool. Scan: checkov -d . --framework terraform. 2000+ policies covering: AWS, Azure, GCP, Kubernetes. Output: PASSED/FAILED with remediation guidance. CI integration: fail pipeline on HIGH/CRITICAL findings. Alternatives: tflint (linting), tfsec (security), terraform-compliance (BDD-style tests). Combine in pipeline: fmt → validate → checkov → plan → apply.",
      "tags": [
        "security",
        "tools",
        "cicd",
        "static-analysis",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-119",
      "question": "What is Terraform's 'tolist' and 'toset' use case difference?",
      "options": [
        "Identical functions",
        "tolist preserves no duplicates",
        "toset is deprecated",
        "tolist: converts to ordered list (duplicates preserved, indexed). toset: converts to unordered set (duplicates removed, keyed by value). for_each requires map or set (not list) → use toset(). When order matters: tolist. For deduplication: toset. Convert back: tolist(toset(var.list)) — deduplicate a list"
      ],
      "correct": 3,
      "explanation": "Pattern: for_each = toset(var.names) — deduplicate and enable for_each. tolist(aws_instance.web[*].id) — convert tuple to list. toset(['a','b','a']) = {'a','b'} (deduped). tolist({'a','b'}) = ['a','b'] (arbitrary order). Use toset for for_each patterns. Use tolist when you need ordered access by index.",
      "tags": [
        "functions",
        "types",
        "for_each",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-120",
      "question": "What is the recommended Terraform file structure for a module?",
      "options": [
        "Standard structure: main.tf (resources),.",
        "Alphabetical file naming",
        "Only provider.tf needed",
        "Single main.tf file"
      ],
      "correct": 0,
      "explanation": "Conventional structure: main.tf (core resources), variables.tf (all variable declarations), outputs.tf (all outputs), versions.tf (required_providers, required_version, provider config). examples/ directory: working example usage. tests/ directory: .tftest.hcl files. README: generated by terraform-docs. CHANGELOG: semantic versioning history. Consistency enables tooling (terraform-docs, semantic-release). variables.tf (input variables), outputs.tf (output values), versions.tf (required_providers and required_version), README.md (usage docs), CHANGELOG.md (version history). Optional: locals.tf, data.tf, provider.tf. Subdirectory for examples/ and tests/",
      "tags": [
        "modules",
        "structure",
        "best-practices",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-121",
      "question": "What is the Terraform taint command and when should you use it?",
      "options": [
        "Prevents a resource from being modified",
        "Deletes a resource immediately",
        "Marks a specific resource for forced replacement on the next terraform apply — the resource will be destroyed and recreated. Use when a resource is in a broken state that Terraform can't detect automatically",
        "Marks a resource as invalid syntax"
      ],
      "correct": 2,
      "explanation": "terraform taint RESOURCE_ADDRESS: forces resource recreation next apply. Deprecated in TF 1.0+ — use terraform apply -replace=RESOURCE_ADDRESS instead. Use when: resource is misconfigured but Terraform shows it as up-to-date, or manual changes outside Terraform broke the resource.",
      "tags": [
        "cli",
        "lifecycle",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-122",
      "question": "What is the Terraform replace option in modern versions?",
      "options": [
        "There is no replace option",
        "terraform apply -replace=RESOURCE_ADDRESS: forces a specific resource to be replaced (destroy + create) in a single apply operation — replaces the deprecated 'terraform taint' command. Safer: shows plan before executing",
        "terraform replace RESOURCE",
        "terraform destroy -target=RESOURCE then apply"
      ],
      "correct": 1,
      "explanation": "terraform apply -replace=google_compute_instance.web: shows plan with resource marked for replacement, then executes. More transparent than taint (shows the replacement plan before confirming). Use when a specific resource needs to be recreated without destroying everything else.",
      "tags": [
        "cli",
        "lifecycle",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-123",
      "question": "What is a Terraform null_resource and when is it used?",
      "options": [
        "A deleted resource placeholder",
        "A resource with no attributes",
        "A resource that implements standard lifecycle but doesn't create real infrastructure — used with provisioners to run scripts, or as a dependency anchor for other resources without creating actual cloud resources",
        "An empty variable"
      ],
      "correct": 2,
      "explanation": "null_resource: triggers = {} map to control when it re-runs. Provisioner executes on create (or destroy). Use cases: run a script after infrastructure is created, create a dependency between resources without a direct reference, trigger provisioners conditionally. terraform-provider-null required.",
      "tags": [
        "null_resource",
        "provisioners",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-124",
      "question": "What is the Terraform random provider used for?",
      "options": [
        "Generates random infrastructure",
        "A shuffle provider",
        "Generates random values (strings,.",
        "A test framework"
      ],
      "correct": 2,
      "explanation": "random_string: generates random alphanumeric string. random_id: generates random bytes in hex/base64. random_password: generates strong password. random_integer: random number in range. These resources are stable after creation (don't regenerate on each apply unless keepers change). integers, UUIDs, passwords) within Terraform configurations — commonly used for: unique resource names (S3 buckets, database passwords), avoiding naming conflicts, generating entropy for resources that require unique IDs",
      "tags": [
        "random",
        "providers",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-125",
      "question": "What is the Terraform time_sleep resource?",
      "options": [
        "From the hashicorp/time provider.",
        "A debugging pause",
        "A timeout configuration",
        "A rate limit feature"
      ],
      "correct": 0,
      "explanation": "time_sleep.wait_30_seconds { create_duration = '30s' depends_on = [aws_instance.db] }. Other resources depend on this sleep. Use sparingly — prefer proper readiness checks. time_static: captures current time as a static value for tagging/versioning. creates a delay between resource creation steps. Use when: a resource needs time to become available after creation (e.g., wait 30s after creating a database before creating a dependent service)",
      "tags": [
        "time",
        "providers",
        "dependencies",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-126",
      "question": "What is the Terraform sensitive function?",
      "options": [
        "Prevents a variable from being used",
        "Encrypts the state file",
        "sensitive(value): marks a value as sensitive in the plan output — redacts the value in logs and console output. Use for passwords, API keys, secrets that should not appear in Terraform plan output or state file display",
        "Marks a file as sensitive"
      ],
      "correct": 2,
      "explanation": "sensitive(): wraps any value to mark it sensitive. Terraform redacts it in plan output (shows as '(sensitive value)'). Note: the value IS still stored in state — sensitive() only affects display. For real secrets: use Vault provider or AWS Secrets Manager to retrieve at runtime, never store in state.",
      "tags": [
        "security",
        "variables",
        "outputs",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-127",
      "question": "What is Terraform's lifecycle precondition and postcondition?",
      "options": [
        "Build conditions",
        "Loop conditions",
        "Module requirements",
        "Validation checks within resource or data source lifecycle blocks — precondition: validates before resource is modified (checks assumptions), postcondition: validates after resource is created (verifies outcomes). Fails the apply with a custom error message if condition is false"
      ],
      "correct": 3,
      "explanation": "lifecycle { precondition { condition = var.instance_type != 't3.nano' error_message = 'Instance type too small for production.' } }. Checks run during plan. Custom error messages guide users. Better than null_resource workarounds. Postcondition verifies the resource was created as expected.",
      "tags": [
        "lifecycle",
        "validation",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-128",
      "question": "What is the Terraform import block (Terraform 1.5+)?",
      "options": [
        "Declarative import configuration in .tf files.",
        "A provider installation block",
        "Imports modules from the registry",
        "A module source declaration"
      ],
      "correct": 0,
      "explanation": "import { to = aws_instance.example, id = 'i-1234567890' }. Run terraform plan: shows what will be imported. terraform apply: performs import. -generate-config-out=generated.tf: auto-generates resource configuration from imported resource. Much better than CLI import for team workflows. defines which existing resources to import into state. Unlike terraform import CLI command, import blocks are plan-able and reviewable before execution. Generate config with -generate-config-out flag",
      "tags": [
        "import",
        "state",
        "cli",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-129",
      "question": "What is Terraform's check block?",
      "options": [
        "A pre-plan validation",
        "A unit test framework",
        "Syntax validation",
        "Custom assertions that run after apply to continuously validate infrastructure — check blocks don't prevent deployment but alert when assertions fail. Used for: verifying URLs are reachable, checking certificate expiry, validating external conditions"
      ],
      "correct": 3,
      "explanation": "check 'website_reachable' { data 'http' 'healthcheck' { url = 'https://example.com' } assert { condition = data.http.healthcheck.status_code == 200 error_message = 'Website returned unexpected status.' } }. Run every plan/apply. Failures are warnings, not errors. Continuous compliance checking.",
      "tags": [
        "validation",
        "testing",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-130",
      "question": "What is the Terraform templatefile function?",
      "options": [
        "Reads a template file and renders it with provided variables — replaces ${variable} placeholders with values. Use for: generating complex configuration files, user_data scripts, Kubernetes manifests with dynamic values",
        "Creates file resources",
        "A string template only",
        "A file copy function"
      ],
      "correct": 0,
      "explanation": "templatefile('scripts/init.sh.tpl', { db_host = aws_db_instance.main.address, env = var.environment }). Template file uses ${var} syntax. Useful for user_data in EC2, cloud-init scripts, config files. Alternative to heredoc strings in resource config.",
      "tags": [
        "functions",
        "templating",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-131",
      "question": "What does terraform fmt -recursive do?",
      "options": [
        "Recursively formats all .tf and .tfvars files in the current directory and all subdirectories to Terraform's canonical style — enforces consistent indentation, spacing, and alignment. Run in CI/CD to catch formatting issues",
        "Validates all configurations",
        "Formats only the current directory",
        "Removes unused variables"
      ],
      "correct": 0,
      "explanation": "terraform fmt: enforces canonical style. -recursive: all subdirectories. -check: return non-zero if files need formatting (for CI/CD gates). -diff: show what would change. Always run before commits in team projects. Formatting is deterministic — same code always formats the same way.",
      "tags": [
        "cli",
        "formatting",
        "best-practices",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-132",
      "question": "What is the Terraform moved block?",
      "options": [
        "Declares that a resource was renamed or moved to a different module — allows refactoring Terraform configurations without destroying and recreating resources. Terraform updates the state mapping instead of performing destructive changes",
        "Migrates state between backends",
        "Moves files between directories",
        "A provider migration helper"
      ],
      "correct": 0,
      "explanation": "moved { from = aws_instance.old_name, to = aws_instance.new_name }. Run terraform plan: shows 'moved' instead of destroy/create. Apply: updates state. moved blocks can also move resources into/out of modules. Remove moved blocks after all team members have applied the change.",
      "tags": [
        "refactoring",
        "state",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-133",
      "question": "What is Terraform's data 'terraform_remote_state' data source?",
      "options": [
        "Reads local state files",
        "A module reference",
        "Accesses outputs from another Terraform state file stored in a remote backend — enables cross-stack references without tight coupling. Consumer stack reads outputs from producer stack's state. Alternative: use an explicit data source or shared config management",
        "A state backup feature"
      ],
      "correct": 2,
      "explanation": "data 'terraform_remote_state' 'vpc' { backend = 's3', config = { bucket = 'my-state', key = 'vpc/terraform.tfstate' } }. Access outputs: data.terraform_remote_state.vpc.outputs.vpc_id. Creates coupling between stacks — consider carefully. Alternative: use shared data sources (AWS SSM, Consul) for looser coupling.",
      "tags": [
        "remote-state",
        "data-sources",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-134",
      "question": "What is Terraform workspace vs Terraform environment?",
      "options": [
        "Terraform workspaces: named instances of state within a single backend — isolate state per workspace (default, dev, staging, prod). 'Environment' is not a built-in Terraform concept — often refers to workspaces or separate state files/backends per environment",
        "They are the same thing",
        "Environments require Terraform Cloud",
        "Workspaces are deprecated"
      ],
      "correct": 0,
      "explanation": "Workspaces: terraform workspace new dev. ${terraform.workspace} interpolation in code. Limitation: same backend, easy to accidentally modify wrong workspace. For production: separate backends per environment often preferred over workspaces. Terraform Cloud: workspaces = full isolated config + state + variables.",
      "tags": [
        "workspaces",
        "state",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-135",
      "question": "What is the Terraform for_each meta-argument difference from count?",
      "options": [
        "count is newer",
        "Identical functionality",
        "for_each only works with sets",
        "for_each iterates over a map or set."
      ],
      "correct": 3,
      "explanation": "for_each { 'us-east-1' = 'ami-123', 'eu-west-1' = 'ami-456' }: resource identified by map key. Remove middle item: only that specific resource destroyed. count [0,1,2]: remove index 1 → index 2 becomes 1 → Terraform sees index 1 changed → destroys+recreates old index 2. Always use for_each for collections. creates one resource per map key/set value with a meaningful identifier. count uses numeric index — fragile when list items are inserted/removed (shifts indices, causes unexpected destroys). for_each preferred for collections",
      "tags": [
        "for_each",
        "count",
        "meta-arguments",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    },
    {
      "id": "tf-136",
      "question": "What is Terraform Cloud's VCS-driven workflow?",
      "options": [
        "A manual workflow",
        "A CLI-only workflow",
        "Connect Terraform Cloud workspace to a VCS repo (GitHub, GitLab, Bitbucket) — automatically triggers speculative plans on PRs and applies on merge to main branch. Review plans in GitHub PR checks before merging. Enables GitOps workflow for infrastructure",
        "A local-only feature"
      ],
      "correct": 2,
      "explanation": "VCS workflow: PR → Terraform Cloud runs speculative plan → plan output shown in PR checks → review changes → merge → Terraform Cloud runs apply automatically. Plan includes: additions, changes, destructions. Team can review infrastructure changes like code changes. Integrates with branch protection rules.",
      "tags": [
        "terraform-cloud",
        "vcs",
        "gitops",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-137",
      "question": "What is the Terraform provider alias and when is it needed?",
      "options": [
        "Allows using the same provider multiple times with different configurations in the same module — common for multi-region or multi-account deployments. Each alias represents a separate provider configuration",
        "A provider version alias",
        "A resource rename",
        "A module alias"
      ],
      "correct": 0,
      "explanation": "provider 'aws' { alias = 'us_east', region = 'us-east-1' }. provider 'aws' { alias = 'eu_west', region = 'eu-west-1' }. Resource: resource 'aws_instance' 'east' { provider = aws.us_east }. Module: module 'network' { providers = { aws = aws.eu_west } }. Essential for multi-region architectures.",
      "tags": [
        "providers",
        "multi-region",
        "aliases",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-138",
      "question": "What is the Terraform console command?",
      "options": [
        "A state viewer",
        "Opens a CLI shell",
        "An interactive REPL (Read-Eval-Print Loop) for evaluating Terraform expressions — test functions, variable interpolations, and data source lookups without running a full plan. Useful for debugging complex expressions",
        "A monitoring console"
      ],
      "correct": 2,
      "explanation": "terraform console: interactive prompt. Type expressions: cidrsubnet('10.0.0.0/16', 8, 3) → shows result. Test: contains(['a','b'], 'a') → true. Access state: data.aws_vpc.main.id → shows actual ID. Load specific vars: terraform console -var-file=dev.tfvars. Exit with Ctrl+D or exit.",
      "tags": [
        "cli",
        "debugging",
        "functions",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-139",
      "question": "What is Terraform's ephemeral values feature (1.10+)?",
      "options": [
        "Values that exist only during a Terraform operation and are never stored in state or plan files — for sensitive values like database passwords, API tokens retrieved from vaults. Marked with ephemeral = true in output blocks",
        "A deletion mechanism",
        "Temporary resource creation",
        "A workspace feature"
      ],
      "correct": 0,
      "explanation": "Ephemeral values (TF 1.10+): sensitive data flows through operations without persisting anywhere. Ephemeral resources: created and destroyed within a single Terraform operation (e.g., retrieve Vault token, use it, Vault revokes it). Complementary to existing sensitive() function which still stores in state.",
      "tags": [
        "security",
        "sensitive-values",
        "terraform-1.10",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-140",
      "question": "What is Terraform's module composition pattern?",
      "options": [
        "A module registry feature",
        "A provider composition",
        "Building complex infrastructure by composing multiple focused modules — each module handles one concern (networking, compute, security). Root module orchestrates child modules. Promotes reuse, testability, and separation of concerns",
        "Using modules inside modules"
      ],
      "correct": 2,
      "explanation": "Root module uses: module 'network' { source = './modules/network' } module 'compute' { source = './modules/compute', vpc_id = module.network.vpc_id }. Modules pass outputs to other modules. Flat module structure preferred over deep nesting. Each module: focused, independently testable, documented.",
      "tags": [
        "modules",
        "architecture",
        "best-practices",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-141",
      "question": "What is the difference between terraform.tfstate and terraform.tfstate.backup?",
      "options": [
        "One is encrypted",
        "terraform.tfstate: current state of infrastructure. terraform.tfstate.backup: automatic copy of the previous state before the last apply. If current state becomes corrupted, backup can help recover. Both should NEVER be committed to git (contain sensitive data, credentials)",
        "Backup is a manual copy",
        "Both are identical"
      ],
      "correct": 1,
      "explanation": "State backup: created automatically before every apply. One backup per state file. If apply fails midway, state may be partially updated — backup helps restore. For team use: remote backends handle state with locking and versioning automatically. Local state: only for solo dev or learning.",
      "tags": [
        "state",
        "backup",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-142",
      "question": "What is Terraform's data source for reading existing infrastructure?",
      "options": [
        "An output statement",
        "Creates new resources",
        "A variable type",
        "Reads attributes of existing infrastructure that was created outside Terraform or by a different Terraform configuration — allows referencing external resources without managing them. Common: read existing VPC ID, security group, AMI ID"
      ],
      "correct": 3,
      "explanation": "data 'aws_vpc' 'existing' { filter { name = 'tag:Name', values = ['production-vpc'] } }. Then reference: data.aws_vpc.existing.id. Data sources query provider APIs on every plan. Don't use data sources for resources you manage with Terraform (creates plan instability).",
      "tags": [
        "data-sources",
        "fundamentals",
        "terraform-003"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "tf-143",
      "question": "What is the Terraform required_providers block and why is it important?",
      "options": [
        "A provider registry login",
        "Declares the providers a module requires with source addresses and version constraints — ensures reproducible deployments by pinning provider versions. Without it: Terraform uses latest version which may introduce breaking changes",
        "A provider download command",
        "A provider alias declaration"
      ],
      "correct": 1,
      "explanation": "terraform { required_providers { aws = { source = 'hashicorp/aws', version = '~> 5.0' } } }. Version constraints: ~> 5.0 (5.x but not 6.0), >= 4.0 (any 4.0+), = 5.1.0 (exact). Always specify. Run terraform providers lock to generate .terraform.lock.hcl with exact checksums for reproducibility.",
      "tags": [
        "providers",
        "versioning",
        "best-practices",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-144",
      "question": "What is Terraform's ignore_changes lifecycle argument?",
      "options": [
        "Tells Terraform to ignore changes to specific attributes when determining if a resource needs to be updated — use when: external processes modify resource attributes (AWS Auto Scaling changes instance count, config management changes OS settings)",
        "Marks a resource as unmanaged",
        "Prevents resource deletion",
        "Skips validation"
      ],
      "correct": 0,
      "explanation": "lifecycle { ignore_changes = [tags, user_data] }. Terraform still creates/destroys the resource but won't update it when these attributes change outside Terraform. Use carefully — ignoring too much defeats the purpose of IaC. ignore_changes = all: never update (use for imported resources temporarily).",
      "tags": [
        "lifecycle",
        "state",
        "management",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-145",
      "question": "What is Terraform's dynamic block?",
      "options": [
        "Generates multiple nested configuration blocks within a resource dynamically based on a collection — replaces repetitive block declarations. Common use: security group rules, IAM policy statements, routing rules where count varies",
        "A loop for resources",
        "A conditional block",
        "A provider feature"
      ],
      "correct": 0,
      "explanation": "dynamic 'ingress' { for_each = var.ingress_rules, content { from_port = ingress.value.from_port, to_port = ingress.value.to_port, protocol = ingress.value.protocol, cidr_blocks = ingress.value.cidr_blocks } }. Each item in var.ingress_rules generates one ingress block. Cleaner than repetitive hardcoded blocks.",
      "tags": [
        "dynamic-blocks",
        "meta-arguments",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-146",
      "question": "What is Terraform's Sentinel policy framework (Terraform Enterprise/Cloud)?",
      "options": [
        "A security scanner",
        "A cost estimation tool",
        "An open-source tool",
        "A policy-as-code framework integrated into Terraform Enterprise and Cloud — evaluates policies written in Sentinel language against plan data before apply. Enforce: compliance, cost controls, security requirements. Three enforcement levels: advisory, soft-mandatory, hard-mandatory"
      ],
      "correct": 3,
      "explanation": "Sentinel: policies run after plan, before apply. Hard-mandatory: block apply if policy fails. Soft-mandatory: allow override with explanation. Advisory: warn only. Use cases: require all resources tagged, block creation in certain regions, enforce instance type limits. Sentinel language: Python-like, purpose-built for policy.",
      "tags": [
        "sentinel",
        "policy",
        "terraform-enterprise",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-147",
      "question": "What is the Terraform compact() function?",
      "options": [
        "Compresses the state file",
        "Removes null and empty string values from a list — useful for cleaning up lists that may contain null values from conditional expressions. Returns a new list with only non-empty, non-null values",
        "A string trim function",
        "A list sort function"
      ],
      "correct": 1,
      "explanation": "compact(['a', '', 'b', null, 'c']) → ['a', 'b', 'c']. Common use: concat() produces nulls from conditional values, compact() cleans them. Example: compact([var.enable_feature ? 'feature-flag' : null, 'always-included']). Use with flatten() for nested list cleaning.",
      "tags": [
        "functions",
        "collections",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-148",
      "question": "What is Terraform's jsonencode and jsondecode?",
      "options": [
        "JSON validation functions",
        "jsonencode: converts HCL value to JSON string. jsondecode: converts JSON string to HCL value. Essential for: creating IAM policies, Kubernetes configs, or any JSON blob that needs to be embedded in Terraform resources as a string attribute",
        "JSON file creation functions",
        "File encoding functions"
      ],
      "correct": 1,
      "explanation": "jsonencode({ Version = '2012-10-17', Statement = [...] }): creates valid JSON string for IAM policy. jsondecode(data.aws_secretsmanager_secret_version.creds.secret_string): parse JSON secret back to map. Better than heredoc JSON: handles quoting, validates syntax, supports HCL expressions inside.",
      "tags": [
        "functions",
        "json",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-149",
      "question": "What is OpenTofu and how does it relate to Terraform?",
      "options": [
        "A Terraform competitor with different syntax",
        "A Terraform plugin",
        "A Terraform cloud service",
        "An open-source fork of Terraform maintained by the Linux Foundation — created in response to HashiCorp's license change (BUSL) in August 2023. Drop-in compatible with Terraform, same HCL syntax. Goal: maintain an open-source version of Terraform under MPL-2.0"
      ],
      "correct": 3,
      "explanation": "OpenTofu forked from Terraform 1.5.x (last MPL-2.0 version). Maintained by Linux Foundation, community-governed. Compatible: same .tf syntax, same providers (initially). Adding features: provider-defined functions, state encryption. Use if: license change is a concern, need fully OSS toolchain. HashiCorp Terraform continues development separately.",
      "tags": [
        "opentofu",
        "history",
        "oss",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-150",
      "question": "What is Terraform Cloud's cost estimation?",
      "options": [
        "Automatically estimates the monthly cost of infrastructure changes in the plan — shows estimated cost delta for proposed changes before applying. Helps teams understand financial impact of infrastructure changes. Available for major AWS, Azure, GCP resources",
        "A billing integration",
        "A budget alert feature",
        "A resource pricing API"
      ],
      "correct": 0,
      "explanation": "Cost estimation: runs after plan, before apply. Shows: prior monthly estimate, new monthly estimate, delta (change in cost). Uses public pricing APIs. Not 100% accurate (reserved pricing, volume discounts not considered). Integrates with Sentinel: can block applies that exceed cost thresholds.",
      "tags": [
        "terraform-cloud",
        "cost",
        "governance",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-151",
      "question": "A Terraform configuration creates an S3 bucket using `count = 3`. After applying, you add a fourth bucket by changing `count = 4`. Which bucket resource is created without modifying any existing ones?",
      "options": [
        "All four buckets are destroyed and recreated since `count` changes require full replacement",
        "Only `aws_s3_bucket.example[3]` is created — Terraform adds the new index without touching existing ones",
        "The bucket at index 0 is destroyed and `aws_s3_bucket.example[0]` through `[3]` are rebuilt from scratch",
        "Terraform imports the existing three buckets and then creates the fourth without a plan step"
      ],
      "correct": 1,
      "explanation": "When you increase `count`, Terraform calculates the diff and only adds the new index. `aws_s3_bucket.example[0]`, `[1]`, and `[2]` already exist in state and match the configuration — they are untouched. Only `aws_s3_bucket.example[3]` is planned as a `+create`. This is why `for_each` with stable keys is preferred over `count` for resource sets — inserting an item in the middle with `count` renumbers indexes and triggers unwanted destroys.",
      "tags": [
        "count",
        "meta-arguments",
        "state",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-152",
      "question": "You have an existing EC2 instance (`i-0abcd1234`) that was created manually and you need to bring it under Terraform management. Which command imports it into the state file?",
      "options": [
        "`terraform import aws_instance.web i-0abcd1234`",
        "`terraform refresh aws_instance.web i-0abcd1234`",
        "`terraform state push aws_instance.web i-0abcd1234`",
        "`terraform state import -resource=aws_instance.web -id=i-0abcd1234`"
      ],
      "correct": 0,
      "explanation": "`terraform import <resource_address> <provider_id>` is the correct command. It adds the existing resource to the Terraform state without creating or destroying it. You still need to write the corresponding `resource` block in your `.tf` file before running `import`. Terraform does NOT generate the configuration for you (use `terraform plan` after import to verify alignment). `state push` uploads a state file. `refresh` updates state from the real infrastructure but doesn't import new resources.",
      "tags": [
        "import",
        "state",
        "cli",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-153",
      "question": "You want to prevent a production database from being accidentally destroyed via `terraform destroy` or a plan that removes it. Which lifecycle configuration achieves this?",
      "options": [
        "Set `lifecycle { ignore_changes = all }` to make Terraform ignore all changes to the resource",
        "Set the resource as `data` instead of `resource` to make it read-only and undeletable by Terraform",
        "Set `lifecycle { create_before_destroy = true }` to ensure a new instance is created before the old one is removed",
        "Set `lifecycle { prevent_destroy = true }` — Terraform will error if a plan would destroy this resource"
      ],
      "correct": 3,
      "explanation": "`lifecycle { prevent_destroy = true }` causes Terraform to return an error if the execution plan would destroy that resource — even in `terraform destroy`. This is a safeguard for critical infrastructure. Note: it only prevents plans that destroy the resource; you can still override it by removing the `prevent_destroy` block and re-running. `ignore_changes = all` prevents Terraform from updating the resource but doesn't prevent destruction. `create_before_destroy` changes the replacement order.",
      "tags": [
        "lifecycle",
        "prevent-destroy",
        "state",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-154",
      "question": "A Terraform module in your configuration needs to use the same AWS provider as the root module but targeting a different region. How do you correctly pass a provider alias to the module?",
      "options": [
        "Set `region = var.region` in the module's provider block and pass the region as a module variable",
        "Set `AWS_REGION=us-west-2` as an environment variable inside the module source directory",
        "Use the `providers` meta-argument in the module block to map the module's provider alias to the root provider alias",
        "Create a new `provider` block inside the module with the target region — modules auto-detect parent providers"
      ],
      "correct": 2,
      "explanation": "To pass a non-default provider (an aliased provider) to a module, use the `providers` meta-argument in the calling `module` block: `providers = { aws = aws.us-west-2 }`. This maps the child module's `aws` provider to the parent's `aws.us-west-2` alias. Modules should not define their own provider blocks in most cases (it creates a separate provider instance). Environment variables apply globally, not per-module. Passing region as a variable works but requires explicit provider configuration inside the module.",
      "tags": [
        "providers",
        "modules",
        "aliases",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-155",
      "question": "Your team wants to move a Terraform resource from one module path to another without destroying and recreating it. What is the correct approach in Terraform 1.1+?",
      "options": [
        "Use `terraform state mv <old_address> <new_address>` to update the state with the new module path",
        "Delete the resource from state with `terraform state rm` and re-import it at the new address",
        "Add a `moved` block in the Terraform configuration that declaratively maps the old address to the new one",
        "Manually edit the `terraform.tfstate` file to update the resource address path"
      ],
      "correct": 2,
      "explanation": "Terraform 1.1 introduced the `moved` block — a declarative, code-reviewable way to refactor resource addresses without destroying infrastructure. Syntax: `moved { from = module.old.aws_instance.web; to = module.new.aws_instance.web }`. Terraform processes this during `plan`/`apply` and updates the state. `terraform state mv` works but is an imperative CLI command not tracked in version control. Editing the state file directly is dangerous and unsupported.",
      "tags": [
        "moved-blocks",
        "refactoring",
        "state",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-156",
      "question": "You need to create a dynamic number of security group rules from a list of CIDR blocks. Which Terraform feature creates multiple nested blocks within a resource without code repetition?",
      "options": [
        "Use a `dynamic` block with a `for_each` argument to iterate over the CIDR list and generate each rule block",
        "Use a `for` expression to generate a map and pass it to the security group resource as a variable",
        "Use `templatefile()` function to render the nested blocks as a string and pass them to the resource",
        "Use `count` on the security group resource itself to create one rule per security group instance"
      ],
      "correct": 0,
      "explanation": "`dynamic` blocks allow you to generate a variable number of nested blocks within a resource. Syntax: `dynamic \"ingress\" { for_each = var.cidr_list; content { cidr_blocks = [ingress.value] } }`. The `content` block defines the structure of each generated block, and `ingress.value` (or `ingress.key`) accesses the iterator. `for` expressions produce values/maps but can't generate nested blocks. `count` applies to the resource level, not nested blocks. `templatefile()` generates strings, not HCL blocks.",
      "tags": [
        "dynamic-blocks",
        "meta-arguments",
        "expressions",
        "terraform-003"
      ],
      "difficulty": "hard",
      "_normalized": true
    },
    {
      "id": "tf-157",
      "question": "You have two resources where resource B needs an attribute from resource A that is only known after `apply` (not during `plan`). What happens during `terraform plan`?",
      "options": [
        "Terraform substitutes a placeholder value in the plan output to show the dependency exists",
        "Terraform fails the plan with an error since unknown values cannot be used in dependent resources",
        "Terraform shows `(known after apply)` for the dependent attribute in the plan and defers the final value to apply time",
        "Terraform requires you to use `depends_on` explicitly to handle unknown values across resources"
      ],
      "correct": 2,
      "explanation": "Terraform displays `(known after apply)` in the plan output for any attribute that depends on a value that the provider doesn't know until the resource is created (e.g., an auto-generated ID, an IP address assigned by the cloud). The plan still proceeds — Terraform knows the resource will be created and can reason about the dependency. The final value is resolved during `apply`. `depends_on` is for implicit dependencies where Terraform cannot detect the dependency automatically, not for handling unknown values.",
      "tags": [
        "planning",
        "dependencies",
        "known-after-apply",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-158",
      "question": "In Terraform 0.15+, `terraform taint` was deprecated. What is the recommended replacement to force recreation of a specific resource?",
      "options": [
        "Run `terraform destroy -target=<resource>` followed by `terraform apply -target=<resource>`",
        "Edit the resource configuration and add a dummy `triggers` variable to force a plan change",
        "Use `terraform apply -replace=<resource_address>` to force the resource to be destroyed and recreated",
        "Delete the resource from the state file with `terraform state rm` and run `terraform apply`"
      ],
      "correct": 2,
      "explanation": "`terraform apply -replace=<resource_address>` (available since Terraform 0.15.2) is the direct replacement for `terraform taint`. It marks the specified resource for replacement in the same apply run, rather than as a separate two-step taint + apply. `terraform taint` still works but is deprecated. `destroy -target` then `apply -target` works but is a two-step manual process. `state rm` followed by `apply` re-creates the resource but loses the import context.",
      "tags": [
        "taint",
        "replace",
        "cli",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-159",
      "question": "Your Terraform Cloud workspace uses a remote backend. You run `terraform plan` locally — what happens to the plan execution?",
      "options": [
        "The plan is uploaded to Terraform Cloud and executed there, streaming logs back to your terminal",
        "Terraform Cloud rejects the local plan and requires you to use the web UI to trigger runs",
        "The plan runs locally on your machine using your local credentials and state is synced to Terraform Cloud after",
        "The plan runs locally but uses the Terraform Cloud API to fetch the current state for comparison"
      ],
      "correct": 0,
      "explanation": "When using the Terraform Cloud remote backend (or HCP Terraform), running `terraform plan` locally sends the configuration to Terraform Cloud, which executes the plan in a managed runner on the Terraform Cloud infrastructure. Logs are streamed back to your terminal in real time. This ensures consistent execution environment, access to workspace variables/secrets, and run history. Plans do not execute locally when using the remote backend — your credentials are not used directly.",
      "tags": [
        "terraform-cloud",
        "remote-backend",
        "workflows",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-160",
      "question": "You use `for_each = toset([\"us-east-1\", \"us-west-2\"])` to create resources in two regions. Later you remove `\"us-east-1\"` from the set. What does Terraform do?",
      "options": [
        "Terraform destroys the `us-east-1` resource instance and leaves `us-west-2` unchanged",
        "Terraform fails the plan with an error because removing a `for_each` key requires a manual state rm first",
        "Terraform rebuilds both resources since the set order changed",
        "Terraform creates a new resource for `us-west-2` and orphans the `us-east-1` instance in state"
      ],
      "correct": 0,
      "explanation": "With `for_each`, each resource instance is keyed by the map key or set value. Removing `\"us-east-1\"` from the set means that key no longer exists in the configuration. Terraform's diff detects that `resource.example[\"us-east-1\"]` exists in state but not in the new config, so it plans a destroy for that instance only. `resource.example[\"us-west-2\"]` is unchanged. This is a key advantage of `for_each` over `count` — removal doesn't cause index renumbering and unintended replacements of other instances.",
      "tags": [
        "for_each",
        "meta-arguments",
        "state",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "tf-multi-1",
      "question": "You inherit a Terraform project that stores state in a local file. Which TWO of the following are valid reasons to migrate the state to a remote backend like S3 + DynamoDB?",
      "options": [
        "Concurrent applies from two engineers will be safely blocked by state locking",
        "Terraform will automatically encrypt resource attributes in transit between providers",
        "State is stored off the developer's laptop, surviving a disk failure",
        "Remote backends remove the need to write any provider authentication credentials"
      ],
      "correct": [
        0,
        2
      ],
      "explanation": "Remote backends (S3+DDB, Terraform Cloud, etc.) provide state locking — preventing the classic \"two applies racing\" bug. They also persist state outside any one engineer's laptop. Backends do NOT change provider transport encryption (TLS is the provider's job) and you still need provider credentials.",
      "tags": [
        "state",
        "backends",
        "terraform-003"
      ],
      "difficulty": "medium",
      "_normalized": true,
      "_lenbal": true
    }
  ]
}
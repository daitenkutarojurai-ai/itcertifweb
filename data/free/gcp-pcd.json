{
  "meta": {
    "id": "gcp-pcd",
    "name": "Google Professional Cloud Developer",
    "vendor": "Google",
    "version": "1.0.0",
    "question_count": 12,
    "last_updated": "2026-07-01",
    "notes": "Initial release — 12 questions spanning the five exam domains (design, build/test, deploy, integrate, operate). Correct positions balanced 3/3/3/3."
  },
  "questions": [
    {
      "id": "gcppcd-001",
      "question": "You are building a low-traffic Python 3.11 web API and want to pay nothing when it receives no requests, without managing a Dockerfile or a cluster. Which Google Cloud runtime is the best fit?",
      "options": [
        "Google Kubernetes Engine Autopilot (managed Kubernetes)",
        "Compute Engine managed instance group (autoscaled VMs)",
        "App Engine standard environment (managed language runtimes)",
        "Cloud Run for Anthos on GKE (Knative on-cluster)"
      ],
      "correct": 2,
      "explanation": "App Engine standard scales to zero, bills per instance-hour only while serving, and provides managed Python/Java/Go/Node/Ruby/PHP runtimes without a Dockerfile — exactly what a low-traffic API needs. GKE Autopilot removes node management but still bills for running pods, Compute Engine MIGs cannot scale to zero, and Cloud Run for Anthos runs on top of a paid GKE cluster.",
      "tags": [
        "app-engine",
        "compute-choice",
        "gcp-pcd"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "gcppcd-002",
      "question": "You must deploy a stateless container that scales to zero, keeps request latency low with occasional bursts, and requires no Kubernetes expertise. Which Google Cloud service is designed for this?",
      "options": [
        "Cloud Run fully managed services (serverless containers)",
        "Compute Engine sole-tenant nodes (dedicated hardware)",
        "GKE Standard with cluster autoscaler (node-pool scaling)",
        "Cloud Functions 1st generation (single-purpose event handlers)"
      ],
      "correct": 0,
      "explanation": "Cloud Run (fully managed) runs any HTTP container, scales to zero, supports min-instances to blunt cold starts, and hides all cluster management. GKE Standard requires you to size and pay for a node pool, sole-tenant nodes are dedicated VMs (no scale-to-zero), and Cloud Functions 1st gen is capped at 1 concurrent request per instance and targets event-driven single-purpose code rather than general containers.",
      "tags": [
        "cloud-run",
        "compute-choice",
        "gcp-pcd"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "gcppcd-003",
      "question": "A workload on GKE needs to call the BigQuery API using a specific Google service account, without mounting a JSON key. Which mechanism should the developer configure?",
      "options": [
        "A downloaded service-account key stored in a Kubernetes Secret",
        "The Compute Engine default service account on every node",
        "A user OAuth 2.0 refresh token embedded in the pod",
        "Workload Identity Federation to a GCP service account (KSA→GSA binding)"
      ],
      "correct": 3,
      "explanation": "GKE Workload Identity binds a Kubernetes service account to a Google service account so pods obtain short-lived credentials from the metadata server — no JSON key on disk, no cluster-wide privilege. Mounting a downloaded key defeats key rotation and violates the least-privilege guidance, using the node default service account grants the same permissions to every pod on the node, and embedding a user refresh token ties a personal identity to a workload.",
      "tags": [
        "gke",
        "workload-identity",
        "iam",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "gcppcd-004",
      "question": "An order-processing service must accept bursts of writes, decouple producers from consumers, and guarantee at-least-once delivery with automatic retries. Which Google Cloud service is purpose-built for this?",
      "options": [
        "Cloud Storage object-change notifications (event fan-out to Pub/Sub)",
        "Cloud Pub/Sub topic and subscription (many-to-many messaging)",
        "Cloud Tasks HTTP target queues (per-task rate/retry control)",
        "Cloud Scheduler cron jobs (time-based triggers)"
      ],
      "correct": 1,
      "explanation": "Pub/Sub decouples publishers from subscribers, absorbs write bursts, guarantees at-least-once delivery, retries failed pulls, and scales to millions of messages per second. Cloud Tasks targets asynchronous per-task workflows with per-task rate limits (better for controlled dispatch of individual jobs), Cloud Scheduler only fires on cron schedules, and Cloud Storage change notifications react to object mutations rather than acting as a general message bus.",
      "tags": [
        "pub-sub",
        "messaging",
        "gcp-pcd"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "gcppcd-005",
      "question": "Your team wants CI to build a container from a Git push and deposit versioned, vulnerability-scanned images that both Cloud Run and GKE can pull. Which pair of services is the current recommended combination?",
      "options": [
        "Cloud Build with Artifact Registry (unified artifact store with scanning)",
        "Cloud Build with the legacy Container Registry (gcr.io buckets)",
        "Jenkins on Compute Engine pushing to Cloud Storage (object bucket)",
        "Cloud Deploy pushing to Artifact Registry (delivery pipeline replaces CI)"
      ],
      "correct": 0,
      "explanation": "Cloud Build compiles, tests, and pushes images; Artifact Registry is the current unified registry (Docker, Maven, npm, Python) with built-in vulnerability scanning and IAM at the repository level. Container Registry is deprecated in favor of Artifact Registry. Cloud Storage is object storage, not a registry. Cloud Deploy handles progressive delivery of an already-built artifact — it does not replace the CI build step.",
      "tags": [
        "cloud-build",
        "artifact-registry",
        "ci-cd",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "gcppcd-006",
      "question": "A new revision of a Cloud Run service must receive only 10% of traffic while the previous revision keeps the remaining 90%, so latency and error metrics can be compared before a full rollout. What is the correct action?",
      "options": [
        "Delete the old revision so the new one instantly serves 100% of requests",
        "Create a second Cloud Run service and update DNS to weight traffic between them",
        "Deploy the new revision with `--no-traffic`, then split traffic 10/90 across the two revisions",
        "Set the new revision's minimum instances to 1 so it receives requests first"
      ],
      "correct": 2,
      "explanation": "Cloud Run supports revision-based traffic splitting: deploy the new revision without sending it traffic (`--no-traffic`), then use `gcloud run services update-traffic --to-revisions NEW=10,OLD=90` to run a canary. Deleting the old revision would remove the safety net, DNS weighting is coarse and ignores Cloud Run's built-in splitter, and min-instances only controls warmup — it does not distribute traffic between revisions.",
      "tags": [
        "cloud-run",
        "traffic-splitting",
        "canary",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "gcppcd-007",
      "question": "An application needs a globally distributed, strongly consistent relational database with horizontal write scale and ANSI SQL. Which Google Cloud database matches those requirements?",
      "options": [
        "Cloud SQL for PostgreSQL (single-region managed Postgres)",
        "Firestore in Native mode (document store, single-region strong consistency)",
        "Cloud Bigtable (wide-column NoSQL, no ANSI SQL, no ACID transactions across rows)",
        "Cloud Spanner (globally consistent, horizontally scalable, ANSI SQL)"
      ],
      "correct": 3,
      "explanation": "Cloud Spanner is the only Google Cloud database that combines global external consistency, horizontal write scale, and ANSI SQL with schema and ACID transactions. Cloud SQL is regional and vertical-scale limited. Firestore is a document store without ANSI SQL. Bigtable is a wide-column NoSQL store optimized for high-throughput single-row operations, not multi-row SQL transactions.",
      "tags": [
        "spanner",
        "databases",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "gcppcd-008",
      "question": "A microservice stores its database password as a Secret Manager secret and must retrieve the latest version at startup with the fewest moving parts and lowest blast radius. Which pattern should the developer use?",
      "options": [
        "Store the secret payload in a Cloud Storage object and read it with `storage.objectViewer`",
        "Grant the workload's service account `roles/secretmanager.secretAccessor` scoped to that specific secret, and read the `latest` version at startup",
        "Embed the password as a plaintext value in a Kubernetes ConfigMap",
        "Grant the service account project-wide `roles/owner` and fetch every secret in the project"
      ],
      "correct": 1,
      "explanation": "Best practice is a resource-scoped `secretAccessor` binding on that single secret, then `secretmanager.access(name='.../versions/latest')` at startup — least privilege, one API, automatic version pickup. Cloud Storage lacks the built-in versioning, IAM audit, and rotation UX of Secret Manager. A ConfigMap stores values in cleartext and is not encrypted like a Secret. Granting `roles/owner` grossly violates least privilege and creates a huge blast radius.",
      "tags": [
        "secret-manager",
        "iam",
        "security",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "gcppcd-009",
      "question": "A latency-sensitive HTTP endpoint occasionally spikes to 3 seconds. The developer needs to see which downstream RPCs contributed to the spike for a specific request. Which Google Cloud Observability tool should they open first?",
      "options": [
        "Cloud Profiler (statistical CPU and heap sampling)",
        "Error Reporting (aggregated exception stack traces)",
        "Cloud Monitoring uptime checks (external synthetic pings)",
        "Cloud Trace (per-request latency waterfall across spans)"
      ],
      "correct": 3,
      "explanation": "Cloud Trace records per-request latency waterfalls with spans across services and dependencies — the right tool to break a single slow request into its RPC contributors. Profiler samples CPU/heap across many requests and cannot attribute a specific one, Error Reporting groups exceptions without a latency timeline, and uptime checks only tell you whether an endpoint is reachable from external probers.",
      "tags": [
        "cloud-trace",
        "observability",
        "gcp-pcd"
      ],
      "difficulty": "easy",
      "_normalized": true
    },
    {
      "id": "gcppcd-010",
      "question": "A Cloud Function must run daily at 03:00 UTC to clean up expired records. Which combination is the recommended trigger?",
      "options": [
        "A Compute Engine VM running `cron` and calling the function URL with curl",
        "App Engine `cron.yaml` targeting the function's HTTPS endpoint from a Standard app",
        "A Cloud Storage object created every night to fire an object-finalize trigger",
        "Cloud Scheduler publishing to a Pub/Sub topic that the function subscribes to"
      ],
      "correct": 3,
      "explanation": "The recommended serverless pattern is Cloud Scheduler → Pub/Sub → Cloud Function: Scheduler owns the cron expression, Pub/Sub gives at-least-once delivery with retries, and the function stays event-driven. A cron VM defeats the point of serverless and adds an always-on cost, `cron.yaml` requires an App Engine Standard app just to fire a schedule, and manufacturing a nightly object to trigger `google.cloud.storage.object.v1.finalized` is a hack that couples cleanup to storage-side events.",
      "tags": [
        "cloud-functions",
        "cloud-scheduler",
        "pub-sub",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "gcppcd-011",
      "question": "A GKE Deployment must roll out a new image so that no more than one pod is unavailable at any time and at most one extra pod exists during the rollout. Which Deployment strategy configuration achieves this?",
      "options": [
        "`strategy: Recreate` (terminate all pods before creating new ones)",
        "`strategy: RollingUpdate` with `maxUnavailable: 1` and `maxSurge: 1`",
        "`strategy: RollingUpdate` with `maxUnavailable: 100%` and `maxSurge: 0`",
        "`strategy: Blue/Green` (create a second Deployment and swap Service selectors)"
      ],
      "correct": 1,
      "explanation": "`RollingUpdate` with `maxUnavailable: 1` and `maxSurge: 1` guarantees at most one pod is being replaced and at most one extra pod runs during the transition. `Recreate` tears down every pod first and violates the availability requirement, `maxUnavailable: 100%` allows all pods to go down at once, and Blue/Green is a manual pattern (not a built-in Kubernetes `strategy` value) requiring extra tooling to orchestrate.",
      "tags": [
        "gke",
        "deployments",
        "rolling-update",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    },
    {
      "id": "gcppcd-012",
      "question": "During a load test, one specific SQL query dominates the CPU profile of a Cloud SQL instance. The developer wants to identify the query and its execution plan without shelling into the database. Which built-in feature should they use?",
      "options": [
        "Cloud Profiler (statistical CPU sampling of the application process)",
        "Cloud Trace (per-request span waterfall of RPC hops)",
        "Cloud SQL Query Insights (top queries, plans, and per-query latency)",
        "Cloud Logging error buckets (grouped log-based error counts)"
      ],
      "correct": 2,
      "explanation": "Query Insights is the Cloud SQL feature that surfaces top queries by load, execution plans, and per-query latency directly in the console — no shell required. Cloud Profiler analyses application-process CPU/heap, not database internals. Cloud Trace shows RPC hop timing but does not decompose a SQL execution plan. Log-based error buckets aggregate errors, not query performance.",
      "tags": [
        "cloud-sql",
        "query-insights",
        "observability",
        "gcp-pcd"
      ],
      "difficulty": "medium",
      "_normalized": true
    }
  ]
}

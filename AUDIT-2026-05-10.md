# Audit 2026-05-10 — CertQuests (itcertifweb)

## Auto-fixed (this run)
- [46986a8] audit: fix broken /src/assets/logo.jpg refs in kubernetes cheat-sheet article
- [f37daa9] audit: add 9 missing pages to sitemap.xml

## TODO — manual attention

### Security
- npm audit not applicable (static-site repo, no package.json).
- Secret scan clean: no live keys, no `.env*` tracked, no obvious leaks. Two grep hits were false positives (legitimate prose about AWS leaks, plus IAM-policy example code in a question pack).

### SEO
- **No `/learning/` hub page**. Many news articles link to `/learning/` (header + footer nav), but there is no `learning/index.html`. Live URL returns 404. The 28 individual `/learning/<id>/` pages exist and are in the sitemap. Recommend creating a hub page modelled on `courses/index.html`.
- **No `/certifications/` hub page**. Each vendor (cisco/aws/microsoft/...) has its own page, but `certifications/index.html` is missing. No internal links currently target `/certifications/` — this is an SEO opportunity, not a broken link.
- **5 missing course HTML pages** present in `Certquestsapp/website/courses/` but missing here: `aws-clf-c02.html`, `az-900.html`, `ccna-200-301.html`, `comptia-security-plus.html`, `terraform-associate.html`. The Certquestsapp build pipeline will overwrite this repo on next deploy — no action required from this side.
- Title/description/canonical/OG/JSON-LD coverage on sampled pages is excellent (3 JSON-LD blocks on the homepage; OG and Twitter Card on every news article).

### Content quality
- All footer/copyright text already says "© 2026" — no stale dates.
- 2024 references in news/career articles are legitimate historical context (events that occurred in 2024); nothing stale.
- All `<img>` tags scanned have `alt` attributes; no thin (<300 word) pages observed in spot checks.

### Bugs
- No build/test/typecheck tooling in this repo (static site). Validation depends on the build that runs in `Certquestsapp` (the source repo). See that repo's audit for the broken `scripts/generate-course-pages.js`.
- Many orphan branches on origin (e.g. `agent/rewrite-comptia-security-plus-1778373278`, `careers/helpdesk-to-cloud-engineer-roadmap-2026`, `harden-aws-clf-c02`). Consider sweeping with `gh api -X DELETE repos/.../git/refs/heads/<branch>` for any merged PR branches.

### Performance
- `src/` total weight: 2.2 MB — fine for a static site.
- No oversized images (>500 KB) in `public/`-equivalent locations.
- No blocking `<script src=…>` tags in `<head>` on sampled pages.
- Service worker (`sw.js`) follows a network-first strategy for HTML. Sensible.

### Accessibility
- Form inputs in `contact.html` and `index.html` have `<label>` elements (newsletter input uses `.visually-hidden` label).
- All scanned `<img>` have `alt` text; no empty `<button></button>` elements.
- Color contrast not auditable from static analysis — recommend a manual Lighthouse pass on `index.html`, `train.html`, and a representative news article.

## Scores (1–10, 10 = perfect)
- Security: 10
- SEO: 8 (sitemap now complete, but `/learning/` and `/certifications/` hub pages missing)
- Content: 9
- Stability: 9 (no build tooling to validate, but the static output is consistent)
- Performance: 9
- Accessibility: 9
- **Overall: 9**

## Trend vs last week
First audit run — no prior `AUDIT-*.md` to compare against. Establishes baseline.

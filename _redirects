/stats.html  /profile.html  301

# Duplicate Vault Associate 002 news page (created 2026-07-16 by scheduled-agent,
# same topic already lives at /news/hashicorp-vault-associate-002-2026/ since 2026-06-25).
# Consolidate SEO signals onto the earlier URL which is the one in sitemap.xml.
/news/vault-associate-002-2026/  /news/hashicorp-vault-associate-002-2026/  301
/news/vault-associate-002-2026  /news/hashicorp-vault-associate-002-2026/  301

# Phase 5.10: bare /train.html is no longer a pack picker —
# /certifications/ is the single browse surface. We can't 301 here:
# Cloudflare Pages _redirects matches on path only and would also
# rewrite /train.html?pack=… (the live quiz runtime), breaking every
# active session. The redirect therefore lives in train.html itself
# (top-of-head script) — fires only when no `?pack=` is present.

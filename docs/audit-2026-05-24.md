# Audit 2026-05-24 — CertQuests (itcertifweb)

Weekly scheduled sweep. Working tree was clean on `main` (1 commit pulled
since last week — `6821aa4 feat(compare): add AWS DVA-C02 vs SAA-C03
comparator`).

## Auto-fixed (this run)

### SEO 🟡 → ✅
- **sitemap.xml**: added **14 missing pages** found on disk but absent
  from the index. Site now publishes **224 URLs** (was 210) with zero
  duplicates. The drift was almost entirely the week's new `/news/*`
  content batch (10 cert briefs) plus 3 careers pages and the
  `/cheatsheets/` hub root.
  - `/cheatsheets/`
  - `/news/aws-developer-associate-dva-c02/`
  - `/news/aws-machine-learning-engineer-associate-mla-c01/`
  - `/news/aws-solutions-architect-professional-sap-c02/`
  - `/news/aws-sysops-administrator-soa-c02/`
  - `/news/azure-az305-solutions-architect-expert-2026/`
  - `/news/cissp-2026/`
  - `/news/comptia-pentest-plus-pt0-003/`
  - `/news/gcp-professional-cloud-architect-2026/`
  - `/news/isc2-ccsp-2026/`
  - `/news/terraform-associate-003-2026/`
  - `/careers/ccna-200-301-interview-questions-2026/`
  - `/careers/comptia-network-plus-worth-it-2026/`
  - `/careers/platform-engineer-2026/`
- **Open Graph + Twitter Card** added to three hub pages that had
  canonical + `<meta description>` but no social-share metadata. Now
  every share link on these surfaces will render a rich preview:
  - `/compare/` (comparator hub)
  - `/salaire/` (salary hub)
  - `/fail-analysis/` (post-mortem hub)

XML well-formedness re-validated after the sitemap edit (`xml.etree`,
no parse errors). `npm test` re-run after all edits: **120/120 passing**.

## Open — needs manual attention

### Security 🟢
- **Secret scan: clean.** Only grep hit is the legitimate `AKIA...`
  prose in `news/aws-72k-bill-overnight/index.html:310` (educational
  article about leaked-key scanners). Same documented false positive
  as the last 3 audits.
- `.gitignore` covers `.env*`, `.dev.vars*`, `.wrangler` — verified.
  No tracked credential files.
- `src/auth.js:30-33` Supabase config still safe: decoded JWT shows
  `"role":"anon"` (public key). Service-role key not in tree.
- **76 `.innerHTML =` sites in `src/*.js`** — no new high-risk additions
  spotted since 2026-05-17. Spot-checked top diff candidates: still
  template-literals over trusted internal state, no user-content sinks.
- No `eval(` / `new Function(` anywhere in `src/`.
- External JS surface is **one** pinned import: `esm.sh/@supabase/supabase-js@2.50.0`
  in `src/auth.js`. No CDN drift. No `http://` external resources.

### SEO 🟡
- **Title-length overruns** (>60 chars truncates in SERPs).
  **165 / 229 pages** fail. Worst offenders this week:
  - `train.html` (66), `study-planner/index.html` (71),
    `prompt-dungeon/index.html` (72),
    `prompt-dungeon/homelab-architect/` (64),
    `prompt-dungeon/youtube-shorts-tech/` (67),
    `cheatsheets/index.html` (65),
    `tool-radar/meilleur-vps-cloud-hosting/` (67).
  - **Skipped — reason:** title rewrites are copywriting, outside the
    auto-fix mandate.
- **Meta-description overruns** (>160 chars get truncated).
  **139 / 229 pages** fail. Worst offenders:
  - `index.html` (239), `train.html` (231), `path.html` (187),
    `prompt-dungeon/index.html` (174),
    several `prompt-dungeon/<slug>/` pages in the 189–213 range.
  - **Skipped — reason:** copy decision; needs editorial trim.
- **404.html**: no canonical link. **Skipped — reason:** intentional
  for an error page (canonicalising 404 → 404 is a no-op; preventing
  it from accidentally signalling itself as the canonical for a real
  URL is the safer default).
- **OG missing on 4 utility pages**: `profile.html`,
  `reset-password.html`, `404.html`, `stats.html`. **Skipped — reason:**
  authenticated/error/redirect surfaces, not meant to be shared.
- `robots.txt` correct (allow-all except `/stats.html`, sitemap line
  present).
- `<html lang>` present on every page in the deployed tree.

### Question-bank integrity 🟢
- Validated all **64 packs / 2,848 questions** (up from 56 packs / 2,762
  questions last week — 8 new packs, +86 questions). **Zero structural
  issues**: every question has ≥2 options, valid `correct` index (or
  array, in-range), non-empty `explanation`, no missing `id`.

### Content datasets 🟢
- `data/career-paths/_index.json`: **15 archetypes**, all archetype JSON
  files exist on disk.
- `data/devstack/`: **12 dossiers** + schema doc — all parse, all
  required fields (`slug`, `company`, `vertical`, `tldr`, `stack`,
  `sources`) present.
- `data/prompts/`: **9 workflows** + schema doc — all parse, all required
  fields (`slug`, `title`, `tldr`, `prompts`) present.
- `data/games/`: schema + **7 mode folders** all present
  (acronym-decoder, boss-fight, break-architecture, lightning-round,
  match-pair, scenario-builder, true-false-blitz).
- `data/fail-analysis/`: **11 dossiers** + schema. Filesystem clean.

### Hub link integrity 🟢
- `careers/index.html`: **45 internal links checked, 0 broken.**
- `index.html`: home links all resolve.
- `career-path/index.html`: archetype cards are JS-rendered from
  `_index.json` at runtime — manifest is in sync (15/15), nothing to
  flag.
- `compare/index.html`: lists all **14** comparator subdirs (brief
  predates 3 new ones — boson-alternatives, aws-clf vs az-900 vs
  gcp-cdl, aws-dva-c02 vs aws-saa-c03 added since brief was written).

### Bugs / Code quality 🟢
- Parse-checked the JS modules modified in the last 7 days. All clean.
- Unit tests: **120/120 passing** (`npm test`).
- `git grep` for `TODO BUG | FIXME | XXX` in `src/*.js` and `*.html`:
  **zero hits**.

### Performance 🟡
- Every `<img>` without `loading="lazy"` is a header favicon
  (above-the-fold, ~228/234 imgs). Below-fold images already lazy.
  **Nothing to auto-fix.**
- Every `<img>` has an `alt` attribute. **Nothing to auto-fix.**
- Every `target="_blank"` link has `rel="..."`. **Nothing to auto-fix.**
- **Render-blocking scripts in `<head>`: 0** across all 229 pages.
- `src/styles/main.css` is **120 KB**, `src/styles/desktop.css` is
  **103 KB** — combined ~223 KB, up from ~215 KB last week. **Open —
  needs manual triage:** same recommendation as 2026-05-17: a route-split
  into `desktop.css` / `path.css` / `mobile.css` is the right fix, but
  the rules are intermixed with mobile-shell + game-HUD so an automatic
  PurgeCSS pass isn't safe.
- `index.html` inline `<style>` totals **85 KB across 2 blocks**
  (improved from 112 KB last week — confirmed via byte count). Per
  `CLAUDE.md` this is intentional (the `mobile-v10-override` cascade
  gate must load before external CSS). **Skipped — documented
  architectural choice.**

### Repo hygiene
- **22 orphan branches on `origin`** (1 new since last week:
  `audit/weekly-2026-05-17`). None auto-deleted because the previous
  agents may still be referencing them. Recommend a one-shot manual
  sweep: keep only `main` + actively-in-flight branches.

## Scores (1–10)
- Security: **10**
- SEO: **9** (sitemap re-synced; OG holes patched on hubs;
  title/description overrun stack still uncapped)
- Content: **10** (zero question-bank defects across 2,848 questions;
  all 4 data folders schema-clean)
- Stability: **10** (120/120 tests passing)
- Performance: **8** (CSS bundles still trending up; inline style
  improved 24%)
- Accessibility: **9** (every image has alt, every link has discernible
  text, lang attr universal)
- **Overall: 9.3**

## Trend vs last week (AUDIT-2026-05-17.md)
- **+0.1 net.** Last week was 9.2; this week is 9.3.
- **Wins:** sitemap drift fixed for the third week in a row (this time
  +14 URLs covering an entire new `/news/*` content batch); 3 hub pages
  gained OG/Twitter cards; question bank grew by 8 packs / 86 questions
  with zero new defects; index.html inline style trimmed 27 KB.
- **Carries over:** title/description length discipline, orphan
  branches, CSS bundle creep. These are now a 3-audit pattern — would
  benefit from a one-shot manual sweep rather than re-flagging weekly.

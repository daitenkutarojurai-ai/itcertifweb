# Session Notes — 2026-04-13

## What was done this session

### 1. Docker DCA Certification — NEW pack (60 questions)

**File created:** `data/free/docker-dca.json`

60 exam-quality questions covering all DCA exam domains:
- Container fundamentals, image vs container lifecycle, OCI runtime
- Dockerfile instructions (CMD/ENTRYPOINT, multi-stage, ARG vs ENV, USER, HEALTHCHECK, COPY vs ADD, layer caching)
- Storage: named volumes, bind mounts, tmpfs, `-v` vs `--mount` syntax, volume plugins
- Networking: bridge/overlay/host/none/macvlan, custom bridge DNS, port publishing, routing mesh
- Docker Swarm: init, services vs tasks, global mode, scale, drain, Raft quorum (2F+1 formula), 3-manager recommendation, secrets, configs, rolling updates, `--update-failure-action rollback`
- Security: Docker Content Trust (DCT), Linux capabilities, namespaces, cgroups, seccomp, `--read-only`, `--privileged` anti-pattern
- CLI: `exec`, `logs`, `stats`, `inspect`, `cp`, `save` vs `export`, `prune`, `history`, `network connect`, `docker commit` anti-pattern
- Docker Compose: `depends_on` caveat (starts but doesn't wait for healthy), resource limits

**index.json updated:** docker-dca → `available: true`, `question_count: 60`

**certifications/linux-devops.html updated:**
- Docker DCA tile: "Coming soon" → "Live now", link to `/train.html?pack=docker-dca`
- Hero eyebrow: "2 LIVE EXAMS" → "3 LIVE EXAMS"
- Hero stats: 500+ → 560+ questions, 2 → 3 live exams
- JSON-LD description updated to reflect 60 questions

---

### 2. Splunk Core Power User — REWORK (50 → 65 questions)

**File rewritten:** `data/free/splunk-core.json` (version 2.0.0)

Upgraded from 50 old study-note-style questions to 65 proper exam-style MCQs:
- SPL commands: dedup, stats, timechart, chart, top/rare, head/tail, sort, where, table, fields, rename, eval, rex
- eval functions: if(), case(), length(), split(), mvindex(), multivalue functions
- Stats aggregation: count, avg, dc(), values() vs list(), streamstats, eventstats
- Time modifiers: `@d`, `earliest/-60m@m`, relative time snapping
- Knowledge objects: field aliases, calculated fields, event types, tags, macros, workflow actions
- Lookups: CSV lookup, KV Store lookup, automatic lookup, inputlookup, outputlookup
- Reports and alerts: scheduled vs real-time, trigger conditions, throttling, report acceleration
- Dashboards: panels, tokens, auto-refresh
- Data inputs: file monitor, HEC, forwarders
- Permissions: User/Power User/Admin roles, App vs Global sharing
- Performance: tstats, search modes (Fast/Smart/Verbose), summary indexing, search optimization
- Advanced: subsearch (timeout/limit), transaction, join, appendcols, bin/bucket, geostats, mvexpand

**index.json updated:** splunk-core → `question_count: 65`, `full_count: 65`

---

### 3. Docker DCA Learning Course — NEW page

**File created:** `learning/docker-dca/index.html`

6-module accordion course with vendor color `#2496ED` (Docker blue):

| Module | Topic | Time |
|---|---|---|
| 01 | Container Fundamentals & Docker Architecture | ~3h |
| 02 | Images & Dockerfile Mastery | ~5h |
| 03 | Storage: Volumes, Bind Mounts & tmpfs | ~3h |
| 04 | Docker Networking: Bridge, Overlay & Host | ~4h |
| 05 | Docker Swarm: Orchestration at Scale | ~5h |
| 06 | Container Security & Production Best Practices | ~5h |

Features:
- Mid-course Spotify CTA (show ID: 68htxokT392uDDDkMQV4pp)
- Bottom dual CTA: `/train.html?pack=docker-dca` quiz button + Spotify podcast link
- Code examples for every major concept
- Study notes and exam tip callout boxes
- Structured data: BreadcrumbList + Course JSON-LD

**data/courses.json updated:** added docker-dca entry (vendor: Docker, color: #2496ED, 6 modules, 25h)

**sitemap.xml updated:** added `/learning/docker-dca/` (2026-04-13)

---

### 4. GitHub Pages HTTPS Fix (session 2 — carried over)

**`.github/workflows/static.yml`** — removed `actions/configure-pages@v5` step.
This action was making a GitHub Pages API call on every deploy which reset `protected_domain_state` to null, triggering TLS re-validation each time. Fix: remove the step; the CNAME file handles the domain configuration.

Note: To fully resolve HTTPS, the custom domain must also be **verified** at the GitHub account level (Settings > Pages > Add a verified domain) with a DNS TXT record:
`_github-pages-challenge-daitenkutarojurai-ai.certquests.com`

---

## Files changed

| File | Change |
|---|---|
| `data/free/docker-dca.json` | NEW — 60 DCA questions |
| `data/free/splunk-core.json` | REWRITE — 65 questions (v2.0.0) |
| `data/index.json` | docker-dca: available=true, q=60; splunk-core: q=65 |
| `certifications/linux-devops.html` | DCA tile live; hero stats updated |
| `learning/docker-dca/index.html` | NEW — 6-module course page |
| `data/courses.json` | Added docker-dca entry |
| `sitemap.xml` | Added /learning/docker-dca/ |
| `CLAUDE.md` | Updated catalog table + Docker DCA section |
| `docs/session-2026-04-13.md` | This file |

## Previous session (2026-04-11/12) summary

- **GCP ACE** added: 60 questions, certifications/google-cloud.html, learning/gcp-ace/ (6 modules)
- **CySA+** reworked: 30 → 55 questions
- 6 learning course pages added: AZ-900, CompTIA A+, Fortinet NSE4, Kubernetes CKAD, RHCSA, Terraform
- GitHub Actions HTTPS fix: removed configure-pages@v5 step

# Audit 2026-05-11 — CertQuests (itcertifweb)

## Auto-fixed (this run)
- [828eeb6] audit: dedup sitemap /news/, drop noindex stats.html

## TODO — manual attention

### Security
- No `package.json` (static-site repo). `npm audit` not applicable here.
- Secret scan: clean. Two grep hits are false positives (legitimate prose about AWS-key scanners in `news/aws-72k-bill-overnight/index.html`, and an IAM-policy example string inside `data/free/terraform-003.json`). No `.env*` files tracked. No live keys.
- Web3Forms `access_key` value in `contact.html` is a public form-recipient identifier, not a sensitive secret — expected for client-side form submissions.

### SEO
- **Still no `/learning/` hub page.** 19 `.html` files link to `/learning/` (header + footer nav) but `learning/index.html` does not exist. Live URL returns 404. Same finding as 2026-05-10. Needs human design decision (mirror `courses/index.html` shape).
- **Still no `/certifications/` hub page.** Per-vendor pages exist (`certifications/aws.html`, etc.) but no `certifications/index.html`. No internal links target it currently — pure SEO opportunity, not a broken link.
- **5 course pages still missing here.** `Certquestsapp/website/courses/` contains `aws-clf-c02.html`, `az-900.html`, `ccna-200-301.html`, `comptia-security-plus.html`, `terraform-associate.html` — none of these are in `itcertifweb/courses/`. The deploy workflow (`deploy-website.yml` on `Certquestsapp`) was supposed to mirror them but hasn't. Either the workflow hasn't fired since the course pages were added, or it's failing. Recommend a `workflow_dispatch` run or investigation of recent Actions logs.
- Sitemap now contains 80 URLs after dedup; all entries point to existing pages (verified by scanning `*.html` files vs `<loc>` entries; only `404.html` and `docs/email-templates/*` are intentionally excluded).
- Title/description/canonical/OG/JSON-LD coverage on sampled pages remains excellent.

### Content quality
- `git log --since='7 days ago' --name-only -- careers/ news/ data/` shows fresh content (`careers/aws-saa-c03-interview-questions-2026/`, `news/aws-ai-practitioner-aif-c01/`, `data/free/aws-mla-c01.json`, hardened distractors across multiple packs). All sampled pages have proper SEO metadata and word counts well above the 300-word floor.
- All footer/copyright text says "© 2026". 2024 references are legitimate historical context (events that occurred in 2024). No stale dates.
- All `<img>` tags scanned have `alt` attributes. No empty alts.

### Bugs
- No build/test/typecheck tooling in this repo by design (static deploy target).
- Internal link check: every `href="/…"` resolves to an actual file/directory. No broken links found.

### Performance
- Site size: 14 MB total, `src/` 2.2 MB, `data/` 4.0 MB. Reasonable for the content footprint.
- Largest images: 3 × 384 KB icons (`icon-512.png`, `favicon-512.png`, `logopouple.png`). All under the 500 KB threshold.
- No oversized images (>500 KB) anywhere in the tree.
- Inline `<script>` tags in `index.html` `<head>` are all `application/ld+json` (non-blocking). Real JS sits below `<body>` and uses `type="module"` (defer-equivalent).
- Service worker uses network-first for HTML — sensible.

### Accessibility
- Form inputs in `contact.html` and the newsletter widget on `index.html` have proper `<label>` associations (newsletter uses `.visually-hidden` label).
- All `<button>` elements scanned have discernible text. No empty buttons.
- All `<img>` have `alt` text.
- Color contrast not auditable from static analysis — recommend a manual Lighthouse pass on `index.html`, `train.html`, and one news article.

### Repo hygiene (carry-over from last week)
- Many orphan branches on `origin` (e.g. `agent/rewrite-aws-saa-c03-*` × 6, `agent/rewrite-comptia-security-plus-*` × 5, `careers/helpdesk-to-cloud-engineer-roadmap-2026`, `harden-aws-clf-c02`, `add-aws-mla-c01-pack`, `agent/harden-gcp-ace-20260511`). Each maps to a merged or abandoned PR. Manual sweep recommended — not auto-deleted because some may still be open. Listed in last week's audit too; no progress.

## Scores (1–10, 10 = perfect)
- Security: 10
- SEO: 9 (sitemap deduped; `/learning/` + `/certifications/` hubs still absent)
- Content: 9
- Stability: 9
- Performance: 9
- Accessibility: 9
- **Overall: 9.2**

## Trend vs last week
- Previous: AUDIT-2026-05-10.md — Overall 9.
- This run: +0.2. Sitemap now de-duplicated and noindex page removed. SEO score nudged from 8 → 9. All other categories steady. Three carry-over items unchanged: `/learning/` hub, `/certifications/` hub, 5 course pages missing from deployed site.

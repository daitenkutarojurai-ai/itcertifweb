{
  "name": "certquests-web",
  "version": "1.0.0",
  "private": true,
  "description": "CertQuests — static IT certification practice site.",
  "scripts": {
    "test": "node --test test/",
    "gen-paths": "node scripts/gen-paths.js",
    "build-core": "node scripts/build-core.js",
    "prebuild": "npm run build-core"
  },
  "engines": {
    "node": ">=18"
  }
}
